TRAE AI IDE IMPLEMENTATION INSTRUCTIONS - SERVICES PAGE ATOMIC ORBITAL BACKGROUNDS
==================================================================================

OBJECTIVE: Implement elegant atomic orbital watermark backgrounds for the Services page, replacing existing backgrounds with new atomic orbital designs that match the scientific/atomic aesthetic of the Services icon theme.

ATOMIC ORBITAL IMAGE ASSETS PROVIDED:
- services-hero-atomic.png (Coral-teal atomic orbital for hero section)
- services-solutions-atomic.png (Teal-coral atomic orbital for services section) 
- services-contact-atomic.png (Compact atomic orbital for bottom sections)

STEP 1: CLEAN EXISTING CONFLICTING CODE
======================================

REMOVE these existing CSS rules completely from the Services HTML file:

1. Find and REMOVE the existing "TARGET ICON BACKGROUND WATERMARKS" section (around lines 1500-1700):
   - Remove: #imc-hero::before { ... } (existing SVG background)
   - Remove: #imc-hero::after { ... } (existing radial gradient)
   - Remove: .services::before { ... } (existing SVG background)
   - Remove: .services::after { ... } (existing radial gradient)

2. Keep the positioning rules (DO NOT REMOVE):
   - Keep: #imc-hero, .services { position: relative; overflow: hidden; }
   - Keep: #imc-hero > *, .services > * { position: relative; z-index: 1; }

STEP 2: IMPLEMENT NEW ATOMIC ORBITAL BACKGROUNDS
===============================================

ADD this exact CSS code to replace the removed sections:

```css
/* ===== ATOMIC ORBITAL BACKGROUNDS ===== */

/* Hero Section - Coral-Teal Atomic Orbital */
#imc-hero::before {
    content: '';
    position: absolute;
    top: 18%;
    right: -12%;
    width: 700px;
    height: 700px;
    background-image: url('assets/services-hero-atomic.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.08;
    transform: rotate(15deg);
    pointer-events: none;
    z-index: 0;
    animation: orbital-rotate 180s linear infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.8) 30%,
        rgba(0,0,0,0.5) 60%,
        rgba(0,0,0,0.2) 80%,
        rgba(0,0,0,0.05) 92%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.8) 30%,
        rgba(0,0,0,0.5) 60%,
        rgba(0,0,0,0.2) 80%,
        rgba(0,0,0,0.05) 92%,
        transparent 100%
    );
}

/* Services Section - Teal-Coral Atomic Orbital */
.services::before {
    content: '';
    position: absolute;
    top: 25%;
    left: -8%;
    width: 750px;
    height: 750px;
    background-image: url('assets/services-solutions-atomic.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.07;
    transform: rotate(-30deg);
    pointer-events: none;
    z-index: 0;
    animation: orbital-rotate-reverse 150s linear infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.75) 25%,
        rgba(0,0,0,0.4) 55%,
        rgba(0,0,0,0.15) 75%,
        rgba(0,0,0,0.03) 90%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.75) 25%,
        rgba(0,0,0,0.4) 55%,
        rgba(0,0,0,0.15) 75%,
        rgba(0,0,0,0.03) 90%,
        transparent 100%
    );
}

/* Process Section - Add Subtle Atomic Orbital */
.process::before {
    content: '';
    position: absolute;
    bottom: 20%;
    right: 5%;
    width: 580px;
    height: 580px;
    background-image: url('assets/services-contact-atomic.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.05;
    transform: rotate(50deg);
    pointer-events: none;
    z-index: 0;
    animation: orbital-float 120s ease-in-out infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.7) 35%,
        rgba(0,0,0,0.3) 65%,
        rgba(0,0,0,0.08) 82%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.7) 35%,
        rgba(0,0,0,0.3) 65%,
        rgba(0,0,0,0.08) 82%,
        transparent 100%
    );
}

/* Investment Levels Section - Add Compact Atomic Orbital */
.investment-levels::before {
    content: '';
    position: absolute;
    top: 30%;
    left: 8%;
    width: 520px;
    height: 520px;
    background-image: url('assets/services-contact-atomic.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.045;
    transform: rotate(-20deg);
    pointer-events: none;
    z-index: 0;
    animation: orbital-pulse 130s ease-in-out infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.6) 40%,
        rgba(0,0,0,0.25) 70%,
        rgba(0,0,0,0.06) 87%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.6) 40%,
        rgba(0,0,0,0.25) 70%,
        rgba(0,0,0,0.06) 87%,
        transparent 100%
    );
}

/* Atomic Orbital Animations */
@keyframes orbital-rotate {
    0% { transform: rotate(15deg); }
    100% { transform: rotate(375deg); }
}

@keyframes orbital-rotate-reverse {
    0% { transform: rotate(-30deg); }
    100% { transform: rotate(-390deg); }
}

@keyframes orbital-float {
    0%, 100% { 
        transform: rotate(50deg) translateY(0px);
        opacity: 0.05;
    }
    50% { 
        transform: rotate(52deg) translateY(-10px);
        opacity: 0.07;
    }
}

@keyframes orbital-pulse {
    0%, 100% { 
        transform: rotate(-20deg) scale(1);
        opacity: 0.045;
    }
    50% { 
        transform: rotate(-18deg) scale(1.03);
        opacity: 0.065;
    }
}

/* Ensure all sections have proper positioning */
#imc-hero,
.services,
.process,
.investment-levels {
    position: relative;
    overflow: hidden;
}

/* Ensure content stays above watermarks */
#imc-hero > *,
.services > *,
.process > *,
.investment-levels > * {
    position: relative;
    z-index: 10;
}

/* Mobile Responsive Adjustments */
@media (max-width: 768px) {
    #imc-hero::before {
        width: 500px;
        height: 500px;
        right: -20%;
        opacity: 0.06;
    }

    .services::before {
        width: 550px;
        height: 550px;
        left: -15%;
        opacity: 0.05;
    }

    .process::before {
        width: 400px;
        height: 400px;
        right: 0%;
        opacity: 0.04;
    }

    .investment-levels::before {
        width: 350px;
        height: 350px;
        left: 3%;
        opacity: 0.035;
    }
}

@media (max-width: 480px) {
    #imc-hero::before {
        width: 380px;
        height: 380px;
        opacity: 0.05;
    }

    .services::before {
        width: 420px;
        height: 420px;
        opacity: 0.04;
    }

    .process::before {
        width: 320px;
        height: 320px;
        opacity: 0.035;
    }

    .investment-levels::before {
        width: 280px;
        height: 280px;
        opacity: 0.03;
    }
}
```

STEP 3: VERIFY IMPLEMENTATION
============================

After implementation, verify these requirements are met:

1. NO old target/SVG backgrounds visible
2. Hero section shows subtle coral-teal atomic orbital in top-right area
3. Services section shows subtle teal-coral atomic orbital in top-left area  
4. Process section shows subtle compact atomic orbital in bottom-right area
5. Investment section shows subtle atomic orbital in top-left area
6. All atomic orbital backgrounds are very subtle scientific watermarks
7. All content remains fully readable and properly positioned above backgrounds
8. Mobile responsive behavior maintained across all screen sizes

CRITICAL SUCCESS CRITERIA:
- Result matches atomic orbital design inspiration
- Atomic orbital backgrounds are elegant scientific watermarks, not prominent design elements
- All existing functionality and layout preserved
- Consistent atomic/scientific aesthetic across all major sections

ASSET REQUIREMENTS:
- Place atomic orbital image files in assets/ folder relative to HTML file
- services-hero-atomic.png → Coral-teal atomic orbital for hero section
- services-solutions-atomic.png → Teal-coral atomic orbital for services section  
- services-contact-atomic.png → Compact atomic orbital for process/investment sections

TROUBLESHOOTING:
- If atomic orbital images don't appear: verify file paths and names match exactly
- If old backgrounds still visible: ensure ALL existing ::before and ::after pseudo-elements are removed from #imc-hero and .services sections
- If backgrounds too prominent: verify opacity values are exactly as specified (0.045-0.08)
- If mobile issues: ensure responsive media queries are included exactly as provided

FINAL VERIFICATION CHECKLIST:
□ Old target/SVG backgrounds completely removed
□ Elegant atomic orbital watermarks visible but subtle
□ Hero orbital: coral-teal, top-right, slow orbital rotation
□ Services orbital: teal-coral, top-left, reverse orbital rotation
□ Process orbital: compact design, bottom-right, orbital float
□ Investment orbital: compact design, top-left, orbital pulse
□ Mobile responsive across all screen sizes
□ All content readable and properly positioned
□ Atomic/scientific aesthetic consistent throughout