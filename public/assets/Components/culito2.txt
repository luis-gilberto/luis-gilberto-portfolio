/* ===== Timeline Option A+ (Minimal + Peek) ===== */
.timeline { position: relative; }
.timeline .timeline-title { color:#F96F6E; letter-spacing:.08em; }

/* Compact head row */
.tl-head {
  display:flex; justify-content:space-between; align-items:baseline;
  gap:.75rem; margin-bottom:.15rem;
}
.tl-year {
  font-weight:700; font-size:.92rem; color:#eaeaea;
}
.tl-title {
  font-size:.9rem; color:rgba(245,245,245,.92);
  white-space:nowrap; overflow:hidden; text-overflow:ellipsis;
  max-width: 60%;
}

/* Subtle left accent for each item (alternates) */
.timeline-item {
  position:relative; padding:.4rem .25rem .6rem .75rem; border-left:2px solid rgba(255,255,255,.07);
  transition: transform .18s ease, border-color .18s ease, background .18s ease;
  border-radius: 4px;
}
.timeline-item:nth-child(odd) { border-left-color: rgba(249,111,110,.25); }   /* coral hint */
.timeline-item:nth-child(even){ border-left-color: rgba(46,211,198,.22); }     /* teal hint */
.timeline-item:hover { transform: translateX(2px); background: rgba(255,255,255,.03); }

/* Peek button (iconless, tiny) */
.tl-peek {
  appearance:none; border:1px solid rgba(255,255,255,.18); background:transparent; color:#ccc;
  font-size:.72rem; padding:.18rem .5rem; border-radius:999px; cursor:pointer;
  transition: background .18s ease, color .18s ease, border-color .18s ease;
}
.tl-peek:hover, .tl-peek:focus-visible { background: rgba(249,111,110,.10); color:#fff; border-color: rgba(249,111,110,.45); }

/* Floating peek card (hidden by default) */
.tl-more {
  position:absolute; left:calc(100% + 10px); top:50%; transform:translateY(-50%);
  min-width:180px; max-width:220px; padding:.6rem .7rem; font-size:.82rem; line-height:1.35;
  background: rgba(26,26,26,.95); border:1px solid rgba(249,111,110,.25); border-radius:.5rem;
  color:rgba(245,245,245,.88); box-shadow:0 10px 30px rgba(0,0,0,.35);
  opacity:0; visibility:hidden; transition:opacity .18s ease, visibility .18s ease;
  z-index:5;
}

/* Reveal on hover/focus (mouse + keyboard) */
.timeline-item:hover .tl-more,
.timeline-item:focus-within .tl-more,
.timeline-item .tl-peek[aria-expanded="true"] + .tl-more {
  opacity:1; visibility:visible;
}

/* Mobile: stack cleanly */
@media (max-width:1024px){
  .tl-more { position:static; transform:none; margin-top:.35rem; opacity:1; visibility:visible; }
  .tl-peek { display:none; } /* auto-show details on tablet/mobile */
  .tl-title { max-width:100%; }
}
