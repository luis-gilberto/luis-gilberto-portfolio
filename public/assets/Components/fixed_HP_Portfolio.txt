<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luis Gilberto | Integrated Creative & Marketing Storytelling</title>
    <meta name="description" content="Big brands, bold campaigns, and a few mic-drop moments along the way. This isn't just a résumé—it's a journey built with context, creativity, and clarity.">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Big+Shoulders+Display:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --coral: #F96F6E;
            --dark: #0E0E0F;
            --text: rgba(245,245,245,0.94);
            --muted: rgba(245,245,245,0.82);
            --glass-bg: rgba(255,255,255,0.04);
            --glass-border: rgba(255,255,255,0.12);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'General Sans', sans-serif;
            background: var(--dark);
            color: var(--text);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* ========================================
           NAVIGATION STYLES
           ======================================== */
        .enhanced-nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(14, 14, 15, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }

        .nav-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .nav-logo img {
            height: 55px;
            width: auto;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link:hover {
            color: var(--coral);
        }

        .mobile-nav-button {
            display: none;
            flex-direction: column;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            gap: 0.25rem;
        }

        .mobile-nav-button span {
            width: 24px;
            height: 2px;
            background: var(--text);
            transition: all 0.3s ease;
        }

        /* ========================================
           HERO SECTION - ANIMATED LANDING INTEGRATION
           ======================================== */
        .hero-container {
            position: relative;
            height: 100vh;
            overflow: hidden;
        }

        /* Animated Tiles for Hero */
        .hero-tiles-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 10;
            transition: opacity 0.5s ease-out;
        }

        .hero-tiles-container.tiles-fadeout {
            opacity: 0;
        }

        /* Background */
        .hero-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 120%;
            height: 120%;
            background: url('assets/images/hp-new_1.png') center/cover;
            filter: blur(1px);
            opacity: 0.08;
            z-index: 1;
            animation: subtleMove 20s ease-in-out infinite;
        }

        @keyframes subtleMove {
            0%, 100% { transform: translate(0, 0) scale(1); }
            25% { transform: translate(-2%, -1%) scale(1.02); }
            50% { transform: translate(1%, 2%) scale(1.01); }
            75% { transform: translate(-1%, 1%) scale(1.03); }
        }

        /* Hero Tiles */
        .hero-tile {
            position: absolute;
            background-size: cover;
            background-position: center;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            backdrop-filter: blur(10px);
            transition: all 3s cubic-bezier(0.4, 0, 0.2, 1);
            opacity: 0;
            overflow: hidden;
        }

        .hero-tile::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.4);
            z-index: 1;
        }

        .hero-tile-content {
            position: relative;
            z-index: 2;
            padding: 24px;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            color: white;
        }

        .hero-tile-title {
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .hero-tile-subtitle {
            font-size: 0.9rem;
            opacity: 0.8;
            line-height: 1.4;
        }

        /* Tile Sizes */
        .hero-tile.position-1 { width: 450px; height: 300px; }
        .hero-tile.position-2 { width: 350px; height: 250px; }
        .hero-tile.position-3 { width: 280px; height: 200px; }
        .hero-tile.position-4 { width: 350px; height: 250px; }
        .hero-tile.position-5 { width: 450px; height: 300px; }

        /* Initial Spread Positions */
        .tile-1-initial { top: 5% !important; left: 5% !important; transform: rotate(-15deg) scale(0.7) !important; }
        .tile-2-initial { top: 5% !important; right: 5% !important; transform: rotate(12deg) scale(0.7) !important; }
        .tile-3-initial { bottom: 5% !important; left: 5% !important; transform: rotate(8deg) scale(0.7) !important; }
        .tile-4-initial { bottom: 5% !important; right: 5% !important; transform: rotate(-10deg) scale(0.7) !important; }
        .tile-5-initial { top: 50% !important; left: 50% !important; transform: translate(-50%, -50%) rotate(5deg) scale(0.6) !important; }

        /* Final Gathered Positions */
        .hero-tile.position-1 { top: 10%; left: 15%; transform: rotate(-2deg); }
        .hero-tile.position-2 { top: 20%; right: 10%; transform: rotate(1deg); }
        .hero-tile.position-3 { bottom: 25%; left: 20%; transform: rotate(-1deg); }
        .hero-tile.position-4 { bottom: 15%; right: 15%; transform: rotate(2deg); }
        .hero-tile.position-5 { top: 35%; left: 50%; transform: translateX(-50%) rotate(-0.5deg); }

        /* Gathering Animation */
        @keyframes tileGather {
            0% { opacity: 0; }
            20% { opacity: 0.6; }
            60% { opacity: 0.9; }
            100% { opacity: 0.85; }
        }

        .hero-tile.gathering {
            animation: tileGather 3s ease-out forwards;
        }

        /* Hero Content */
        .hero-section {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            z-index: 20;
            opacity: 0;
            transform: translateY(30px);
            transition: all 1s ease-out;
            background: linear-gradient(135deg, #0a0a0b 0%, #1a1a1c 50%, #0f0f10 100%);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
        }

        .hero-section.hero-visible {
            opacity: 1;
            transform: translateY(0);
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .hero-section h1 {
            font-family: Georgia, serif;
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 500;
            margin-bottom: 2rem;
            line-height: 1.2;
            color: #F2F2F2;
        }

        .hero-section h1 .breathing-text {
            color: #F2F2F2;
            font-weight: 500;
            font-style: italic;
            display: inline-block;
            animation: breathe 3s ease-in-out infinite;
        }

        @keyframes breathe {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.08); opacity: 0.85; }
        }

        .hero-section h1 .period {
            color: var(--coral);
        }

        .hero-portrait {
            width: 188px;
            height: 188px;
            border-radius: 50%;
            object-fit: cover;
            margin: 2rem 0;
            filter: grayscale(100%);
            transition: all 0.8s ease;
            cursor: pointer;
            box-shadow: 0 0 30px rgba(249, 111, 110, 0.2), 0 0 60px rgba(249, 111, 110, 0.1);
        }

        .hero-portrait:hover {
            filter: grayscale(0%);
            box-shadow: 0 0 80px rgba(249, 111, 110, 0.6), 
                       0 0 120px rgba(249, 111, 110, 0.4), 
                       0 0 160px rgba(249, 111, 110, 0.2);
            transform: scale(1.05);
        }

        .hero-subhead {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            justify-content: center;
            font-size: 1.25rem;
            font-weight: 400;
            margin-bottom: 3rem;
            opacity: 0.9;
            max-width: 600px;
        }

        .subhead-line {
            display: inline-block;
        }

        .coral-period {
            color: var(--coral);
        }

        /* Desktop: Add periods after first two sentences */
        @media (min-width: 769px) {
            .subhead-line:nth-child(1)::after { content: "."; }
            .subhead-line:nth-child(2)::after { content: "."; }
        }

        /* Mobile: Stack the subhead lines */
        @media (max-width: 768px) {
            .hero-subhead {
                flex-direction: column;
                gap: 0.25rem;
                text-align: center;
            }
            
            .subhead-line {
                display: block;
                line-height: 1.4;
            }
        }

        .hero-buttons {
            display: flex;
            gap: 2rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .hero-button {
            padding: 16px 32px;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .hero-button.primary {
            background: var(--coral);
            color: white;
        }

        .hero-button.primary:hover {
            background: #e55a59;
            transform: translateY(-2px);
        }

        .hero-button.secondary {
            background: transparent;
            color: var(--text);
            border: 2px solid rgba(255,255,255,0.2);
        }

        .hero-button.secondary:hover {
            border-color: var(--coral);
            color: var(--coral);
        }

        /* ========================================
           QUICK TOUR SECTION (SEPARATE FROM HERO)
           ======================================== */
        .quick-tour-section {
            padding: 80px 0;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            background-image: url('assets/images/Premium_black_leather_texture.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            width: 100%;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        .main-headline {
            font-size: 3.2rem;
            font-weight: 400;
            line-height: 1.2;
            color: #F96F6E;
            margin-bottom: 24px;
            font-family: 'Big Shoulders Display', sans-serif;
            letter-spacing: -0.01em;
        }

        .section-description {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #FFB3B3;
            opacity: 1;
            margin-bottom: 1rem;
            font-weight: 400;
            font-family: 'Inter', sans-serif;
        }

        .section-description.white-text {
            color: white;
        }

        .section-description.coral-text {
            color: #FF6B6B;
        }

        /* Cards Container */
        .cards-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
        }

        .card-stack {
            position: relative;
            width: 400px;
            height: 500px;
            margin-bottom: 40px;
            perspective: 1000px;
        }

        /* Tour Cards (Different from Hero Tiles) */
        .tour-card {
            position: absolute;
            width: 300px;
            height: 420px;
            transform-style: preserve-3d;
            transition: transform 0.8s ease-in-out;
            cursor: pointer;
            border-radius: 15px;
        }

        .tour-card.flipped {
            transform: rotateY(180deg) !important;
        }

        .card-face {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
            overflow: hidden;
        }

        .card-back {
            background-image: url('assets/images/card.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            transform: rotateY(0deg);
        }

        .card-front {
            background: rgba(0, 0, 0, 0.9);
            border: 2px solid rgba(0, 0, 0, 0.3);
            padding: 25px;
            color: white;
            backdrop-filter: blur(10px);
            transform: rotateY(180deg);
            display: flex;
            flex-direction: column;
            overflow-y: auto;
            border-radius: 15px;
        }

        .card-icon {
            font-size: 2rem;
            margin-bottom: 15px;
            color: #FF6B6B;
            text-align: center;
        }

        .card-content {
            flex: 1;
            margin-bottom: 20px;
        }

        .card-content p {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: #CCCCCC;
            font-size: 0.9rem;
        }

        .card-cta {
            margin-top: auto;
            display: inline-block;
            background: #F96F6E;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 25px;
            font-family: 'Inter', sans-serif;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            box-shadow: 0 4px 15px rgba(249, 111, 110, 0.3);
            width: 100%;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            text-decoration: none;
        }

        .card-cta:hover {
            background: #ff5252;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(249, 111, 110, 0.4);
        }

        /* Desktop Card Positioning */
        @media (min-width: 769px) {
            .tour-card.position-1 { top: 0; left: 50px; z-index: 5; transform: translateX(0) rotate(-2deg); }
            .tour-card.position-2 { top: 15px; left: 40px; z-index: 4; transform: translateX(0) rotate(1deg); }
            .tour-card.position-3 { top: 30px; left: 30px; z-index: 3; transform: translateX(0) rotate(-1deg); }
            .tour-card.position-4 { top: 45px; left: 20px; z-index: 2; transform: translateX(0) rotate(2deg); }
            .tour-card.position-5 { top: 60px; left: 10px; z-index: 1; transform: translateX(0) rotate(-1deg); }
        }

        /* Shuffle Button */
        .shuffle-button {
            background: rgba(255, 107, 107, 0.2);
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 107, 107, 0.4);
            color: white;
            padding: 15px 30px;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 40px;
        }

        .shuffle-button:hover {
            background: rgba(255, 107, 107, 0.3);
            border-color: rgba(255, 107, 107, 0.6);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(255, 107, 107, 0.4);
        }

        /* Shuffle Animations */
        .card-stack.shuffling .tour-card {
            transition: all 1.2s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        @media (min-width: 769px) {
            .shuffling .tour-card.position-1 { transform: translateX(0) rotate(-2deg) translateY(-30px) scale(0.9); }
            .shuffling .tour-card.position-2 { transform: translateX(0) rotate(1deg) translateY(-25px) scale(0.9); }
            .shuffling .tour-card.position-3 { transform: translateX(0) rotate(-1deg) translateY(-20px) scale(0.9); }
            .shuffling .tour-card.position-4 { transform: translateX(0) rotate(2deg) translateY(-15px) scale(0.9); }
            .shuffling .tour-card.position-5 { transform: translateX(0) rotate(-1deg) translateY(-10px) scale(0.9); }

            .shuffling .tour-card.position-1.flipped { transform: translateX(0) rotate(-2deg) translateY(-30px) scale(0.9) rotateY(180deg); }
            .shuffling .tour-card.position-2.flipped { transform: translateX(0) rotate(1deg) translateY(-25px) scale(0.9) rotateY(180deg); }
            .shuffling .tour-card.position-3.flipped { transform: translateX(0) rotate(-1deg) translateY(-20px) scale(0.9) rotateY(180deg); }
            .shuffling .tour-card.position-4.flipped { transform: translateX(0) rotate(2deg) translateY(-15px) scale(0.9) rotateY(180deg); }
            .shuffling .tour-card.position-5.flipped { transform: translateX(0) rotate(-1deg) translateY(-10px) scale(0.9) rotateY(180deg); }
        }

        /* ========================================
           WHAT'S NEXT SECTION
           ======================================== */
        .whats-next {
            background: #1a1a1a;
            padding: 120px 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        #constellation-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .whats-next .container {
            position: relative;
            z-index: 2;
        }

        .whats-next h2 {
            font-family: 'Big Shoulders Display', sans-serif;
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 2rem;
            color: white;
        }

        .next-description {
            font-size: 1.2rem;
            color: rgba(255,255,255,0.8);
            max-width: 600px;
            margin: 0 auto 3rem auto;
            line-height: 1.6;
        }

        .cta-button {
            display: inline-block;
            background: var(--coral);
            color: white;
            padding: 18px 36px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .cta-button:hover {
            background: #e55a59;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(249, 111, 110, 0.4);
        }

        /* ========================================
           FOOTER
           ======================================== */
        .footer {
            background: #0a0a0a;
            color: rgba(255,255,255,0.6);
            padding: 60px 0 40px 0;
            text-align: center;
        }

        .footer-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 2rem;
        }

        .footer .brand {
            font-family: 'Big Shoulders Display', sans-serif;
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
        }

        .footer .nav {
            display: flex;
            gap: 2rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .footer .nav a {
            color: rgba(255,255,255,0.6);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer .nav a:hover {
            color: var(--coral);
        }

        .footer .social {
            display: flex;
            gap: 1.5rem;
        }

        .footer .social a {
            color: rgba(255,255,255,0.6);
            transition: color 0.3s ease;
        }

        .footer .social a:hover {
            color: var(--coral);
        }

        .footer .legal {
            font-size: 0.9rem;
            color: rgba(255,255,255,0.4);
        }

        /* ========================================
           MOBILE RESPONSIVE
           ======================================== */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .mobile-nav-button {
                display: flex;
            }

            .nav-logo img {
                height: 40px;
            }

            .hero-tile.position-1,
            .hero-tile.position-5 {
                width: 300px;
                height: 200px;
            }

            .hero-tile.position-2,
            .hero-tile.position-4 {
                width: 250px;
                height: 180px;
            }

            .hero-tile.position-3 {
                width: 200px;
                height: 150px;
            }

            .hero-tile-content {
                padding: 16px;
            }

            .hero-tile-title {
                font-size: 1rem;
            }

            .hero-tile-subtitle {
                font-size: 0.8rem;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: center;
                gap: 1rem;
            }

            .hero-button {
                width: 200px;
            }

            .main-headline {
                font-size: 2.5rem;
            }

            .card-stack {
                width: 280px;
                height: 400px;
            }

            .tour-card {
                width: 250px;
                height: 350px;
            }

            .tour-card.position-1 { top: 0; left: 15px; transform: rotate(-1deg); }
            .tour-card.position-2 { top: 8px; left: 12px; transform: rotate(0.5deg); }
            .tour-card.position-3 { top: 16px; left: 9px; transform: rotate(-0.5deg); }
            .tour-card.position-4 { top: 24px; left: 6px; transform: rotate(1deg); }
            .tour-card.position-5 { top: 32px; left: 3px; transform: rotate(-0.5deg); }

            .whats-next h2 {
                font-size: 2.5rem;
            }

            .next-description {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="enhanced-nav">
        <div class="nav-content">
            <div class="nav-logo">
                <a href="index.html">
                    <img src="assets/Components/AUg_logo_White.png" alt="Luis Gilberto Logo">
                </a>
            </div>
            
            <div class="nav-links">
                <a href="index.html" class="nav-link active">Home</a>
                <a href="about.html" class="nav-link">About Me</a>
                <a href="cv.html" class="nav-link">CV</a>
                <a href="timeline.html" class="nav-link">Timeline</a>
                <a href="myexperience.html" class="nav-link">Experience</a>
                <a href="contact.html" class="nav-link">Contact</a>
            </div>
            
            <button class="mobile-nav-button">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>

    <!-- Hero Container with Animated Landing -->
    <div class="hero-container">
        <!-- Background -->
        <div class="hero-background"></div>

        <!-- Animated Tiles Container (Hero Animation) -->
        <div class="hero-tiles-container" id="heroTilesContainer">
            <!-- Hero Tile 1 -->
            <div class="hero-tile position-1" id="heroTile1" style="background-image: url('assets/images/familysafety.png')">
                <div class="hero-tile-content">
                    <div class="hero-tile-title">Family Safety</div>
                    <div class="hero-tile-subtitle">Protection and peace of mind</div>
                </div>
            </div>

            <!-- Hero Tile 2 -->
            <div class="hero-tile position-2" id="heroTile2" style="background-image: url('assets/images/Teams_screens.png')">
                <div class="hero-tile-content">
                    <div class="hero-tile-title">Teams Together</div>
                    <div class="hero-tile-subtitle">Collaboration and connection</div>
                </div>
            </div>

            <!-- Hero Tile 3 -->
            <div class="hero-tile position-3" id="heroTile3" style="background-image: url('assets/images/ffc1.jpg')">
                <div class="hero-tile-content">
                    <div class="hero-tile-title">Free to be Free</div>
                    <div class="hero-tile-subtitle">Creative expression campaign</div>
                </div>
            </div>

            <!-- Hero Tile 4 -->
            <div class="hero-tile position-4" id="heroTile4" style="background-image: url('assets/images/edgemobile.png')">
                <div class="hero-tile-content">
                    <div class="hero-tile-title">Edge Mobile</div>
                    <div class="hero-tile-subtitle">Mobile browsing innovation</div>
                </div>
            </div>

            <!-- Hero Tile 5 -->
            <div class="hero-tile position-5" id="heroTile5" style="background-image: url('assets/FSA_Content-Filters.png')">
                <div class="hero-tile-content">
                    <div class="hero-tile-title">Content Filters</div>
                    <div class="hero-tile-subtitle">Safe digital experiences</div>
                </div>
            </div>
        </div>

        <!-- Hero Section (Revealed After Animation) -->
        <div class="hero-section" id="heroSection">
            <h1>I make tech<br>feel <em><span class="breathing-text">human</span></em><span class="period">.</span></h1>
            <img src="assets/images/hp-new_1.png" alt="Luis Gilberto" class="hero-portrait">
            <div class="hero-subhead">
                <span class="subhead-line">Clarity first</span>
                <span class="subhead-line">Beautiful execution</span>
                <span class="subhead-line">Systems that scale<span class="coral-period">.</span></span>
            </div>
            <div class="hero-buttons">
                <a href="myexperience.html#featured-work-anchor" class="hero-button primary">Explore my work</a>
                <a href="timeline.html" class="hero-button secondary">My Journey</a>
            </div>
        </div>
    </div>

    <!-- Quick Tour Section -->
    <section id="quick-tour" class="quick-tour-section">
        <div class="container">
            <div class="section-header">
                <h2 class="main-headline">
                    Got a challenge?<br>
                    Let's tackle it
                </h2>
                <p class="section-description white-text">
                    The hard stuff is where I thrive. If something's in your way, we'll figure it out together.
                </p>
            </div>
            
            <div class="cards-container">
                <div class="card-stack" id="cardStack">
                    <!-- Card 1: Launch Challenge -->
                    <div class="tour-card position-1" data-card="1">
                        <div class="card-face card-back"></div>
                        <div class="card-face card-front">
                            <div class="card-icon">🚀</div>
                            <div class="card-content">
                                <p>73% of launches miss their targets. If yours gets buried in the noise, I craft systems that cut through and convert.</p>
                            </div>
                            <a href="myexperience.html#stories" class="card-cta">Explore Launch Work</a>
                        </div>
                    </div>

                    <!-- Card 2: Brand Challenge -->
                    <div class="tour-card position-2" data-card="2">
                        <div class="card-face card-back"></div>
                        <div class="card-face card-front">
                            <div class="card-icon">🧠</div>
                            <div class="card-content">
                                <p>89% of brands fail to stand out. If yours blends into the background, I reposition it to be unmistakable.</p>
                            </div>
                            <a href="case-study-free-to-be-free.html" class="card-cta">Explore Brand Work</a>
                        </div>
                    </div>

                    <!-- Card 3: Campaign Challenge -->
                    <div class="tour-card position-3" data-card="3">
                        <div class="card-face card-back"></div>
                        <div class="card-face card-front">
                            <div class="card-icon">📈</div>
                            <div class="card-content">
                                <p>68% of campaigns don't convert. If yours gets likes but not leads, I build campaigns that drive results.</p>
                            </div>
                            <a href="myexperience.html#edge-campaigns" class="card-cta">View Performance Results</a>
                        </div>
                    </div>

                    <!-- Card 4: Messaging Challenge -->
                    <div class="tour-card position-4" data-card="4">
                        <div class="card-face card-back"></div>
                        <div class="card-face card-front">
                            <div class="card-icon">🧠</div>
                            <div class="card-content">
                                <p>95% of messages are forgotten. If yours fades fast, I create stories that connect and stay.</p>
                            </div>
                            <a href="case-study-fsa.html" class="card-cta">Experience Storytelling</a>
                        </div>
                    </div>

                    <!-- Card 5: Consistency Challenge -->
                    <div class="tour-card position-5" data-card="5">
                        <div class="card-face card-back"></div>
                        <div class="card-face card-front">
                            <div class="card-icon">🧭</div>
                            <div class="card-content">
                                <p>Inconsistent messaging costs brands 23%. If your voice feels scattered, I unify it across every touchpoint.</p>
                            </div>
                            <a href="case-study-teams-final-dm.html" class="card-cta">See Teams Launch System</a>
                        </div>
                    </div>
                </div>
                
                <p class="section-description coral-text" style="text-align: center; margin: 2rem 0 1.5rem 0; background: rgba(0, 0, 0, 0.3); padding: 1rem 2rem; border-radius: 8px; backdrop-filter: blur(2px);">
                    Tap on the cards and see how I've done it before.
                </p>
                
                <div class="action-buttons">
                    <button class="shuffle-button" id="shuffleBtn">Shuffle cards</button>
                </div>
            </div>
        </div>
    </section>

    <!-- What's Next Section -->
    <section id="whats-next-section" class="whats-next">
        <canvas id="constellation-canvas"></canvas>
        <div class="container">
            <h2>Let's make ideas matter.</h2>
            <p class="next-description">
                I partner with bold brands and brilliant people ready to break through the noise. Together, we'll translate your business goals into unforgettable creative...and measurable growth.
            </p>
            <a href="contact.html" class="cta-button">Let's talk about what's next.</a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-inner">
            <div class="brand">Luis Gilberto</div>

            <nav class="nav" aria-label="Footer">
                <a href="portfolio/">Portfolio</a>
                <a href="about-integrated-marketing-expert/">About</a>
                <a href="insights/">Insights</a>
                <a href="my-experience/">Experience</a>
                <a href="contact.html">Contact</a>
            </nav>

            <div class="social">
                <a href="https://luisgilberto00.link" aria-label="Gravatar">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"/>
                    </svg>
                </a>
                <a href="https://www.linkedin.com/in/luisgilberto00" aria-label="LinkedIn">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                    </svg>
                </a>
                <a href="mailto:me@luis-gilberto.com" aria-label="Email">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
                    </svg>
                </a>
            </div>

            <div class="legal">&copy; <span id="yr"></span> Luis Gilberto · Built with intention.</div>
        </div>
    </footer>

    <script>
        // Set current year
        document.getElementById('yr').textContent = new Date().getFullYear();

        // ========================================
        // HERO ANIMATION (FROM ANIMATED LANDING)
        // ========================================
        document.addEventListener('DOMContentLoaded', function() {
            const heroTiles = document.querySelectorAll('.hero-tile');
            const heroTilesContainer = document.getElementById('heroTilesContainer');
            const heroSection = document.getElementById('heroSection');

            // Phase 1: Add spread classes (0-0.1s)
            setTimeout(() => {
                heroTiles.forEach((tile, index) => {
                    tile.classList.add('tile-initial');
                    tile.classList.add(`tile-${index + 1}-initial`);
                    tile.style.opacity = '0.6';
                });
            }, 100);

            // Phase 2: Remove spread, let tiles gather (0.1-3s)
            setTimeout(() => {
                heroTiles.forEach((tile, index) => {
                    tile.classList.remove('tile-initial');
                    tile.classList.remove(`tile-${index + 1}-initial`);
                    tile.classList.add('gathering');
                });
            }, 3000);

            // Phase 3: Fade out tiles (4-4.5s)
            setTimeout(() => {
                heroTilesContainer.classList.add('tiles-fadeout');
            }, 4000);

            // Phase 4: Reveal hero (4.5s+)
            setTimeout(() => {
                heroSection.classList.add('hero-visible');
            }, 4500);
        });

        // ========================================
        // CARD GAME (FOR QUICK TOUR SECTION)
        // ========================================
        class CardGame {
            constructor() {
                this.cards = [];
                this.cardStack = null;
                this.shuffleButton = null;
                this.lastShuffleOrder = null;
                this.isShuffling = false;
                this.init();
            }

            init() {
                // Wait for DOM and hero animation to complete
                setTimeout(() => this.setup(), 5000);
            }

            setup() {
                this.cardStack = document.querySelector('.card-stack');
                this.cards = Array.from(document.querySelectorAll('.tour-card'));
                this.shuffleButton = document.getElementById('shuffleBtn');

                if (!this.cardStack || this.cards.length === 0) {
                    console.error('Card game elements not found');
                    return;
                }

                this.setupCards();
                this.setupShuffleButton();
                this.setupEntranceAnimation();
            }

            setupCards() {
                this.cards.forEach((card, index) => {
                    card.style.cursor = 'pointer';
                    card.style.pointerEvents = 'auto';
                    
                    const flipHandler = (e) => this.flipCard(e, card, index);
                    card.addEventListener('click', flipHandler);
                    card.addEventListener('touchstart', flipHandler, { passive: false });
                    
                    const cardFaces = card.querySelectorAll('.card-face');
                    cardFaces.forEach(face => {
                        face.style.pointerEvents = 'auto';
                        face.style.cursor = 'pointer';
                    });
                });
            }

            flipCard(e, card, index) {
                if (e.target.tagName === 'A' || e.target.closest('a') || e.target.classList.contains('card-cta')) {
                    return;
                }
                
                e.preventDefault();
                e.stopPropagation();
                
                if (this.isShuffling) return;
                
                const isFlipped = card.classList.contains('flipped');
                
                if (isFlipped) {
                    card.classList.remove('flipped');
                } else {
                    card.classList.add('flipped');
                }
            }

            setupShuffleButton() {
                if (!this.shuffleButton) return;
                this.shuffleButton.addEventListener('click', () => this.shuffleCards());
            }

            shuffleCards() {
                if (this.isShuffling || this.cards.length === 0) return;
                
                this.isShuffling = true;
                this.cardStack.classList.add('shuffling');
                
                this.cards.forEach(card => {
                    card.classList.remove('flipped');
                });
                
                const positions = ['position-1', 'position-2', 'position-3', 'position-4', 'position-5'];
                let newOrder;
                let attempts = 0;
                
                do {
                    newOrder = this.fisherYatesShuffle([...positions]);
                    attempts++;
                } while (
                    this.lastShuffleOrder && 
                    JSON.stringify(newOrder) === JSON.stringify(this.lastShuffleOrder) && 
                    attempts < 50
                );
                
                this.lastShuffleOrder = [...newOrder];
                
                this.cards.forEach((card, index) => {
                    card.classList.remove('position-1', 'position-2', 'position-3', 'position-4', 'position-5');
                    card.classList.add(newOrder[index]);
                });
                
                setTimeout(() => {
                    this.cardStack.classList.remove('shuffling');
                    this.isShuffling = false;
                }, 1200);
            }

            fisherYatesShuffle(array) {
                for (let i = array.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [array[i], array[j]] = [array[j], array[i]];
                }
                return array;
            }

            setupEntranceAnimation() {
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            this.cards.forEach((card, index) => {
                                setTimeout(() => {
                                    card.style.opacity = '1';
                                    card.classList.add('animate-in');
                                }, index * 200);
                            });
                            observer.unobserve(entry.target);
                        }
                    });
                }, { threshold: 0.3 });

                if (this.cardStack) {
                    observer.observe(this.cardStack);
                }
            }
        }

        // Initialize card game after hero animation
        const cardGame = new CardGame();

        // ========================================
        // CONSTELLATION CANVAS (WHAT'S NEXT SECTION)
        // ========================================
        function initConstellation() {
            const canvas = document.getElementById('constellation-canvas');
            if (!canvas) return;
            
            const ctx = canvas.getContext('2d');
            const section = document.getElementById('whats-next-section');
            
            let nodes = [];
            let mouse = { x: 0, y: 0 };
            let isMobile = window.innerWidth <= 768;
            
            const nodeConfig = {
                count: isMobile ? 30 : 50,
                maxSpeed: 0.3,
                connectionDistance: isMobile ? 80 : 120,
                activationRadius: isMobile ? 60 : 100,
                colors: {
                    bg: '#1a1a1a',
                    node: '#FF6B6B',
                    nodeHighlight: '#FF5959',
                    connection: '#FF6B6B',
                    connectionHighlight: '#FF5959'
                }
            };
            
            function resizeCanvas() {
                const rect = section.getBoundingClientRect();
                canvas.width = rect.width;
                canvas.height = rect.height;
            }
            
            section.addEventListener('mousemove', function(e) {
                const rect = canvas.getBoundingClientRect();
                mouse.x = e.clientX - rect.left;
                mouse.y = e.clientY - rect.top;
            });
            
            class Node {
                constructor() {
                    this.x = Math.random() * canvas.width;
                    this.y = Math.random() * canvas.height;
                    this.vx = (Math.random() - 0.5) * nodeConfig.maxSpeed;
                    this.vy = (Math.random() - 0.5) * nodeConfig.maxSpeed;
                    this.radius = Math.random() * 2 + 1;
                    this.highlighted = false;
                    this.isSpecial = Math.random() < 0.1;
                }
                
                update() {
                    this.x += this.vx;
                    this.y += this.vy;
                    
                    if (this.x <= 0 || this.x >= canvas.width) this.vx *= -1;
                    if (this.y <= 0 || this.y >= canvas.height) this.vy *= -1;
                    
                    this.x = Math.max(0, Math.min(canvas.width, this.x));
                    this.y = Math.max(0, Math.min(canvas.height, this.y));
                    
                    const distance = Math.sqrt(
                        Math.pow(this.x - mouse.x, 2) + Math.pow(this.y - mouse.y, 2)
                    );
                    this.highlighted = distance < nodeConfig.activationRadius;
                }
            }
            
            function createNodes() {
                nodes = [];
                for (let i = 0; i < nodeConfig.count; i++) {
                    nodes.push(new Node());
                }
            }
            
            function updateNodes() {
                nodes.forEach(node => node.update());
            }
            
            function drawConnections() {
                for (let i = 0; i < nodes.length; i++) {
                    for (let j = i + 1; j < nodes.length; j++) {
                        const distance = Math.sqrt(
                            Math.pow(nodes[i].x - nodes[j].x, 2) + 
                            Math.pow(nodes[i].y - nodes[j].y, 2)
                        );
                        
                        if (distance < nodeConfig.connectionDistance) {
                            const opacity = 1 - (distance / nodeConfig.connectionDistance);
                            const isHighlighted = nodes[i].highlighted || nodes[j].highlighted;
                            const isSpecialConnection = nodes[i].isSpecial || nodes[j].isSpecial;
                            
                            ctx.strokeStyle = isHighlighted || isSpecialConnection ? 
                                `rgba(255, 89, 89, ${opacity * 0.8})` : 
                                `rgba(255, 107, 107, ${opacity * 0.3})`;
                            ctx.lineWidth = isHighlighted || isSpecialConnection ? 1.5 : 0.8;
                            
                            ctx.beginPath();
                            ctx.moveTo(nodes[i].x, nodes[i].y);
                            ctx.lineTo(nodes[j].x, nodes[j].y);
                            ctx.stroke();
                        }
                    }
                }
            }
            
            function drawNodes() {
                nodes.forEach(node => {
                    const glowSize = node.highlighted || node.isSpecial ? 8 : 4;
                    const alpha = node.highlighted || node.isSpecial ? 0.8 : 0.4;
                    
                    ctx.shadowColor = node.highlighted || node.isSpecial ? 
                        nodeConfig.colors.nodeHighlight : nodeConfig.colors.node;
                    ctx.shadowBlur = glowSize;
                    
                    ctx.fillStyle = node.highlighted || node.isSpecial ? 
                        `rgba(255, 89, 89, ${alpha})` : 
                        `rgba(255, 107, 107, ${alpha})`;
                    
                    ctx.beginPath();
                    ctx.arc(node.x, node.y, node.radius, 0, Math.PI * 2);
                    ctx.fill();
                    
                    ctx.shadowBlur = 0;
                });
            }
            
            function animateRandomNodes() {
                if (isMobile) {
                    nodes.forEach(node => node.highlighted = false);
                    
                    const numToHighlight = Math.floor(Math.random() * 5) + 2;
                    for (let i = 0; i < numToHighlight; i++) {
                        const randomNode = nodes[Math.floor(Math.random() * nodes.length)];
                        randomNode.highlighted = true;
                        
                        setTimeout(() => {
                            randomNode.highlighted = false;
                        }, 800 + Math.random() * 500);
                    }
                }
            }
            
            if (isMobile) {
                setInterval(animateRandomNodes, 1200);
            }
            
            function animate() {
                ctx.fillStyle = nodeConfig.colors.bg;
                ctx.fillRect(0, 0, canvas.width, canvas.height);
                
                updateNodes();
                drawConnections();
                drawNodes();
                
                requestAnimationFrame(animate);
            }
            
            resizeCanvas();
            createNodes();
            animate();
            
            window.addEventListener('resize', function() {
                isMobile = window.innerWidth <= 768;
                resizeCanvas();
                createNodes();
            });
        }

        // Initialize constellation when page loads
        document.addEventListener('DOMContentLoaded', initConstellation);
    </script>
</body>
</html>