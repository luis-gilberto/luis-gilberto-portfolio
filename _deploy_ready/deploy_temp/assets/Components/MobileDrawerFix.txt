Task: Fix mobile drawer styling on brand-guidelines-updated.html to match the correct design specifications.

MOBILE DRAWER CORRECTIONS NEEDED:

1. HEADER BRANDING (Top of Drawer):

WRONG (Current):
- "LUIS GILBERTO" / "Marketing & Creative"
- All caps, corporate tone

CORRECT (Target):
```html
<div class="mobile-drawer-header">
    <div class="mobile-drawer-brand">
        <span class="mobile-drawer-name">Luis Gilberto</span>
        <span class="mobile-drawer-role">Creative Strategy</span>
    </div>
</div>
```

CSS:
```css
.mobile-drawer-header {
    padding: 2rem 0 2rem 1rem;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    margin-bottom: 2rem;
}

.mobile-drawer-brand {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.mobile-drawer-name {
    font-family: var(--font-narrative);  /* Playfair Display */
    font-size: 1.5rem;
    font-weight: 600;
    color: #ffffff;
    letter-spacing: -0.02em;
}

.mobile-drawer-role {
    font-family: var(--font-system);  /* Inter */
    font-size: 0.8125rem;
    font-weight: 400;
    color: rgba(255,255,255,0.5);
    letter-spacing: 0.02em;
}
```

2. SECTION LABELS:
```css
.mobile-nav-label {
    font-family: var(--font-system);
    font-size: 0.6875rem;  /* Very small */
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    margin-bottom: 1.25rem;
    padding-left: 0.25rem;
}
```

3. GLOBAL NAVIGATION LINKS (Large Editorial Style):
```css
.mobile-nav-link-primary {
    font-family: var(--font-narrative);  /* Playfair Display */
    font-size: 1.75rem;  /* LARGE - This is key! */
    font-weight: 500;
    line-height: 1.3;
    color: rgba(255,255,255,0.85);
    padding: 0.5rem 0.25rem;
    transition: all 0.2s ease;
    text-decoration: none;
    letter-spacing: -0.01em;
    position: relative;
}

.mobile-nav-link-primary:hover {
    color: #ffffff;
    transform: translateX(4px);
}

/* Active state for Portfolio */
.mobile-nav-link-primary.active {
    color: var(--coral);
    font-weight: 600;
}

.mobile-nav-link-primary.active::before {
    content: '|';
    position: absolute;
    left: -1rem;
    color: var(--coral);
    font-weight: 600;
}
```

4. PORTFOLIO NAVIGATION LINKS (Smaller System Font):
```css
.mobile-nav-link-secondary {
    font-family: var(--font-system);  /* Inter */
    font-size: 1rem;  /* Smaller than global */
    font-weight: 400;
    line-height: 1.5;
    color: rgba(255,255,255,0.65);
    padding: 0.625rem 0.25rem;
    transition: all 0.2s ease;
    text-decoration: none;
    position: relative;
}

.mobile-nav-link-secondary:hover {
    color: rgba(255,255,255,0.9);
    transform: translateX(4px);
}

/* Active state for Brand */
.mobile-nav-link-secondary.active {
    color: var(--coral);
    font-weight: 600;
}

.mobile-nav-link-secondary.active::before {
    content: '|';
    position: absolute;
    left: -1rem;
    color: var(--coral);
    font-weight: 600;
}
```

5. HTML STRUCTURE FOR MENU:
```html
<div class="mobile-menu-overlay" id="mobileOverlay">
    <div class="mobile-drawer">
        <button class="mobile-menu-close" id="mobileMenuClose">
            ✕
        </button>
        
        <div class="mobile-menu-content">
            <div class="mobile-drawer-header">
                <div class="mobile-drawer-brand">
                    <span class="mobile-drawer-name">Luis Gilberto</span>
                    <span class="mobile-drawer-role">Creative Strategy</span>
                </div>
            </div>

            <!-- GLOBAL SECTION -->
            <div class="mobile-nav-section">
                <div class="mobile-nav-label">GLOBAL</div>
                <a href="/index.html" class="mobile-nav-link-primary active">Portfolio</a>
                <a href="/insights/" class="mobile-nav-link-primary">Insights</a>
                <a href="/TheHub/" class="mobile-nav-link-primary">The Hub</a>
                <a href="https://portal.luis-gilberto.com" class="mobile-nav-link-primary">The Portal</a>
            </div>

            <!-- PORTFOLIO SECTION -->
            <div class="mobile-nav-section">
                <div class="mobile-nav-label">PORTFOLIO</div>
                <a href="/index.html" class="mobile-nav-link-secondary">Home</a>
                <a href="/timeline.html" class="mobile-nav-link-secondary">Timeline</a>
                <a href="/myexperience.html" class="mobile-nav-link-secondary">Experience</a>
                <a href="/about.html" class="mobile-nav-link-secondary">About</a>
                <a href="/brand-guidelines-updated.html" class="mobile-nav-link-secondary active">Brand</a>
            </div>
        </div>
    </div>
</div>
```

6. CLOSE BUTTON STYLING:
```css
.mobile-menu-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 44px;
    height: 44px;
    border: none;
    border-radius: 8px;
    background: transparent;
    color: #ffffff;
    font-size: 32px;
    line-height: 1;
    cursor: pointer;
    transition: background 0.2s ease;
    z-index: 10;
}

.mobile-menu-close:hover {
    background: rgba(255,255,255,0.1);
}
```

7. SPACING STRUCTURE:
```css
.mobile-nav-section {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin-bottom: 2.5rem;
}

.mobile-nav-section:last-child {
    margin-bottom: 1rem;
}
```

8. ACTIVE STATE RULES:

- In GLOBAL section: "Portfolio" should have active class (coral color + "|" prefix)
- In PORTFOLIO section: "Brand" should have active class (coral color + "|" prefix)
- Active links use font-weight: 600
- Active links have "|" character positioned before text using ::before pseudo-element

KEY DIFFERENCES TO IMPLEMENT:
✅ Header uses "Luis Gilberto" + "Creative Strategy" (not all caps)
✅ Global links are LARGE (1.75rem) using Playfair Display
✅ Portfolio links are smaller (1rem) using Inter
✅ Both "Portfolio" and "Brand" are highlighted in coral
✅ Active items have "|" prefix character
✅ More vertical spacing and breathing room
✅ Editorial typography tone vs. corporate tone
✅ Close button is simple X without heavy styling

Result: Mobile drawer will match the right-side reference design with proper typography hierarchy, active states, and editorial styling.
