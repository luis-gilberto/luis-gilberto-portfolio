I want to refine the unified nav on the LIVE Proof of Life article by making it a clean hamburger menu on mobile.

Use the same HTML file as before, the one that contains:
    <title>Proof of Life - Luis Gilberto</title>
and the inline <style> block plus:
    <header class="lg-site-header" ...>

You are allowed to edit exactly ONE file:
- That Proof of Life HTML file.

Guardrails:
- Before changing anything, create a new backup of this file in the same folder with "_backup_navmobile" added before the extension.
- Do NOT change any other files.
- Do NOT remove or reorder any existing hero, article content, images, or footer CTA.
- Do NOT change the theme toggle script or any other scripts except for the small nav shadow script we added earlier.
- In CSS, you may ONLY append new rules at the end of the existing <style> block.

Follow these steps exactly.

------------------------------------------------
1) Create a new backup of the file
------------------------------------------------

- If the current file is for example "index.html", create:
  - index_backup_navmobile.html

Confirm the original filename and the backup filename after you create it.

------------------------------------------------
2) Update the nav markup to support hamburger
------------------------------------------------

2a) Add an id to the nav

- In the header markup, find this block (or the close equivalent):

    <nav class="lg-main-nav" aria-label="Main navigation">

- Replace just that opening tag with this:

    <nav class="lg-main-nav" id="lgMainNav" aria-label="Main navigation">

- Leave all of the <a> links inside it exactly as they are.

2b) Enhance the toggle button

- In the same header, find this button block:

    <button class="lg-nav-toggle" aria-label="Toggle navigation">
      <span class="lg-nav-toggle-bar"></span>
      <span class="lg-nav-toggle-bar"></span>
    </button>

- Replace that entire button block with this version:

    <button
      class="lg-nav-toggle"
      aria-label="Toggle navigation"
      aria-expanded="false"
      aria-controls="lgMainNav"
    >
      <span class="lg-nav-toggle-bar"></span>
      <span class="lg-nav-toggle-bar"></span>
    </button>

------------------------------------------------
3) Append mobile hamburger CSS at the end of <style>
------------------------------------------------

- In the <head>, locate the large inline <style> block that defines :root, body, .hero, .article-body, and the lg-site-header rules we added earlier.
- Scroll to the VERY END of that <style> block and APPEND this CSS, without deleting or changing anything above it:

    /* Mobile hamburger layout for unified nav - Proof of Life */
    @media (max-width: 768px) {
      .lg-site-header {
        position: sticky;
        top: 0;
        z-index: 110;
      }

      .lg-site-header-inner {
        max-width: 100%;
        padding: 0 1.25rem;
        height: 56px;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
      }

      .lg-logo-text {
        font-size: 16px;
      }

      .lg-main-nav {
        position: absolute;
        left: 0;
        right: 0;
        top: 56px;
        padding: 0.75rem 1.25rem 1rem;
        background: var(--black);
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        display: none;
        flex-direction: column;
        gap: 0.5rem;
      }

      .lg-site-header.is-open .lg-main-nav {
        display: flex;
      }

      .lg-nav-link {
        font-size: 13px;
        letter-spacing: 0.08em;
      }

      .lg-nav-toggle {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 40px;
        height: 40px;
        border-radius: 8px;
        border: 1px solid rgba(255, 255, 255, 0.18);
        background: transparent;
      }

      .lg-nav-toggle-bar {
        width: 18px;
        height: 2px;
        margin: 4px auto;
        background: var(--white);
      }
    }

Notes:
- This media query is meant to override the earlier mobile rules where .lg-main-nav might be hidden with display: none.
- We are not deleting the old rule; this later block should take precedence.

------------------------------------------------
4) Replace the existing nav shadow script with a combined script
------------------------------------------------

- Scroll to the bottom of the HTML file and locate the small script we previously added for the header shadow. It should look like this (or very close):

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const header = document.querySelector(".lg-site-header");
        if (!header) return;

        function updateHeaderShadow() {
          if (window.scrollY > 4) {
            header.classList.add("is-scrolled");
          } else {
            header.classList.remove("is-scrolled");
          }
        }

        updateHeaderShadow();
        window.addEventListener("scroll", updateHeaderShadow);
      });
    </script>

- Replace that entire <script> block with this new combined script:

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const header = document.querySelector(".lg-site-header");
        const toggle = document.querySelector(".lg-nav-toggle");
        const nav = document.querySelector(".lg-main-nav");

        if (!header) return;

        function updateHeaderShadow() {
          if (window.scrollY > 4) {
            header.classList.add("is-scrolled");
          } else {
            header.classList.remove("is-scrolled");
          }
        }

        updateHeaderShadow();
        window.addEventListener("scroll", updateHeaderShadow);

        if (toggle && nav) {
          toggle.addEventListener("click", function () {
            const isOpen = header.classList.toggle("is-open");
            toggle.setAttribute("aria-expanded", isOpen ? "true" : "false");
          });
        }
      });
    </script>

- Do NOT modify the line that loads the theme toggle script:
    <script src="/assets/js/theme-toggle.js" defer></script>
  or any other external scripts. Leave them exactly as they are.

------------------------------------------------
5) Final report
------------------------------------------------

When you are done, tell me:
- The exact path and name of the original Proof of Life file and its new backup file.
- Confirm that only this one HTML file was modified.
- Paste the final <header class="lg-site-header"> block as it now appears.
- Paste the final @media (max-width: 768px) CSS block you appended.
- Confirm that:
  - Desktop still shows the full horizontal nav.
  - On mobile, the nav links are hidden until the hamburger is tapped.
  - Tapping the hamburger toggles the menu open and closed.
  - Hero, article content, and footer CTA are visually unchanged.
  - Theme toggle and dark mode behavior still work.
