1) Fonts (brand-aligned, minimal)

Find (in <head>): your current Google Fonts links.
Keep Inter if you need it elsewhere, but for the hero we’ll use Recoleta (headline) + General Sans (body/CTA).

Replace those links with:

<!-- Brand Fonts (lean) -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=General+Sans:wght@400;600&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Recoleta:wght@600;700&display=swap" rel="stylesheet">

2) HTML (hero content only)

Find this block:
<section class="hero">
    <div class="eyebrow">Strategic Marketing Leadership</div>
    <h1 class="hero-title">Where Strategy Meets<br>Creative Excellence</h1>
    <p class="hero-subtitle">
        Bridging the strategy-creative divide with proven Microsoft-level expertise 
        and access to world-class creative partnerships. When strategic leadership 
        meets creative excellence, brands don't just grow—they transform.
    </p>
    <div class="cta-row">
        <a href="/TheHub/contact.html" class="btn btn-primary">Start the Conversation</a>
        <a href="#capabilities" class="btn btn-ghost">Explore Capabilities</a>
    </div>
    <p class="hero-note">
        <span class="bridge-highlight">Creative bench strength available</span> for full-spectrum campaign execution
    </p>
</section>

Replace with (one colored word + no em-dash):

<section class="hero">
  <div class="eyebrow">Strategic Marketing Leadership</div>

  <h1 class="hero-title">
    Where Strategy Meets<br><span class="hl-one">Creative</span> Excellence
  </h1>

  <p class="hero-subtitle">
    I lead integrated marketing that connects the plan to the pixels, so brands move faster, resonate deeper, and perform smarter.
  </p>

  <div class="cta-row">
    <a href="/TheHub/contact.html" class="btn btn-primary" aria-label="Start the conversation">Start the conversation</a>
    <a href="#capabilities" class="btn btn-ghost" aria-label="Explore capabilities">Explore capabilities</a>
  </div>

  <p class="hero-note">
    <a href="#bench" class="bench-link">Creative bench strength available</a> <span class="muted">for full-spectrum campaign execution</span>
  </p>
</section>

3) CSS (tighten performance + typography + one-word coral)
3a) Replace hero type + layout

Find (CSS):
.hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 2rem;
    position: relative;
    background: radial-gradient(circle at 30% 20%, rgba(249, 111, 110, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 70% 80%, rgba(46, 211, 198, 0.06) 0%, transparent 50%);
}

Replace with:

.hero{
  /* mobile-first height avoids address-bar jump */
  min-height: 88svh;
  display:flex; flex-direction:column; justify-content:center; align-items:center;
  text-align:center; padding: clamp(20px, 4vw, 32px);
  position:relative; overflow:hidden;
  background: #0E0F10; /* clean dark base; gradients removed to help LCP */
}

3b) Replace hero title font + remove gradient clip

Find (CSS block starting with .hero-title {):
.hero-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    background: linear-gradient(180deg, var(--ink) 0%, var(--ink-dim) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

Replace with (Recoleta; no background-clip):
.hero-title{
  font-family: "Recoleta", ui-serif, Georgia, "Times New Roman", serif;
  font-weight:700;
  line-height:1.05;
  font-size: clamp(2.2rem, 7vw, 3.4rem);
  margin: 0 0 .8rem 0;
  color: var(--ink);
  letter-spacing: 0;
}

3c) One-word coral rule

Add right after the .hero-title block:
.hl-one{ color: var(--coral); } /* one colored word rule */

3d) Subtitle font + spacing (General Sans)

Find .hero-subtitle { ... } and replace with:

.hero-subtitle{
  font-family: "General Sans", ui-sans-serif, Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, sans-serif;
  font-size: clamp(1rem, 2.6vw, 1.15rem);
  line-height: 1.6;
  color: var(--ink-dim);
  max-width: 48ch;
  margin: 0 auto clamp(18px, 3.2vh, 26px);
}

3e) Bench note polish (optional, tiny)
Add or update:
.bench-link{ color: var(--ink); text-decoration: underline; text-decoration-color: rgba(249,111,110,.6); text-underline-offset: 3px; }
.bench-link:hover{ color: var(--coral); }
.muted{ color: var(--ink-muted); }

4) Simplified orbital watermark (lightweight, brand-true)

Replace your current .hero::before { … } orbital block with this single SVG data-URI (≈1KB). It uses your coral and sits subtly behind content.

Find (starts with /* ===== ATOMIC ORBITAL BACKGROUND WATERMARKS ===== */ and the .hero::before block) and replace the entire .hero::before{...} with:


.hero::before{
  content:"";
  position:absolute; inset:auto; right:-12vw; top:8vh;
  width: min(64vw, 520px); aspect-ratio:1/1;
  opacity:.09; filter: blur(.3px);
  background-repeat:no-repeat; background-size:contain; background-position:center;
  /* Simple orbital built as inline SVG for speed */
  background-image: url("data:image/svg+xml;utf8,\
  <svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 220 220' fill='none'>\
    <g stroke='%23F96F6E' stroke-width='16' stroke-linecap='round' stroke-linejoin='round'>\
      <circle cx='170' cy='60' r='36'/><circle cx='54' cy='90' r='28'/>\
      <circle cx='110' cy='184' r='28'/>\
      <path d='M83 103 L136 82 M69 118 L96 170 M146 98 L124 160'/>\
    </g>\
  </svg>");
  pointer-events:none;
}


5) Micro-perf tweaks (no layout break)

Change min-height: 100vh → 88svh (already in step 3a) to reduce mobile viewport jumps.

Remove heavy layered gradients in the hero background (done in step 3a) to help LCP.

Keep your CTAs as-is; they’re already lean.

Final copy (as requested, no em-dash)

Headline: Where Strategy Meets <span class="hl-one">Creative</span> Excellence
Subcopy: I lead integrated marketing that connects the plan to the pixels, so brands move faster, resonate deeper, and perform smarter.

That preserves my tone, implements the one colored word rule, aligns fonts with my Hub brand, and tightens loading on mobile.

For the orbital to very slowly rotate (still cheap), add:

@keyframes slowspin{ from{ transform: rotate(0deg);} to{ transform: rotate(360deg);} }
.hero::before{ animation: slowspin 46s linear infinite; }
@media (prefers-reduced-motion:reduce){ .hero::before{ animation:none; } }























