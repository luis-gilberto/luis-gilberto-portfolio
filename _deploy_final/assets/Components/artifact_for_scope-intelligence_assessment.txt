<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Strategic Assessment - Luis Gilberto</title>
    <meta name="description" content="Complete our strategic marketing assessment to receive your personalized engagement recommendation.">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Big+Shoulders+Display:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --coral: #FF6B5A;
            --coral-light: #FF8B7A;
            --teal: #4ECDC4;
            --teal-light: #6EDDD6;
            --dark: #0A0A0B;
            --gray-900: #0E0E0F;
            --gray-800: #1A1A1C;
            --gray-700: #2A2A2D;
            --gray-600: #3A3A3E;
            --gray-400: #9CA3AF;
            --gray-300: #D1D5DB;
            --white: #FFFFFF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--dark);
            color: var(--white);
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Header Section */
        .assessment-header {
            background: linear-gradient(135deg, var(--dark) 0%, var(--gray-900) 100%);
            padding: 6rem 0 4rem;
            position: relative;
            overflow: hidden;
        }

        .assessment-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 25% 75%, rgba(255, 107, 90, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 75% 25%, rgba(78, 205, 196, 0.08) 0%, transparent 50%);
            pointer-events: none;
        }

        .header-content {
            position: relative;
            z-index: 2;
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--coral);
            text-decoration: none;
            font-weight: 500;
            margin-bottom: 3rem;
            padding: 0.75rem 1.5rem;
            background: rgba(255, 107, 90, 0.1);
            border: 1px solid rgba(255, 107, 90, 0.2);
            border-radius: 50px;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }

        .back-link:hover {
            background: rgba(255, 107, 90, 0.15);
            border-color: rgba(255, 107, 90, 0.3);
            transform: translateY(-2px);
        }

        .assessment-header h1 {
            font-family: 'Big Shoulders Display', sans-serif;
            font-size: clamp(2.5rem, 6vw, 4.5rem);
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            letter-spacing: -0.02em;
            background: linear-gradient(135deg, var(--white) 0%, var(--gray-300) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .assessment-header p {
            font-size: 1.25rem;
            color: var(--gray-400);
            font-weight: 300;
            max-width: 600px;
            margin: 0 auto;
        }

        /* Main Assessment Section */
        .assessment-main {
            padding: 4rem 0 8rem;
            background: var(--gray-900);
            position: relative;
        }

        .assessment-container {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
        }

        /* Progress Indicator */
        .progress-section {
            margin-bottom: 4rem;
            text-align: center;
        }

        .progress-steps {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .progress-step {
            display: flex;
            align-items: center;
            gap: 1rem;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 1rem 1.5rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .progress-step.active {
            border-color: rgba(255, 107, 90, 0.3);
            background: rgba(255, 107, 90, 0.05);
        }

        .step-number {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, var(--coral) 0%, var(--teal) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            color: var(--white);
        }

        .step-number.inactive {
            background: var(--gray-600);
        }

        .step-text {
            font-weight: 500;
            color: var(--gray-300);
            font-size: 0.9rem;
        }

        .progress-arrow {
            color: var(--coral);
            font-size: 1.2rem;
        }

        /* Form Container */
        .form-container {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 3rem;
            backdrop-filter: blur(20px);
            position: relative;
            overflow: hidden;
        }

        .form-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--coral) 0%, var(--teal) 100%);
        }

        /* Form Placeholder Styling */
        .form-placeholder {
            text-align: center;
            padding: 4rem 2rem;
            background: rgba(255, 255, 255, 0.02);
            border: 2px dashed rgba(255, 107, 90, 0.2);
            border-radius: 16px;
            position: relative;
        }

        .form-placeholder::before {
            content: '📋';
            font-size: 3rem;
            display: block;
            margin-bottom: 1.5rem;
            opacity: 0.7;
        }

        .form-placeholder p {
            color: var(--gray-400);
            font-size: 1.1rem;
            margin-bottom: 1rem;
            line-height: 1.6;
        }

        .form-placeholder strong {
            color: var(--coral);
        }

        /* Expected Form Styling */
        .form-section {
            margin-bottom: 2.5rem;
        }

        .form-section h3 {
            font-family: 'Big Shoulders Display', sans-serif;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--white);
            margin-bottom: 1rem;
            padding-left: 1rem;
            border-left: 3px solid var(--coral);
        }

        .form-field {
            margin-bottom: 1.5rem;
        }

        .form-field label {
            display: block;
            color: var(--gray-300);
            font-weight: 500;
            margin-bottom: 0.5rem;
        }

        .form-field input,
        .form-field select,
        .form-field textarea {
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 1rem;
            color: var(--white);
            font-family: inherit;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-field input:focus,
        .form-field select:focus,
        .form-field textarea:focus {
            outline: none;
            border-color: var(--coral);
            box-shadow: 0 0 0 3px rgba(255, 107, 90, 0.1);
        }

        .form-field textarea {
            min-height: 120px;
            resize: vertical;
        }

        /* Radio and Checkbox Groups */
        .radio-group,
        .checkbox-group {
            display: grid;
            gap: 0.75rem;
            margin-top: 0.5rem;
        }

        .radio-option,
        .checkbox-option {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .radio-option:hover,
        .checkbox-option:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 107, 90, 0.2);
        }

        .radio-option input,
        .checkbox-option input {
            width: auto;
            margin: 0;
        }

        /* Submit Button */
        .submit-section {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .submit-btn {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            background: linear-gradient(135deg, var(--coral) 0%, var(--coral-light) 100%);
            color: var(--white);
            padding: 1.5rem 3rem;
            border: none;
            border-radius: 16px;
            font-size: 1.2rem;
            font-weight: 700;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(255, 107, 90, 0.3);
        }

        .submit-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s ease;
        }

        .submit-btn:hover::before {
            left: 100%;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 25px 50px rgba(255, 107, 90, 0.4);
        }

        /* Security Note */
        .security-note {
            margin-top: 2rem;
            text-align: center;
            color: var(--gray-500);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .security-note::before {
            content: '🔒';
            font-size: 1rem;
        }

        /* Results Preview */
        .results-preview {
            margin-top: 4rem;
            background: rgba(78, 205, 196, 0.03);
            border: 1px solid rgba(78, 205, 196, 0.1);
            border-radius: 20px;
            padding: 2.5rem;
            text-align: center;
        }

        .results-preview h3 {
            font-family: 'Big Shoulders Display', sans-serif;
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--teal);
            margin-bottom: 1rem;
        }

        .results-preview p {
            color: var(--gray-300);
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        .results-features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }

        .results-feature {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            color: var(--gray-400);
            font-size: 0.9rem;
        }

        .results-feature::before {
            content: '✓';
            color: var(--teal);
            font-weight: 700;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 0 1rem;
            }

            .progress-steps {
                flex-direction: column;
                gap: 1rem;
            }

            .progress-arrow {
                transform: rotate(90deg);
            }

            .form-container {
                padding: 2rem;
            }

            .form-placeholder {
                padding: 3rem 1.5rem;
            }

            .results-features {
                grid-template-columns: 1fr;
            }

            .submit-btn {
                padding: 1.25rem 2.5rem;
                font-size: 1.1rem;
            }
        }

        /* Loading State */
        .loading {
            opacity: 0.7;
            pointer-events: none;
        }

        .loading .submit-btn {
            background: var(--gray-600);
        }

        .loading .submit-btn::after {
            content: '';
            width: 20px;
            height: 20px;
            border: 2px solid var(--white);
            border-top: 2px solid transparent;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-left: 0.5rem;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in-up {
            animation: fadeInUp 0.6s ease forwards;
        }
    </style>
</head>
<body class="assessment-page">
    <!-- Header Section -->
    <header class="assessment-header">
        <div class="container">
            <div class="header-content fade-in-up">
                <a href="../" class="back-link">← Back to Scope Intelligence</a>
                <h1>Strategic Marketing Assessment</h1>
                <p>Help us recommend the optimal marketing engagement for your business situation</p>
            </div>
        </div>
    </header>

    <!-- Main Assessment Section -->
    <main class="assessment-main">
        <div class="container">
            <div class="assessment-container">
                <!-- Progress Indicator -->
                <div class="progress-section fade-in-up">
                    <div class="progress-steps">
                        <div class="progress-step active">
                            <div class="step-number">1</div>
                            <div class="step-text">Assessment</div>
                        </div>
                        <div class="progress-arrow">→</div>
                        <div class="progress-step">
                            <div class="step-number inactive">2</div>
                            <div class="step-text">Analysis</div>
                        </div>
                        <div class="progress-arrow">→</div>
                        <div class="progress-step">
                            <div class="step-number inactive">3</div>
                            <div class="step-text">Recommendations</div>
                        </div>
                    </div>
                </div>

                <!-- Form Container -->
                <div class="form-container fade-in-up">
                    <div id="form-container">
                        <!-- Placeholder for Google Form -->
                        <div class="form-placeholder">
                            <p>Strategic Assessment form will be embedded here once Google Form is created.</p>
                            <p><strong>Next step:</strong> Create Google Form covering business stage, objectives, timeline, budget, team structure, and market complexity.</p>
                        </div>
                        
                        <!-- Example of what the form structure should look like -->
                        <!-- This can be removed once Google Form is embedded -->
                        <div style="display: none;">
                            <div class="form-section">
                                <h3>Business Context</h3>
                                <div class="form-field">
                                    <label>What stage is your business in?</label>
                                    <div class="radio-group">
                                        <div class="radio-option">
                                            <input type="radio" name="business_stage" value="startup">
                                            <span>Early-stage startup (pre-revenue or early revenue)</span>
                                        </div>
                                        <div class="radio-option">
                                            <input type="radio" name="business_stage" value="growth">
                                            <span>Growth stage (established revenue, scaling)</span>
                                        </div>
                                        <div class="radio-option">
                                            <input type="radio" name="business_stage" value="enterprise">
                                            <span>Enterprise (established business, optimization focus)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="form-section">
                                <h3>Marketing Objectives</h3>
                                <div class="form-field">
                                    <label>What are your primary marketing goals? (Select all that apply)</label>
                                    <div class="checkbox-group">
                                        <div class="checkbox-option">
                                            <input type="checkbox" name="objectives" value="brand_awareness">
                                            <span>Build brand awareness and recognition</span>
                                        </div>
                                        <div class="checkbox-option">
                                            <input type="checkbox" name="objectives" value="lead_generation">
                                            <span>Generate qualified leads</span>
                                        </div>
                                        <div class="checkbox-option">
                                            <input type="checkbox" name="objectives" value="revenue_growth">
                                            <span>Drive revenue growth</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="submit-section">
                                <button type="submit" class="submit-btn">
                                    Get My Strategic Recommendation →
                                </button>
                                <div class="security-note">
                                    Your information is secure and will never be shared
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- What You'll Receive -->
                <div class="results-preview fade-in-up">
                    <h3>What You'll Receive</h3>
                    <p>Your personalized assessment results will include a detailed recommendation report with actionable insights tailored to your specific business situation.</p>
                    
                    <div class="results-features">
                        <div class="results-feature">Optimal engagement type recommendation</div>
                        <div class="results-feature">Custom project timeline and milestones</div>
                        <div class="results-feature">Investment guidance and ROI projections</div>
                        <div class="results-feature">Implementation roadmap and next steps</div>
                        <div class="results-feature">Strategic planning resources</div>
                        <div class="results-feature">Follow-up consultation opportunity</div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Intersection Observer for animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('fade-in-up');
                    }
                });
            }, observerOptions);

            // Observe animated elements
            document.querySelectorAll('.fade-in-up').forEach(el => {
                observer.observe(el);
            });

            // Form submission simulation
            const submitBtn = document.querySelector('.submit-btn');
            if (submitBtn) {
                submitBtn.addEventListener('click', function(e) {
                    if (this.tagName === 'BUTTON') {
                        e.preventDefault();
                        // Add loading state
                        document.body.classList.add('loading');
                        
                        // Simulate processing
                        setTimeout(() => {
                            document.body.classList.remove('loading');
                            alert('Assessment submitted! You will receive your recommendations shortly.');
                        }, 2000);
                    }
                });
            }

            // Progress step animation
            setTimeout(() => {
                const steps = document.querySelectorAll('.progress-step');
                steps.forEach((step, index) => {
                    setTimeout(() => {
                        step.style.transform = 'scale(1.05)';
                        setTimeout(() => {
                            step.style.transform = 'scale(1)';
                        }, 200);
                    }, index * 500);
                });
            }, 1000);
        });
    </script>
</body>
</html>