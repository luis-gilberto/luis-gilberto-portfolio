LUIS GILBERTO - PASSWORD PRICING FIX INSTRUCTIONS
===============================================

PROBLEM: Password functionality not working due to HTML/JS mismatches

SOLUTION: 3 Simple Steps

STEP 1: REPLACE HTML SECTION
----------------------------
Find this around line 520-560:

<div class="password-input-group">
    <input type="password" id="pricing-password" class="password-input" placeholder="Enter access code" maxlength="20">
    <button type="button" class="password-submit" id="password-submit">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
            <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
    </button>
</div>

<div class="password-error" id="password-error">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
        <circle cx="12" cy="12" r="10" stroke="#F96F6E" stroke-width="2"/>
        <line x1="15" y1="9" x2="9" y2="15" stroke="#F96F6E" stroke-width="2"/>
        <line x1="9" y1="9" x2="15" y2="15" stroke="#F96F6E" stroke-width="2"/>
    </svg>
    <span>Incorrect access code. Please try again.</span>
</div>

REPLACE WITH THIS:

<form class="password-form" id="password-form">
    <input 
        type="password" 
        id="password-input" 
        class="password-input"
        placeholder="Enter access code"
        maxlength="20"
        required
    >
    <button type="submit" class="password-submit" id="password-submit">
        <span class="button-text">Access</span>
        <span class="button-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
                <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </span>
    </button>
</form>

<div class="password-error" id="password-error">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
        <circle cx="12" cy="12" r="10" stroke="#F96F6E" stroke-width="2"/>
        <line x1="15" y1="9" x2="9" y2="15" stroke="#F96F6E" stroke-width="2"/>
        <line x1="9" y1="9" x2="15" y2="15" stroke="#F96F6E" stroke-width="2"/>
    </svg>
    <span class="error-text">Incorrect access code. Please try again.</span>
</div>

STEP 2: ADD CSS STYLES
----------------------
Add this to your <style> section around line 200:

.password-form {
    display: flex;
    gap: 10px;
    margin-bottom: 15px;
}

.button-text {
    margin-right: 8px;
}

.button-icon {
    display: inline-flex;
    align-items: center;
}

.error-text {
    display: inline;
}

STEP 3: UPDATE JAVASCRIPT
-------------------------
Find the bindEvents() method around line 760:

OLD:
bindEvents() {
    const toggle = document.getElementById('pricing-toggle');
    const passwordForm = document.getElementById('password-form');
    const passwordInput = document.getElementById('password-input');

    if (toggle) {
        toggle.addEventListener('change', () => this.handleToggle());
    }

    if (passwordForm) {
        passwordForm.addEventListener('submit', (e) => this.handlePasswordSubmit(e));
    }

    if (passwordInput) {
        passwordInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                e.preventDefault();
                this.handlePasswordSubmit(e);
            }
        });
    }
}

NEW:
bindEvents() {
    const toggle = document.getElementById('pricing-toggle');
    const passwordForm = document.getElementById('password-form');

    if (toggle) {
        toggle.addEventListener('change', () => this.handleToggle());
    }

    if (passwordForm) {
        passwordForm.addEventListener('submit', (e) => this.handlePasswordSubmit(e));
    }
}

Then find handlePasswordSubmit method and replace:

OLD:
const passwordInput = document.getElementById('password-input');

NEW:
const passwordInput = document.getElementById('password-input');

PASSWORD TO USE: access2024

TESTING:
1. Save file
2. Refresh page 
3. Toggle "Show Detailed Pricing"
4. Enter: access2024
5. Click Access button
6. Should show pricing grid

MAIN FIXES:
- Changed pricing-password to password-input ID
- Added form wrapper for proper submission
- Added button-text and error-text spans
- Fixed event binding

That's it! The password should work now.