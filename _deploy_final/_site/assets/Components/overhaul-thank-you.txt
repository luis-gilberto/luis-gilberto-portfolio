code
Html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Received | Luis Gilberto</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Big+Shoulders+Display:wght@600;700;800&display=swap" rel="stylesheet">
    
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <style>
        /* === SYSTEM VARIABLES === */
        :root {
            --coral: #F96F6E;
            --teal: #2ED3C6;
            --bg-primary: #FFFFFF;
            --text-primary: #1a1a1a;
            --text-secondary: #555555;
            --border-light: rgba(0, 0, 0, 0.06);
            --grid-color: rgba(0, 0, 0, 0.08);
            --card-bg: #FFFFFF;
            --shadow-soft: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        [data-theme="dark"] {
            --bg-primary: #0E0E0F;
            --text-primary: rgba(245, 245, 245, 0.94);
            --text-secondary: rgba(245, 245, 245, 0.6);
            --border-light: rgba(255, 255, 255, 0.05);
            --grid-color: rgba(255, 255, 255, 0.03);
            --card-bg: rgba(255, 255, 255, 0.02);
            --shadow-soft: 0 4px 20px rgba(0, 0, 0, 0.4);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-primary);
            background-image: radial-gradient(var(--grid-color) 1px, transparent 1px);
            background-size: 24px 24px;
            color: var(--text-primary);
            line-height: 1.6;
            padding-top: 112px;
            transition: background-color 0.3s ease, color 0.3s ease;
            overflow-x: hidden;
        }

        /* === CURSOR === */
        #cursor-lens { position: fixed; pointer-events: none; z-index: 200000; width: 40px; height: 40px; transform: translate(-50%, -50%); transition: all 0.15s cubic-bezier(0.25, 0.46, 0.45, 0.94); }
        .lens-outer { position: absolute; top: 50%; left: 50%; width: 40px; height: 40px; border: 2px solid var(--coral); border-radius: 50%; transform: translate(-50%, -50%); opacity: 0.4; transition: all 0.2s ease; }
        .lens-center { position: absolute; top: 50%; left: 50%; width: 4px; height: 4px; background: var(--teal); border-radius: 50%; transform: translate(-50%, -50%); box-shadow: 0 0 10px var(--teal); }
        body.cursor-hover .lens-outer { width: 56px; height: 56px; border-color: var(--teal); opacity: 0.8; }
        @media (pointer: coarse) { #cursor-lens { display: none !important; } body { cursor: auto !important; } }

        /* === NAVIGATION (Always Dark) === */
        .primary-nav { position: fixed; top: 0; left: 0; width: 100%; height: 64px; background: rgba(26, 26, 26, 0.98); backdrop-filter: blur(12px); border-bottom: 1px solid rgba(255, 255, 255, 0.1); z-index: 1000; }
        .primary-nav-container { max-width: 1400px; margin: 0 auto; padding: 0 2rem; height: 64px; display: flex; align-items: center; justify-content: space-between; }
        .nav-logo img { height: 48px; width: auto; } /* Matches Contact */
        .nav-links { display: flex; gap: 2rem; align-items: center; }
        .nav-link { color: rgba(255, 255, 255, 0.7); text-decoration: none; font-size: 14px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.08em; transition: color 0.2s ease; }
        .nav-link:hover { color: #ffffff; }
        
        .theme-toggle { position: fixed; top: 76px; right: 20px; z-index: 1002; background: var(--bg-primary); border: 1px solid var(--border-light); border-radius: 12px; width: 44px; height: 44px; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: transform 0.2s; box-shadow: var(--shadow-soft); color: var(--text-primary); }
        .theme-toggle:hover { transform: scale(1.05); border-color: var(--coral); }
        [data-theme="dark"] .sun-icon { display: block; } [data-theme="dark"] .moon-icon { display: none; }
        .sun-icon { display: none; } .moon-icon { display: block; }

        /* Mobile Toggle */
        .mobile-menu-toggle { display: none; background: transparent; border: none; cursor: pointer; padding: 0.75rem; color: #fff; z-index: 1002; }
        @media (max-width: 1023px) { .nav-links { display: none; } .mobile-menu-toggle { display: flex; } }

        /* === HERO SECTION === */
        .container { max-width: 1000px; margin: 0 auto; padding: 0 2rem; text-align: center; }
        .hero-section { padding: 4rem 0 2rem; position: relative; }
        
        .status-icon-wrapper {
            width: 80px; height: 80px; margin: 0 auto 2rem;
            position: relative; display: flex; align-items: center; justify-content: center;
        }
        .status-ring {
            position: absolute; inset: 0; border: 2px solid var(--coral); border-radius: 50%;
            animation: pulseRing 2s infinite; opacity: 0;
        }
        .status-check {
            width: 100%; height: 100%; background: linear-gradient(135deg, var(--coral), #ff8a8a);
            border-radius: 50%; display: flex; align-items: center; justify-content: center;
            color: white; font-size: 32px; box-shadow: 0 10px 30px rgba(249, 111, 110, 0.4);
            position: relative; z-index: 2;
        }
        @keyframes pulseRing { 0% { transform: scale(0.8); opacity: 0.5; } 100% { transform: scale(2); opacity: 0; } }

        .hero-title { font-family: 'Big Shoulders Display', sans-serif; font-size: clamp(3rem, 8vw, 5rem); margin-bottom: 1rem; line-height: 1; letter-spacing: -1px; text-transform: uppercase; }
        .hero-title span { color: var(--teal); }
        .hero-desc { font-size: 1.2rem; color: var(--text-secondary); max-width: 600px; margin: 0 auto 2rem; }
        .quote { font-family: 'Inter', serif; font-style: italic; color: var(--coral); margin-bottom: 4rem; opacity: 0.8; }

        /* === NEXT STEPS GRID === */
        .steps-section { padding: 4rem 0; }
        .steps-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; text-align: left; }
        
        .step-card {
            background: var(--card-bg); border: 1px solid var(--border-light); border-radius: 16px;
            padding: 2.5rem; transition: transform 0.3s ease, border-color 0.3s ease;
            position: relative; overflow: hidden;
        }
        .step-card:hover { transform: translateY(-5px); border-color: var(--teal); box-shadow: var(--shadow-soft); }
        
        .step-num { font-family: 'Big Shoulders Display'; font-size: 4rem; color: var(--border-light); position: absolute; top: 10px; right: 20px; font-weight: 800; opacity: 0.5; }
        .step-title { font-size: 1.25rem; font-weight: 700; margin-bottom: 1rem; color: var(--text-primary); position: relative; z-index: 2; }
        .step-desc { font-size: 0.95rem; color: var(--text-secondary); line-height: 1.6; position: relative; z-index: 2; }

        /* === CTA SECTION === */
        .cta-section { padding: 4rem 0 6rem; }
        .btn-group { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 2rem; }
        .btn { padding: 14px 32px; border-radius: 50px; font-weight: 600; text-decoration: none; transition: all 0.2s; }
        .btn-primary { background: var(--text-primary); color: var(--bg-primary); }
        .btn-primary:hover { background: var(--coral); color: white; transform: translateY(-2px); }
        .btn-outline { border: 1px solid var(--border-light); color: var(--text-primary); }
        .btn-outline:hover { border-color: var(--text-primary); }

        /* === MOBILE DRAWER (Standardized) === */
        .mobile-menu-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.35); z-index: 2000; opacity: 0; pointer-events: none; transition: opacity 0.3s ease; }
        .mobile-menu-overlay.active { opacity: 1; pointer-events: auto; }
        .mobile-drawer { position: absolute; top: 0; right: 0; height: 100vh; width: 85vw; max-width: 400px; background: #000000; transform: translateX(100%); transition: transform 0.35s ease; display: flex; flex-direction: column; overflow-y: auto; }
        .mobile-menu-overlay.active .mobile-drawer { transform: translateX(0); }
        .mobile-menu-close { position: absolute; top: 1rem; right: 1rem; background: none; border: none; color: white; font-size: 32px; cursor: pointer; z-index: 10; }
        .mobile-menu-content { padding: 2rem; display: flex; flex-direction: column; gap: 2rem; margin-top: 2rem; }
        .mobile-nav-label { font-size: 0.7rem; color: #666; text-transform: uppercase; letter-spacing: 0.15em; margin-bottom: 1rem; }
        .mobile-link { display: block; color: #aaa; text-decoration: none; font-size: 1.1rem; margin-bottom: 1rem; transition: color 0.2s; }
        .mobile-link.main { font-family: 'Big Shoulders Display'; font-size: 2rem; color: #fff; font-weight: 700; margin-bottom: 0.5rem; }
        .mobile-link:hover { color: var(--coral); }

        /* === FOOTER STYLES (Standardized) === */
        .site-footer { background: #0A0A0A; border-top: 1px solid rgba(255,255,255,0.1); padding: 4rem 2rem 2rem; color: #ffffff; margin-top: auto; }
        .footer-container { max-width: 1400px; margin: 0 auto; display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr; gap: 3rem; }
        .footer-brand h3 { font-family: 'Big Shoulders Display'; font-size: 1.5rem; text-transform: uppercase; color: white; margin-bottom: 1rem; }
        .footer-tagline { color: rgba(255,255,255,0.5); font-size: 0.9rem; margin-bottom: 2rem; }
        .footer-heading { font-family: 'Big Shoulders Display'; font-size: 1rem; letter-spacing: 0.1em; color: rgba(255,255,255,0.4); text-transform: uppercase; margin-bottom: 1.5rem; }
        .footer-list { list-style: none; display: flex; flex-direction: column; gap: 0.75rem; }
        .footer-link { color: rgba(255,255,255,0.7); text-decoration: none; font-size: 0.9rem; transition: color 0.2s; }
        .footer-link:hover { color: white; }
        .footer-bottom { border-top: 1px solid rgba(255,255,255,0.05); margin-top: 3rem; padding-top: 2rem; text-align: center; color: rgba(255,255,255,0.3); font-size: 0.8rem; }
        
        @media (max-width: 900px) { .footer-container { grid-template-columns: 1fr; text-align: center; gap: 2rem; } }
    </style>
</head>
<body>
    <div id="cursor-lens"><div class="lens-outer"></div><div class="lens-center"></div></div>

    <!-- HEADER -->
    <nav class="primary-nav">
        <div class="primary-nav-container">
            <a href="/index.html" class="nav-logo">
                <img src="/assets/images/AUg_logo_White.png" alt="LG">
            </a>
            <div class="nav-links">
                <a href="/index.html" class="nav-link">Portfolio</a>
                <a href="/insights/index.html" class="nav-link">Insights</a>
                <a href="/TheHub/index.html" class="nav-link">The Hub</a>
                <a href="https://portal.luis-gilberto.com" class="nav-link" target="_blank">The Portal</a>
            </div>
            <button class="mobile-menu-toggle" id="mobileMenuToggle">
                <i class="fa-solid fa-bars" style="font-size: 24px;"></i>
            </button>
        </div>
    </nav>

    <!-- THEME TOGGLE -->
    <button class="theme-toggle" id="themeToggle">
        <i class="fa-regular fa-sun sun-icon"></i>
        <i class="fa-regular fa-moon moon-icon"></i>
    </button>

    <!-- MOBILE DRAWER -->
    <div class="mobile-menu-overlay" id="mobileMenuOverlay">
        <div class="mobile-drawer">
            <button class="mobile-menu-close" id="mobileMenuClose">&times;</button>
            <div class="mobile-menu-content">
                <div>
                    <div class="mobile-nav-label">Global</div>
                    <a href="/index.html" class="mobile-link main">Portfolio</a>
                    <a href="/insights/index.html" class="mobile-link main">Insights</a>
                    <a href="/TheHub/index.html" class="mobile-link main">The Hub</a>
                    <a href="https://portal.luis-gilberto.com" class="mobile-link main">The Portal</a>
                </div>
                <div>
                    <div class="mobile-nav-label">Connect</div>
                    <a href="/about.html" class="mobile-link">About Me</a>
                    <a href="/contact.html" class="mobile-link">Contact</a>
                </div>
            </div>
        </div>
    </div>

    <!-- MAIN CONTENT -->
    <main>
        <section class="hero-section">
            <div class="container">
                <div class="status-icon-wrapper">
                    <div class="status-ring"></div>
                    <div class="status-check"><i class="fa-solid fa-check"></i></div>
                </div>
                <h1 class="hero-title">Message <span>Received</span></h1>
                <p class="hero-desc">Your project details are in my hands. Whether this came from The Hub or a direct inquiry, I'm already reviewing the context.</p>
                <div class="quote">"Every great campaign starts with a conversation. Let's make yours count."</div>
            </div>
        </section>

        <section class="steps-section">
            <div class="container">
                <h2 style="font-family:'Big Shoulders Display'; font-size:2.5rem; margin-bottom:2rem;">What Happens Next?</h2>
                <div class="steps-grid">
                    <div class="step-card">
                        <div class="step-num">01</div>
                        <h3 class="step-title">Review & Analysis</h3>
                        <p class="step-desc">I'll review your brief (or message) within 24 hours to understand the friction points and goals you've outlined.</p>
                    </div>
                    <div class="step-card">
                        <div class="step-num">02</div>
                        <h3 class="step-title">Discovery Response</h3>
                        <p class="step-desc">I'll send a personalized response. If the fit is right, we'll book a 30-minute discovery call to align on scope.</p>
                    </div>
                    <div class="step-card">
                        <div class="step-num">03</div>
                        <h3 class="step-title">Strategic Proposal</h3>
                        <p class="step-desc">Following our chat, I'll craft a tailored proposal or roadmap that transforms your challenges into momentum.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="cta-section">
            <div class="container">
                <h2 style="font-family:'Big Shoulders Display'; font-size:2.5rem;">While You Wait</h2>
                <div class="btn-group">
                    <a href="/index.html" class="btn btn-primary">View Portfolio</a>
                    <a href="/insights/index.html" class="btn btn-outline">Read Insights</a>
                </div>
            </div>
        </section>
    </main>

    <!-- STANDARD FOOTER -->
    <footer class="site-footer">
        <div class="footer-container">
            <div class="footer-brand">
                <a href="/index.html" style="text-decoration:none; display:flex; align-items:center; gap:10px;">
                    <img src="/assets/images/Logomark_White_a.png" alt="LG" style="height:32px; opacity:0.9;">
                    <h3 style="margin:0; font-size:1.5rem;">Luis Gilberto</h3>
                </a>
                <p class="footer-tagline">Making technology feel human through clarity, beautiful execution, and systems that scale.</p>
            </div>
            <div>
                <h4 class="footer-heading">Ecosystem</h4>
                <ul class="footer-list">
                    <li><a href="/index.html" class="footer-link">Portfolio</a></li>
                    <li><a href="/TheHub/index.html" class="footer-link">The Hub</a></li>
                    <li><a href="/insights/index.html" class="footer-link">Insights</a></li>
                    <li><a href="https://portal.luis-gilberto.com" class="footer-link" style="color:var(--teal);">The Portal</a></li>
                </ul>
            </div>
            <div>
                <h4 class="footer-heading">Connect</h4>
                <ul class="footer-list">
                    <li><a href="/contact.html" class="footer-link">Contact</a></li>
                    <li><a href="/about.html" class="footer-link">About Me</a></li>
                    <li><a href="https://linkedin.com/in/luisgilberto00" class="footer-link">LinkedIn</a></li>
                </ul>
            </div>
            <div>
                <h4 class="footer-heading">Status</h4>
                <div style="background:rgba(255,255,255,0.05); padding:1rem; border-radius:8px; border:1px solid rgba(255,255,255,0.1);">
                    <div style="display:flex; align-items:center; gap:8px; margin-bottom:8px;">
                        <span style="width:8px; height:8px; background:var(--teal); border-radius:50%;"></span>
                        <span style="font-weight:700; text-transform:uppercase; font-size:0.8rem;">Available</span>
                    </div>
                    <p style="font-size:0.8rem; margin:0; opacity:0.7;">Accepting new strategic projects.</p>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            &copy; 2026 Luis Gilberto. All rights reserved.
        </div>
    </footer>

    <!-- SCRIPTS -->
    <script>
        // Theme Logic
        const themeToggle = document.getElementById('themeToggle');
        const html = document.documentElement;
        const savedTheme = localStorage.getItem('theme') || 'light';
        html.setAttribute('data-theme', savedTheme);

        if(themeToggle) {
            themeToggle.addEventListener('click', () => {
                const current = html.getAttribute('data-theme');
                const next = current === 'light' ? 'dark' : 'light';
                html.setAttribute('data-theme', next);
                localStorage.setItem('theme', next);
            });
        }

        // Mobile Menu Logic
        const menuToggle = document.getElementById('mobileMenuToggle');
        const menuOverlay = document.getElementById('mobileMenuOverlay');
        const menuClose = document.getElementById('mobileMenuClose');

        if(menuToggle && menuOverlay) {
            menuToggle.addEventListener('click', () => {
                menuOverlay.classList.add('active');
                document.body.style.overflow = 'hidden';
            });
            const closeMenu = () => {
                menuOverlay.classList.remove('active');
                document.body.style.overflow = '';
            };
            menuClose.addEventListener('click', closeMenu);
            menuOverlay.addEventListener('click', (e) => {
                if(e.target === menuOverlay) closeMenu();
            });
        }

        // Cursor Logic
        const cursor = document.getElementById('cursor-lens');
        document.addEventListener('mousemove', (e) => {
            if(cursor) {
                cursor.style.left = e.clientX + 'px';
                cursor.style.top = e.clientY + 'px';
            }
        });
        document.querySelectorAll('a, button').forEach(el => {
            el.addEventListener('mouseenter', () => document.body.classList.add('cursor-hover'));
            el.addEventListener('mouseleave', () => document.body.classList.remove('cursor-hover'));
        });
    </script>
</body>
</html>