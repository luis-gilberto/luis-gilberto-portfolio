# ==========================================
# WRANGLER DEPLOYMENT STRATEGY - EXCLUSION LIST
# ==========================================
# This file ensures ONLY the Vanilla HTML site is deployed to Cloudflare Pages.
# The React Portal (creative-portal/) is explicitly ignored to prevent
# accidental deployment or build interference.

# 1. Exclude React Portal Application
creative-portal/

# 2. Large assets excluded to stay under Cloudflare Pages 25MB per-file limit
**/*.zip
**/*.tar
**/*.tar.gz
**/*.7z
**/*.mp4
**/*.mov
**/*.avi
CRITICAL_ASSETS/**
assets/videos/**
!insights/assets/videos/**
!assets/Components/Insights_animation/**

# 3. Node and tooling
node_modules/
.git/
.github/
.DS_Store

# 4. Python virtual envs and heavy native deps
.venv/
.venv/**
venv/
venv/**
**/.venv/**
**/venv/**

# 5. Misc local dev projects not part of static portfolio deploy
homepage-circular-test/
insights-video-worker/
ScopeIQ_PREFERRED_VERSION_BACKUP/
IMCServices_scopeiq-wizard_BACKUP/

# 6. Legacy deploy snapshots (do not serve)
_deploy_v2/
_deploy_final/
