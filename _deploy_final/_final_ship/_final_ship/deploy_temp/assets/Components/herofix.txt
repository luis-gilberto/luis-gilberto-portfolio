# COMPLETE HERO FIX - ONE COPY/PASTE SOLUTION

## STEP 1: REPLACE HERO HTML
Find your current hero section (starts with `<section class="hero" id="hero">`) and replace it entirely with this:

```html
<section class="hero" id="hero">
    <div class="hero-content">
        <div class="hero-badge">ADVISORY</div>
        
        <img src="/assets/Luis_portrait_circle.png" alt="Luis Gilberto" class="hero-photo" />
        
        <h1>Strategic Consulting by Luis Gilberto</h1>
        <p class="hero-subtitle">15+ years at Microsoft, now helping brands unlock clarity and growth.</p>
        <p class="hero-value-prop">
            Proven strategy meets fresh creativity. From positioning to execution, I'll help you move faster and smarter.
        </p>
        
        <div class="hero-buttons">
            <a href="https://luis-gilberto.com/IMCServices/scopeiq-wizard/" class="hero-cta">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                Get Custom Quote
            </a>
            <a href="https://luis-gilberto.com/contact?from=imc#cta-hook" class="hero-cta" style="background: transparent; border: 2px solid var(--lg-coral); color: var(--lg-coral); padding: 12px 26px;">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                Start Strategy Discussion
            </a>
        </div>
    </div>
</section>
```

## STEP 2: REPLACE HERO CSS
Find all your current hero CSS rules (everything from `/* Hero Section */` down to the responsive section) and replace them entirely with this:

```css
/* Hero Section */
.hero {
    padding: 2rem 0 4rem;
    margin-top: 60px;
    max-width: 1000px;
    margin-left: auto;
    margin-right: auto;
}

.hero-content {
    position: relative;
    text-align: left;
    line-height: 1.4;
}

.hero-badge {
    font-family: 'Inter', sans-serif;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--lg-coral);
    margin-bottom: 1.5rem;
    display: block;
    font-weight: 600;
    text-align: center;
    opacity: 0.8;
}

.hero-photo {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 
        0 8px 32px rgba(249, 111, 110, 0.25),
        0 0 0 4px rgba(249, 111, 110, 0.1),
        0 0 0 8px rgba(249, 111, 110, 0.05);
    
    float: left;
    margin: 0.5rem 3rem 2rem 0;
    shape-outside: circle(75px at 50% 50%);
    shape-margin: 2.5rem;
    
    position: relative;
    z-index: 3;
    animation: fadeInPhoto 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    filter: drop-shadow(0 4px 20px rgba(249, 111, 110, 0.15));
}

.hero h1 {
    font-size: clamp(3rem, 6vw, 4.5rem);
    margin-bottom: 2rem;
    color: var(--lg-fg);
    line-height: 1.05;
    clear: none;
    text-align: left;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-top: -0.5rem;
    text-indent: 0;
}

.hero-subtitle {
    font-size: 1.35rem;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    line-height: 1.5;
    clear: none;
    font-weight: 400;
    letter-spacing: -0.01em;
}

.hero-value-prop {
    font-size: 1.1rem;
    color: var(--text-muted);
    margin-bottom: 2.5rem;
    line-height: 1.7;
    clear: left;
    max-width: none;
    font-weight: 300;
    letter-spacing: 0.01em;
}

.hero-buttons {
    display: flex;
    gap: 1.5rem;
    margin-top: 2rem;
    justify-content: flex-start;
    flex-wrap: wrap;
    clear: left;
}

.hero-cta {
    display: inline-flex;
    align-items: center;
    padding: 14px 28px;
    background: var(--lg-coral);
    color: var(--lg-fg);
    text-decoration: none;
    border-radius: 10px;
    font-weight: 500;
    font-size: 1rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 2px solid var(--lg-coral);
    letter-spacing: 0.01em;
}

.hero-cta:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 35px rgba(249, 111, 110, 0.4), 0 0 30px rgba(249, 111, 110, 0.2);
}

.hero-cta svg {
    margin-right: 10px;
    width: 18px;
    height: 18px;
}

@keyframes fadeInPhoto {
    from { 
        opacity: 0; 
        transform: scale(0.9) translateY(20px);
        filter: drop-shadow(0 0 0 rgba(249, 111, 110, 0));
    }
    to { 
        opacity: 1; 
        transform: scale(1) translateY(0);
        filter: drop-shadow(0 4px 20px rgba(249, 111, 110, 0.15));
    }
}

@supports not (shape-outside: circle()) {
    .hero-content {
        display: grid;
        grid-template-columns: 140px 1fr;
        gap: 3rem;
        align-items: start;
        margin-top: 2rem;
    }
    
    .hero-photo {
        float: none;
        shape-outside: none;
        shape-margin: 0;
        margin: 0;
        justify-self: center;
    }
    
    .hero-badge {
        grid-column: 1 / -1;
        margin-bottom: 1rem;
    }
}

@media (max-width: 768px) {
    .hero {
        max-width: 100%;
        padding: 1.5rem 0 3rem;
    }
    
    .hero-photo {
        float: none;
        shape-outside: none;
        shape-margin: 0;
        margin: 0 auto 2rem auto;
        display: block;
        width: 110px;
        height: 110px;
    }
    
    .hero-content {
        text-align: center;
        padding: 0 1rem;
    }
    
    .hero h1 {
        text-align: center;
        clear: both;
        font-size: clamp(2.5rem, 8vw, 3.5rem);
        margin-bottom: 1.5rem;
        margin-top: 0;
    }
    
    .hero-subtitle,
    .hero-value-prop {
        text-align: center;
        clear: both;
    }
    
    .hero-subtitle {
        font-size: 1.2rem;
        margin-bottom: 1rem;
    }
    
    .hero-value-prop {
        font-size: 1rem;
        margin-bottom: 2rem;
    }
    
    .hero-buttons {
        justify-content: center;
        clear: both;
        gap: 1rem;
    }
    
    .hero-cta {
        padding: 12px 24px;
        font-size: 0.95rem;
    }
}
```

## STEP 3: FIX NAVIGATION SHADOW GHOST (BONUS FIX)
Find the mobile nav section around line 360 and replace with this to eliminate the shadow completely:

```css
@media (max-width: 768px) {
    .nav-close {
        display: flex;
    }
    
    .nav-menu {
        position: fixed;
        top: 0;
        right: 0;
        height: 90vh;
        max-height: 90vh;
        width: 100%;
        margin-top: 0;
        border-radius: 0;
        background: rgba(26, 26, 26, 0.95);
        backdrop-filter: blur(20px);
        transform: translateX(100%);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        overflow-y: auto;
        z-index: 999;
        opacity: 1;
        visibility: visible;
        border-left: 1px solid var(--gold-light);
        padding: 1.5rem;
        box-shadow: none !important;
    }
    
    .nav-menu.open {
        transform: translateX(0);
        animation: fadeInDrawer 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: none !important;
    }
    
    .nav-menu.active {
        box-shadow: none !important;
    }
}

@keyframes fadeInDrawer {
    from {
        opacity: 0;
        transform: translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}
```

## DONE
Save files, refresh browser. You now have an elegant editorial layout with sophisticated text wrapping and no navigation shadow.