
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Hub | Luis Gilberto - Strategic Systems & Advisory</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400;1,700&family=Inter:wght@300;400;500;600;700&family=Big+Shoulders+Display:wght@700;800;900&display=swap" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    
    <style>
        /* ========================================= 
           CORE VARIABLES (v2.2 Trinity System) 
           ========================================= */ 
        :root { 
            --coral: #F96F6E; 
            --teal: #2ED3C6; 
            --ink: #0A0A0A; 
            --cloud: #F4F1ED; 
            --bg-body: #0A0A0A; 
            --surface-dark: #111216; 
            --surface-dark-2: #141414;
            --border-subtle: rgba(255,255,255,0.08);
            --border-light: 1px solid rgba(255,255,255,0.08); 
            --radius: 16px; 
            --radius-sm: 8px;
            --header-height: 72px; 
            --container-width: 1400px; 

            /* Trinity Typography */
            --font-strategist: 'Big Shoulders Display', sans-serif;
            --font-storyteller: 'Playfair Display', serif;
            --font-orchestrator: 'Inter', sans-serif;
            
            /* Transitions */
            --ease-out: cubic-bezier(0.4, 0, 0.2, 1);
            --ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
        } 

        [data-theme="light"] {
            --bg-body: #F4F1ED;
            --ink: #F4F1ED;
            --cloud: #0A0A0A;
            --surface-dark: #FFFFFF;
            --surface-dark-2: #F9F9F9;
            --border-subtle: rgba(0,0,0,0.08);
            --border-light: 1px solid rgba(0,0,0,0.08);
        }

        /* ========================================= 
           RESET & BASE 
           ========================================= */ 
        * { margin: 0; padding: 0; box-sizing: border-box; } 
        html { scroll-behavior: smooth; } 
        body { 
            background-color: var(--bg-body); 
            color: #EDEDED; 
            font-family: var(--font-orchestrator); 
            line-height: 1.6; 
            overflow-x: hidden; 
            transition: background-color 0.3s ease, color 0.3s ease;
            padding-top: var(--header-height);
        } 
        [data-theme="light"] body { color: #111; }

        h1, h2, h3, h4 { font-family: var(--font-strategist); text-transform: uppercase; line-height: 0.95; color: #FFF; } 
        [data-theme="light"] h1, [data-theme="light"] h2, [data-theme="light"] h3, [data-theme="light"] h4 { color: #111; }
        p { font-weight: 300; margin-bottom: 1rem; color: rgba(255,255,255,0.7); } 
        [data-theme="light"] p { color: #555; }
        a { text-decoration: none; color: inherit; transition: all 0.2s ease; } 

        .container { max-width: var(--container-width); margin: 0 auto; padding: 0 2rem; } 
        .section { padding: 8rem 0; position: relative; } 

        /* ========================================= 
           1. SITE HEADER 
           ========================================= */ 
        .site-header { 
            position: fixed; top: 0; left: 0; width: 100%; height: var(--header-height); 
            background: rgba(10,10,10,0.95); backdrop-filter: blur(12px); 
            border-bottom: 1px solid rgba(255,255,255,0.06); z-index: 10000; 
        } 
        [data-theme="light"] .site-header { 
            background: rgba(244,241,237,0.95); 
            border-bottom: 1px solid rgba(0,0,0,0.06); 
        }

        .nav-container { 
            max-width: 1400px; margin: 0 auto; padding: 0 2rem; height: 100%; 
            display: flex; align-items: center; justify-content: space-between;
        } 

        .nav-logo { display: flex; align-items: center; flex-shrink: 0; }
        .nav-logo img { height: 36px; width: auto; filter: brightness(1.2); } 
        [data-theme="light"] .nav-logo img { filter: none; }

        /* ========================================= 
           2. DESKTOP NAV with MEGA MENUS
           ========================================= */ 
        .desktop-nav { 
            display: flex; align-items: center; gap: 0.25rem; height: 100%; 
        } 

        .nav-link { 
            font-family: var(--font-orchestrator); font-size: 0.8rem; font-weight: 600; 
            text-transform: uppercase; letter-spacing: 0.06em; color: rgba(255,255,255,0.6); 
            text-decoration: none; padding: 0.5rem 0.875rem; border-radius: 6px; 
            transition: all 0.2s; display: flex; align-items: center; gap: 6px; 
            background: transparent; border: none; cursor: pointer; white-space: nowrap;
        } 
        [data-theme="light"] .nav-link { color: #666; }
        .nav-link:hover, .nav-link:focus { color: #FFF; background: rgba(255,255,255,0.05); } 
        .nav-link.active { color: #FFF; background: rgba(255,255,255,0.06); } 
        [data-theme="light"] .nav-link:hover, [data-theme="light"] .nav-link.active { color: #000; background: rgba(0,0,0,0.05); }
        .nav-link .fa-chevron-down { font-size: 0.55rem; opacity: 0.45; transition: transform 0.3s var(--ease-out); }
        
        /* Dropdown trigger wrapper */
        .has-dropdown { position: relative; height: 100%; display: flex; align-items: center; } 
        .has-dropdown:hover .mega-dropdown { 
            opacity: 1; visibility: visible; transform: translateX(-50%) translateY(0); pointer-events: auto; 
        }
        .has-dropdown:hover .nav-link .fa-chevron-down { transform: rotate(180deg); }
        
        /* ---- PORTFOLIO Mega Dropdown ---- */
        .mega-dropdown { 
            position: absolute; top: var(--header-height); left: 50%; 
            transform: translateX(-50%) translateY(8px); 
            width: 520px; background: #0A0A0A; border: 1px solid rgba(255,255,255,0.1); 
            border-radius: 16px; padding: 1.25rem; box-shadow: 0 20px 50px rgba(0,0,0,0.5); 
            opacity: 0; visibility: hidden; pointer-events: none; transition: all 0.25s var(--ease-out); 
        } 
        [data-theme="light"] .mega-dropdown { background: #FFF; border: 1px solid rgba(0,0,0,0.1); box-shadow: 0 20px 50px rgba(0,0,0,0.1); }
        
        .mega-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.25rem; } 
        .mega-link { 
            display: flex; align-items: flex-start; gap: 0.875rem; padding: 0.875rem; 
            border-radius: 10px; text-decoration: none; transition: background 0.2s; 
        } 
        .mega-link:hover { background: rgba(255,255,255,0.05); } 
        [data-theme="light"] .mega-link:hover { background: rgba(0,0,0,0.04); }

        .mega-icon { 
            width: 32px; height: 32px; background: rgba(255,255,255,0.05); border-radius: 8px; 
            display: flex; align-items: center; justify-content: center; color: rgba(255,255,255,0.5); 
            flex-shrink: 0; font-size: 0.85rem; transition: all 0.2s;
        } 
        [data-theme="light"] .mega-icon { background: rgba(0,0,0,0.04); color: #888; }
        .mega-link:hover .mega-icon { background: var(--coral); color: #FFF; } 
        .mega-title { display: block; font-family: var(--font-orchestrator); font-weight: 600; font-size: 0.85rem; color: #FFF; margin-bottom: 0.15rem; } 
        [data-theme="light"] .mega-title { color: #111; }
        .mega-desc { display: block; font-size: 0.72rem; color: #666; line-height: 1.4; } 

        /* ---- THE HUB Mega Dropdown ---- */
        .hub-mega-dropdown {
            position: absolute;
            top: var(--header-height);
            left: 50%;
            transform: translateX(-50%) translateY(8px);
            width: 520px;
            background: #0A0A0A;
            border: 1px solid rgba(255,255,255,0.10);
            border-radius: 16px;
            padding: 1.25rem;
            box-shadow: 0 20px 50px rgba(0,0,0,0.55);
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
            transition: all 0.25s var(--ease-out);
        }
        [data-theme="light"] .hub-mega-dropdown {
            background: #FFF;
            border-color: rgba(0,0,0,0.10);
            box-shadow: 0 20px 50px rgba(0,0,0,0.12);
        }
        .has-dropdown:hover .hub-mega-dropdown {
            opacity: 1; visibility: visible; transform: translateX(-50%) translateY(0); pointer-events: auto;
        }

        .hub-mega-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.25rem;
        }

        .hub-mega-link {
            display: flex;
            align-items: flex-start;
            gap: 0.875rem;
            padding: 0.875rem;
            border-radius: 10px;
            text-decoration: none;
            transition: background 0.2s var(--ease-out), border-color 0.2s var(--ease-out);
            border: 1px solid transparent;
        }
        .hub-mega-link:hover {
            background: rgba(255,255,255,0.05);
        }
        [data-theme="light"] .hub-mega-link:hover {
            background: rgba(0,0,0,0.04);
        }
        .hub-mega-link.active {
            border-color: rgba(249,111,110,0.55);
            background: rgba(249,111,110,0.06);
        }

        .hub-mega-icon {
            width: 32px;
            height: 32px;
            background: rgba(255,255,255,0.05);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: rgba(255,255,255,0.55);
            flex-shrink: 0;
            font-size: 0.85rem;
            transition: background 0.2s var(--ease-out), color 0.2s var(--ease-out);
        }
        [data-theme="light"] .hub-mega-icon {
            background: rgba(0,0,0,0.04);
            color: #888;
        }
        .hub-mega-link:hover .hub-mega-icon {
            background: var(--coral);
            color: #FFF;
        }

        .hub-mega-title {
            display: block;
            font-family: var(--font-orchestrator);
            font-weight: 600;
            font-size: 0.85rem;
            color: #FFF;
            margin-bottom: 0.15rem;
        }
        [data-theme="light"] .hub-mega-title {
            color: #111;
        }

        .hub-mega-desc {
            display: block;
            font-size: 0.72rem;
            color: rgba(255,255,255,0.45);
            line-height: 1.4;
        }
        [data-theme="light"] .hub-mega-desc {
            color: #666;
        }

        .hub-mega-footer {
            margin-top: 0.75rem;
            padding-top: 0.75rem;
            border-top: 1px solid rgba(255,255,255,0.06);
            display: flex;
            justify-content: flex-end;
        }
        [data-theme="light"] .hub-mega-footer {
            border-top-color: rgba(0,0,0,0.08);
        }

        .hub-mega-portal-link {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-family: var(--font-orchestrator);
            font-size: 0.78rem;
            font-weight: 600;
            color: var(--coral);
            text-decoration: none;
            transition: color 0.2s var(--ease-out);
        }
        .hub-mega-portal-link i {
            font-size: 0.65rem;
            transition: transform 0.2s var(--ease-out);
        }
        .hub-mega-portal-link:hover {
            color: #FFF;
        }
        [data-theme="light"] .hub-mega-portal-link:hover {
            color: #111;
        }
        .hub-mega-portal-link:hover i {
            transform: translateX(3px);
        }

        /* ========================================= 
           3. NAV ACTIONS & MOBILE TOGGLE
           ========================================= */
        .nav-actions { display: flex; align-items: center; gap: 0.5rem; flex-shrink: 0; } 
        .theme-toggle, .mobile-toggle { 
            background: none; border: none; cursor: pointer; color: rgba(255,255,255,0.6); 
            font-size: 1.1rem; display: flex; align-items: center; justify-content: center; 
            width: 40px; height: 40px; border-radius: 50%; transition: all 0.2s; 
        } 
        [data-theme="light"] .theme-toggle, [data-theme="light"] .mobile-toggle { color: #555; }
        .theme-toggle:hover, .mobile-toggle:hover { background: rgba(255,255,255,0.06); color: #FFF; }
        [data-theme="light"] .theme-toggle:hover, [data-theme="light"] .mobile-toggle:hover { background: rgba(0,0,0,0.06); color: #111; }

        .mobile-toggle { display: none; }

        /* ========================================= 
           4. MOBILE DRAWER 
           ========================================= */ 
        .drawer-overlay {
            position: fixed; inset: 0; background: rgba(0,0,0,0.6); 
            backdrop-filter: blur(4px); z-index: 19999;
            opacity: 0; visibility: hidden; transition: all 0.3s ease;
        }
        .drawer-overlay.active { opacity: 1; visibility: visible; }

        .mobile-drawer {
            position: fixed; top: 0; right: 0; width: 100%; max-width: 420px; height: 100dvh;
            background: #0A0A0A; z-index: 20000; transform: translateX(100%);
            transition: transform 0.45s var(--ease-spring);
            display: flex; flex-direction: column; overflow-y: auto; overflow-x: hidden;
        }
        [data-theme="light"] .mobile-drawer { background: #F4F1ED; }
        .mobile-drawer.active { transform: translateX(0); }

        .drawer-header { 
            display: flex; justify-content: space-between; align-items: center; 
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid rgba(255,255,255,0.06);
            flex-shrink: 0;
        }
        [data-theme="light"] .drawer-header { border-bottom: 1px solid rgba(0,0,0,0.06); }
        .drawer-header img { height: 28px; }
        .drawer-close { 
            background: none; border: none; color: rgba(255,255,255,0.5); 
            font-size: 1.25rem; cursor: pointer; 
            width: 40px; height: 40px; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            transition: all 0.2s;
        }
        [data-theme="light"] .drawer-close { color: #888; }
        .drawer-close:hover { background: rgba(255,255,255,0.06); color: #FFF; }

        .drawer-content { flex: 1; padding: 0; display: flex; flex-direction: column; }

        /* Hub Section — Featured at top */
        .drawer-hub-section {
            padding: 1.5rem;
            background: transparent;
            border-bottom: 1px solid rgba(255,255,255,0.06);
            position: relative;
        }
        [data-theme="light"] .drawer-hub-section {
            border-bottom: 1px solid rgba(0,0,0,0.06);
        }
        .drawer-hub-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 2px;
            height: 100%;
            background: rgba(249,111,110,0.75);
            border-radius: 0 2px 2px 0;
        }

        .drawer-section-label { 
            display: flex; align-items: center; gap: 0.5rem;
            font-family: var(--font-strategist); font-size: 0.7rem; 
            text-transform: uppercase; letter-spacing: 0.12em; 
            color: var(--coral); margin-bottom: 1.25rem; font-weight: 800; 
        }
        .drawer-section-label i { font-size: 0.6rem; }

        .drawer-hub-grid {
            display: grid; grid-template-columns: 1fr 1fr; gap: 0.625rem;
        }
        .drawer-hub-card {
            display: flex; flex-direction: column; gap: 0.5rem;
            padding: 1rem; border-radius: 12px;
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
            text-decoration: none; transition: all 0.25s;
        }
        [data-theme="light"] .drawer-hub-card { background: rgba(0,0,0,0.02); border-color: rgba(0,0,0,0.06); }
        .drawer-hub-card:hover, .drawer-hub-card:active {
            background: rgba(255,255,255,0.06);
            border-color: rgba(255,255,255,0.12);
            transform: translateY(-2px);
        }
        .drawer-hub-card.active {
            border-color: var(--coral);
            background: rgba(249,111,110,0.06);
        }
        .drawer-hub-card-icon {
            width: 32px; height: 32px; border-radius: 8px;
            background: rgba(255,255,255,0.05);
            display: flex; align-items: center; justify-content: center;
            font-size: 0.8rem; color: rgba(255,255,255,0.4); transition: all 0.2s;
        }
        .drawer-hub-card.active .drawer-hub-card-icon { background: var(--coral); color: #FFF; }
        .drawer-hub-card-title {
            font-family: var(--font-strategist); font-size: 1rem;
            text-transform: uppercase; color: #FFF; letter-spacing: 0.02em;
        }
        [data-theme="light"] .drawer-hub-card-title { color: #111; }
        .drawer-hub-card-sub {
            font-size: 0.7rem; color: rgba(255,255,255,0.4); line-height: 1.4;
        }
        [data-theme="light"] .drawer-hub-card-sub { color: #999; }

        /* Global Nav Section */
        .drawer-global-section { padding: 1.5rem; flex: 1; }
        .drawer-global-section .drawer-section-label { color: rgba(255,255,255,0.3); }
        [data-theme="light"] .drawer-global-section .drawer-section-label { color: #bbb; }

        .drawer-global-link {
            display: flex; align-items: center; gap: 0.875rem;
            padding: 0.875rem 0;
            border-bottom: 1px solid rgba(255,255,255,0.04);
            text-decoration: none; transition: all 0.2s;
        }
        [data-theme="light"] .drawer-global-link { border-bottom-color: rgba(0,0,0,0.04); }
        .drawer-global-link:last-child { border-bottom: none; }
        .drawer-global-link:hover .drawer-global-text { color: #FFF; }
        [data-theme="light"] .drawer-global-link:hover .drawer-global-text { color: #111; }

        .drawer-global-icon {
            width: 36px; height: 36px; border-radius: 8px;
            background: rgba(255,255,255,0.04);
            display: flex; align-items: center; justify-content: center;
            font-size: 0.85rem; color: rgba(255,255,255,0.3);
            flex-shrink: 0; transition: all 0.2s;
        }
        [data-theme="light"] .drawer-global-icon { background: rgba(0,0,0,0.04); color: #aaa; }
        .drawer-global-link:hover .drawer-global-icon { color: var(--teal); }

        .drawer-global-text {
            font-family: var(--font-orchestrator); font-size: 0.9rem;
            font-weight: 500; color: rgba(255,255,255,0.6); transition: color 0.2s;
        }
        [data-theme="light"] .drawer-global-text { color: #666; }
        .drawer-global-link.active .drawer-global-text { color: var(--coral); font-weight: 600; }

        .drawer-footer {
            padding: 1.25rem 1.5rem;
            border-top: 1px solid rgba(255,255,255,0.06);
            display: flex; align-items: center; justify-content: space-between;
            flex-shrink: 0;
        }
        [data-theme="light"] .drawer-footer { border-top-color: rgba(0,0,0,0.06); }
        .drawer-footer-status {
            display: flex; align-items: center; gap: 0.5rem;
            font-size: 0.75rem; color: rgba(255,255,255,0.4);
        }
        [data-theme="light"] .drawer-footer-status { color: #999; }
        .drawer-status-dot {
            width: 6px; height: 6px; background: var(--teal); border-radius: 50%;
            box-shadow: 0 0 6px rgba(46,211,198,0.4);
        }
        .drawer-theme-toggle {
            background: none; border: 1px solid rgba(255,255,255,0.08);
            cursor: pointer; color: rgba(255,255,255,0.4);
            font-size: 0.9rem; width: 36px; height: 36px; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            transition: all 0.2s;
        }
        [data-theme="light"] .drawer-theme-toggle { border-color: rgba(0,0,0,0.1); color: #888; }
        .drawer-theme-toggle:hover { background: rgba(255,255,255,0.06); color: #FFF; }

        /* ========================================= 
           5. HUB HERO 
           ========================================= */ 
        .hero-section { 
            background-color: var(--bg-body); 
            min-height: 72vh; display: flex; align-items: center; 
            padding: 4rem 0;
        }
        .hero-grid { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 3rem; 
            align-items: center;
            width: 100%;
            max-width: 1100px;
            margin: 0 auto;
        }
        .hero-content {
            display: flex; flex-direction: column; align-items: flex-start;
        }
        
        .hero-headline { 
            font-size: clamp(3rem, 5.5vw, 5rem); 
            margin-bottom: 1.5rem; 
            line-height: 0.92; 
        }
        .hero-subhead { 
            font-size: 1.05rem; color: rgba(255,255,255,0.55); 
            max-width: 480px; line-height: 1.8; text-align: left;
        }
        [data-theme="light"] .hero-subhead { color: #666; }

        /* Editorial emphasis: italic + slightly brighter, not colored */
        .hero-subhead em {
            font-family: var(--font-storyteller);
            font-style: italic;
            color: rgba(255,255,255,0.85);
            font-weight: 400;
        }
        [data-theme="light"] .hero-subhead em {
            color: #222;
        }

        .cta-buttons-hero { display: flex; gap: 0.875rem; flex-wrap: wrap; margin-top: 2rem; align-items: center; }
        .btn-coral { 
            background: var(--coral); color: #FFF; padding: 0.875rem 2rem; 
            border-radius: 50px; font-weight: 700; font-size: 0.8rem;
            text-transform: uppercase; letter-spacing: 0.04em;
            transition: all 0.3s var(--ease-out); 
        }
        .btn-coral:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(249,111,110,0.35); }
        .btn-outline-teal { 
            border: 1px solid var(--teal); color: var(--teal); padding: 0.875rem 2rem; 
            border-radius: 50px; font-weight: 700; font-size: 0.8rem;
            text-transform: uppercase; letter-spacing: 0.04em;
            transition: all 0.3s var(--ease-out); 
        }
        .btn-outline-teal:hover { background: rgba(46,211,198,0.1); transform: translateY(-2px); }

        .hero-visual { display: flex; justify-content: center; align-items: center; }
        .hero-visual img { 
            width: 100%; max-width: 480px; 
            filter: drop-shadow(0 0 32px rgba(249, 111, 110, 0.10));
            transition: max-width 0.3s var(--ease-out);
        }

        /* ========================================= 
           6. ARCHITECTURE (Pillars) 
           ========================================= */ 
        .section-title { text-align: center; margin-bottom: 0.75rem; font-size: clamp(1.75rem, 3vw, 2.25rem); }
        .section-subtitle { text-align: center; color: rgba(255,255,255,0.5); font-size: 0.95rem; }
        [data-theme="light"] .section-subtitle { color: #888; }

        .architecture-grid { 
            display: grid; grid-template-columns: repeat(4, 1fr); 
            gap: 1.25rem; margin-top: 3.5rem; 
        }
        .arch-card { 
            background: var(--surface-dark-2); 
            border: 1px solid rgba(255,255,255,0.05); 
            padding: 2.5rem 1.75rem; border-radius: 16px; transition: all 0.4s var(--ease-out);
            text-decoration: none; display: block;
        }
        .arch-card:hover { 
            transform: translateY(-8px); border-color: var(--teal); 
            box-shadow: 0 16px 40px rgba(0,0,0,0.25); 
        }
        .icon-circle { 
            width: 64px; height: 64px; border-radius: 50%; 
            border: 1px solid rgba(255,255,255,0.08); 
            display: flex; align-items: center; justify-content: center; 
            margin-bottom: 1.75rem; transition: all 0.4s var(--ease-out); overflow: hidden;
        }
        .arch-card:hover .icon-circle { border-color: var(--teal); transform: scale(1.05); }
        .icon-circle img { width: 100%; height: 100%; border-radius: 50%; object-fit: cover; }
        .arch-title { font-size: 1.5rem; margin-bottom: 0.4rem; }
        .arch-role { 
            font-family: var(--font-orchestrator);
            font-size: 0.7rem; text-transform: uppercase; color: var(--teal); 
            font-weight: 700; margin-bottom: 1rem; display: block; letter-spacing: 0.06em;
        }
        .arch-desc { font-size: 0.9rem; color: rgba(255,255,255,0.5); margin-bottom: 0; line-height: 1.6; }
        [data-theme="light"] .arch-desc { color: #888; }

        /* ========================================= 
           7. THE FLOW 
           ========================================= */ 
        .section-flow { background: var(--surface-dark); padding: 6rem 0; }
        [data-theme="light"] .section-flow { background: #FFF; }

        .flow-container { 
            display: flex; align-items: stretch; justify-content: center; 
            gap: 0; max-width: 1100px; margin: 3.5rem auto 0; 
        }
        .flow-node { 
            background: var(--surface-dark-2); border: 1px solid rgba(255,255,255,0.08); 
            padding: 1.5rem 1.25rem; flex: 1; max-width: 240px;
            border-radius: 12px; transition: all 0.4s var(--ease-out); position: relative; z-index: 2;
            opacity: 0.5;
        }
        [data-theme="light"] .flow-node { background: #F9F9F9; border-color: rgba(0,0,0,0.06); }
        .flow-node.active { 
            border-color: var(--teal); opacity: 1;
            box-shadow: 0 8px 24px rgba(0,0,0,0.15); 
        }
        .flow-connector { 
            flex-grow: 0; width: 48px; height: 1px; 
            background: rgba(255,255,255,0.08); align-self: center; 
            position: relative; flex-shrink: 0;
        }
        .signal-line { 
            position: absolute; inset: 0; width: 0; 
            background: var(--teal); opacity: 0; transition: width 0.8s ease; 
        }
        .flow-connector.active .signal-line { width: 100%; opacity: 1; }
        .flow-title { font-size: 1.2rem; margin-bottom: 0.2rem; }
        .flow-sub { 
            font-family: var(--font-orchestrator);
            font-size: 0.65rem; color: var(--coral); text-transform: uppercase; 
            font-weight: 700; margin-bottom: 0.5rem; display: block; letter-spacing: 0.08em;
        }

        /* ========================================= 
           8. FOOTER 
           ========================================= */ 
        .site-footer { 
            background-color: #050505; border-top: 1px solid rgba(255, 255, 255, 0.06); 
            padding: 5rem 0 0; color: #ffffff; position: relative; z-index: 10; 
        } 
        .footer-container { max-width: 1400px; margin: 0 auto; padding: 0 2rem; } 
        .footer-grid-layout { 
            display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr; 
            gap: 3rem; margin-bottom: 4rem; 
        } 
        .footer-brand-col { display: flex; flex-direction: column; gap: 1.25rem; } 
        .footer-logo-link { display: flex; align-items: center; gap: 0.75rem; text-decoration: none; }
        .footer-logo-img { height: 28px; width: auto; opacity: 0.8; } 
        .footer-brand-name { 
            font-family: var(--font-strategist); font-size: 1.25rem; font-weight: 700; 
            color: #ffffff; letter-spacing: 0.02em; text-transform: uppercase; 
        } 
        .footer-tagline { font-size: 0.875rem; line-height: 1.6; color: rgba(255, 255, 255, 0.45); margin: 0; } 
        .footer-social { display: flex; gap: 0.75rem; margin-top: 0.25rem; } 
        .social-icon { 
            display: flex; align-items: center; justify-content: center; 
            width: 36px; height: 36px; border-radius: 50%; 
            background: rgba(255, 255, 255, 0.04); color: rgba(255,255,255,0.5); 
            text-decoration: none; transition: all 0.2s ease; 
            border: 1px solid rgba(255, 255, 255, 0.08); 
        } 
        .social-icon:hover { background: var(--coral); border-color: var(--coral); color: #FFF; transform: translateY(-2px); } 
        .footer-nav-heading { 
            font-family: var(--font-strategist); font-size: 0.75rem; font-weight: 700; 
            text-transform: uppercase; letter-spacing: 0.1em; color: #555; margin-bottom: 1.25rem; 
        } 
        .footer-nav-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.625rem; } 
        .footer-nav-link { font-size: 0.875rem; color: rgba(255,255,255,0.5); font-weight: 400; text-decoration: none; transition: all 0.2s ease; } 
        .footer-nav-link:hover { color: #FFFFFF; } 
        
        .footer-status-card { 
            background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.06); 
            padding: 1.25rem; border-radius: 10px; text-align: left; 
        } 
        .status-indicator { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.875rem; } 
        .status-dot { width: 7px; height: 7px; background: var(--teal); border-radius: 50%; box-shadow: 0 0 8px rgba(46, 211, 198, 0.4); } 
        .status-text { font-family: var(--font-strategist); font-size: 0.8rem; font-weight: 700; text-transform: uppercase; color: #ffffff; letter-spacing: 0.04em; } 
        .status-description { font-size: 0.8rem; color: rgba(255, 255, 255, 0.45); line-height: 1.5; margin-bottom: 1rem; } 
        .status-cta { 
            display: inline-flex; align-items: center; gap: 0.5rem; color: #ffffff; 
            text-decoration: none; font-size: 0.8rem; font-weight: 500; 
            border-bottom: 1px solid rgba(255,255,255,0.2); padding-bottom: 2px; transition: all 0.2s ease; 
        } 
        .status-cta:hover { border-color: #ffffff; } 
        
        .footer-bottom-bar { 
            border-top: 1px solid rgba(255, 255, 255, 0.06); background: #050505; padding: 1.25rem 0; 
        } 
        .footer-bottom-container { 
            max-width: 1400px; margin: 0 auto; padding: 0 2rem; 
            display: flex; justify-content: space-between; align-items: center; 
        } 
        .footer-copyright { font-size: 0.75rem; color: rgba(255, 255, 255, 0.3); } 

        /* ========================================= 
           9. CUSTOM CURSOR (Desktop only) 
           ========================================= */ 
        #cursor-lens { 
            position: fixed; pointer-events: none; z-index: 99999; 
            width: 36px; height: 36px; transform: translate(-50%, -50%); 
            transition: all 0.08s ease; 
        }
        .lens-outer { 
            position: absolute; inset: 0; border: 1.5px solid var(--coral); 
            border-radius: 50%; opacity: 0.35; transition: all 0.2s ease; 
        }
        .lens-center { 
            position: absolute; top: 50%; left: 50%; width: 4px; height: 4px; 
            background: var(--teal); border-radius: 50%; transform: translate(-50%, -50%); 
        }
        body.cursor-hover .lens-outer { border-color: var(--teal); transform: scale(1.6); opacity: 0.5; }

        /* ========================================= 
           RESPONSIVE — TABLET (≤1024px) 
           ========================================= */ 
        @media (max-width: 1024px) {
            :root { --header-height: 64px; }
            body { cursor: auto; }
            #cursor-lens { display: none; }
            
            .desktop-nav { display: none; }
            .mobile-toggle { display: flex; }
            .nav-container { padding: 0 1.5rem; }

            .hero-section { min-height: auto; padding: 3.5rem 0 4rem; }
            .hero-grid { 
                grid-template-columns: 1fr; text-align: center; gap: 2.5rem;
                max-width: none;
            }
            .hero-content { 
                display: flex; flex-direction: column; align-items: center;
                max-width: none;
            }
            .hero-visual { order: -1; justify-content: center; }
            .hero-visual img { max-width: 420px; }
            .hero-headline { font-size: clamp(2.75rem, 7vw, 4rem); }
            .hero-subhead { 
                margin: 0 auto; max-width: 560px; text-align: center; 
                font-size: 1rem; line-height: 1.7;
            }
            .cta-buttons-hero { justify-content: center; }

            .section { padding: 5rem 0; }
            .architecture-grid { grid-template-columns: 1fr 1fr; gap: 1rem; margin-top: 2.5rem; }
            .arch-card { padding: 2rem 1.5rem; }
            .arch-card:hover { transform: none; }

            .section-flow { padding: 4rem 0; }
            .flow-container { 
                flex-wrap: nowrap; overflow-x: auto; 
                padding: 0 1.5rem; gap: 0; margin-top: 2.5rem;
                -webkit-overflow-scrolling: touch; scrollbar-width: none;
            }
            .flow-container::-webkit-scrollbar { display: none; }
            .flow-node { min-width: 180px; flex-shrink: 0; }
            .flow-connector { min-width: 24px; width: 24px; }

            .footer-grid-layout { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
            .footer-brand-col { grid-column: 1 / -1; text-align: center; align-items: center; }
            .footer-social { justify-content: center; }
            .footer-bottom-container { flex-direction: column; gap: 0.75rem; text-align: center; }
        }

        /* ========================================= 
           RESPONSIVE — MOBILE (≤767px) 
           ========================================= */ 
        @media (max-width: 767px) {
            :root { --header-height: 60px; }
            .container { padding: 0 1.25rem; }
            .nav-container { padding: 0 1rem; }
            .nav-logo img { height: 30px; }

            /* Hero — single column, centered, touch-friendly */
            .hero-section { padding: 2rem 0 3rem; }
            .hero-visual img { max-width: 260px; }
            .hero-headline { 
                font-size: clamp(2.25rem, 10vw, 3rem); 
                margin-bottom: 1rem; line-height: 0.9;
            }
            .hero-subhead { 
                font-size: 0.95rem; line-height: 1.65; 
                max-width: 400px;
            }
            .cta-buttons-hero { 
                flex-direction: column; align-items: center; 
                gap: 0.75rem; margin-top: 1.75rem; width: 100%;
            }
            .btn-coral, .btn-outline-teal { 
                width: 100%; max-width: 300px; text-align: center; 
                padding: 1rem 1.5rem; font-size: 0.85rem;
            }

            .section { padding: 3.5rem 0; }
            .architecture-grid { grid-template-columns: 1fr; gap: 0.875rem; margin-top: 2rem; }
            .arch-card { 
                padding: 1.75rem 1.5rem; 
                display: grid; grid-template-columns: 48px 1fr; gap: 1rem 1.25rem; align-items: start;
            }
            .icon-circle { width: 48px; height: 48px; margin-bottom: 0; grid-row: 1 / 3; }
            .arch-title { font-size: 1.25rem; align-self: end; margin-bottom: 0.15rem; }
            .arch-role { margin-bottom: 0; align-self: start; }
            .arch-desc { grid-column: 1 / -1; margin-top: 0.25rem; }

            .section-flow { padding: 3rem 0; }
            .flow-container { 
                flex-direction: column; align-items: center; 
                overflow-x: visible; padding: 0 1.25rem; margin-top: 2rem;
            }
            .flow-node { min-width: unset; width: 100%; max-width: 320px; }
            .flow-connector { width: 1px; min-width: 1px; height: 32px; align-self: center; }
            .signal-line { width: 1px !important; height: 0; transition: height 0.8s ease; }
            .flow-connector.active .signal-line { height: 100%; width: 1px !important; }

            .footer-grid-layout { grid-template-columns: 1fr; gap: 2rem; text-align: center; }
            .footer-brand-col { align-items: center; }
            .footer-social { justify-content: center; }
            .footer-nav-list { align-items: center; }
            .footer-status-card { text-align: center; }
            .status-indicator { justify-content: center; }

            .mobile-drawer { max-width: 100%; }
            .drawer-hub-grid { grid-template-columns: 1fr 1fr; gap: 0.5rem; }
        }

        /* ========================================= 
           RESPONSIVE — SMALL MOBILE (≤380px) 
           ========================================= */ 
        @media (max-width: 380px) {
            .drawer-hub-grid { grid-template-columns: 1fr; }
            .hero-headline { font-size: 2rem; }
            .hero-visual img { max-width: 220px; }
            .btn-coral, .btn-outline-teal { max-width: 100%; }
        }

        /* ========================================= 
           ANIMATIONS 
           ========================================= */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-in {
            opacity: 0; animation: fadeInUp 0.6s var(--ease-out) forwards;
        }
        .delay-1 { animation-delay: 0.1s; }
        .delay-2 { animation-delay: 0.2s; }
        .delay-3 { animation-delay: 0.3s; }
    </style>
</head>
<body>

    <!-- Custom Cursor (Desktop only) -->
    <div id="cursor-lens">
        <div class="lens-outer"></div>
        <div class="lens-center"></div>
    </div>

    <!-- ===================== HEADER ===================== -->
    <header class="site-header">
        <div class="nav-container">
            <a href="/index.html" class="nav-logo">
                <img src="/assets/images/AUg_logo_White.png" alt="Luis Gilberto">
            </a>

            <nav class="desktop-nav">
                <!-- Portfolio Mega Dropdown -->
                <div class="nav-item has-dropdown">
                    <button class="nav-link">Portfolio <i class="fas fa-chevron-down"></i></button>
                    <div class="mega-dropdown">
                        <div class="mega-grid">
                            <a href="/index.html" class="mega-link">
                                <div class="mega-icon"><i class="fas fa-home"></i></div>
                                <div class="mega-text"><span class="mega-title">Home</span><span class="mega-desc">Return to the overview.</span></div>
                            </a>
                            <a href="/timeline.html" class="mega-link">
                                <div class="mega-icon"><i class="fas fa-history"></i></div>
                                <div class="mega-text"><span class="mega-title">Timeline</span><span class="mega-desc">15+ years of milestones.</span></div>
                            </a>
                            <a href="/myexperience.html" class="mega-link">
                                <div class="mega-icon"><i class="fas fa-briefcase"></i></div>
                                <div class="mega-text"><span class="mega-title">Experience</span><span class="mega-desc">Case studies & deep dives.</span></div>
                            </a>
                            <a href="/about.html" class="mega-link">
                                <div class="mega-icon"><i class="fas fa-user"></i></div>
                                <div class="mega-text"><span class="mega-title">About Me</span><span class="mega-desc">The human behind the systems.</span></div>
                            </a>
                        </div>
                    </div>
                </div>

                <a href="/insights/" class="nav-link">Insights</a>

                <!-- THE HUB Mega Dropdown -->
                <div class="nav-item has-dropdown">
                    <button class="nav-link active">The Hub <i class="fas fa-chevron-down"></i></button>
                    <div class="hub-mega-dropdown">
                        <div class="hub-mega-grid">
                            <a href="/TheHub/index.html" class="hub-mega-link active">
                                <div class="hub-mega-icon"><i class="fas fa-house"></i></div>
                                <div class="hub-mega-text">
                                    <span class="hub-mega-title">Hub Home</span>
                                    <span class="hub-mega-desc">System overview and entry point.</span>
                                </div>
                            </a>
                            <a href="/TheHub/strategy-iq.html" class="hub-mega-link">
                                <div class="hub-mega-icon"><i class="fas fa-chart-line"></i></div>
                                <div class="hub-mega-text">
                                    <span class="hub-mega-title">StrategyIQ</span>
                                    <span class="hub-mega-desc">Benchmark maturity and define the roadmap.</span>
                                </div>
                            </a>
                            <a href="/TheHub/advisory/" class="hub-mega-link">
                                <div class="hub-mega-icon"><i class="fas fa-compass"></i></div>
                                <div class="hub-mega-text">
                                    <span class="hub-mega-title">Advisory</span>
                                    <span class="hub-mega-desc">Leadership alignment and decision support.</span>
                                </div>
                            </a>
                            <a href="/TheHub/studio.html" class="hub-mega-link">
                                <div class="hub-mega-icon"><i class="fas fa-palette"></i></div>
                                <div class="hub-mega-text">
                                    <span class="hub-mega-title">The Studio</span>
                                    <span class="hub-mega-desc">Creative production and execution.</span>
                                </div>
                            </a>
                        </div>
                        <div class="hub-mega-footer">
                            <a href="https://portal.luis-gilberto.com" class="hub-mega-portal-link">
                                Enter The Portal <i class="fas fa-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </div>

                <a href="https://portal.luis-gilberto.com" class="nav-link">The Portal</a>
            </nav>

            <div class="nav-actions">
                <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">
                    <i class="fa-regular fa-moon"></i>
                </button>
                <button class="mobile-toggle" id="mobileMenuToggle" aria-label="Open menu">
                    <i class="fa-solid fa-bars"></i>
                </button>
            </div>
        </div>
    </header>

    <!-- ===================== MOBILE DRAWER ===================== -->
    <div class="drawer-overlay" id="drawerOverlay"></div>
    <div class="mobile-drawer" id="mobileDrawer">
        <div class="drawer-header">
            <img src="/assets/images/Logomark_White_a.png" alt="Luis Gilberto">
            <button class="drawer-close" id="drawerClose" aria-label="Close menu">
                <i class="fa-solid fa-xmark"></i>
            </button>
        </div>

        <div class="drawer-content">
            <!-- Hub Section — Featured -->
            <div class="drawer-hub-section">
                <div class="drawer-section-label">
                    <i class="fa-solid fa-diagram-project"></i>
                    The Hub
                </div>
                <div class="drawer-hub-grid">
                    <a href="/TheHub/index.html" class="drawer-hub-card active">
                        <div class="drawer-hub-card-icon"><i class="fa-solid fa-house"></i></div>
                        <span class="drawer-hub-card-title">Home</span>
                        <span class="drawer-hub-card-sub">System overview</span>
                    </a>
                    <a href="/TheHub/strategy-iq.html" class="drawer-hub-card">
                        <div class="drawer-hub-card-icon"><i class="fa-solid fa-chart-line"></i></div>
                        <span class="drawer-hub-card-title">StrategyIQ™</span>
                        <span class="drawer-hub-card-sub">Diagnostic engine</span>
                    </a>
                    <a href="/TheHub/advisory/" class="drawer-hub-card">
                        <div class="drawer-hub-card-icon"><i class="fa-solid fa-compass"></i></div>
                        <span class="drawer-hub-card-title">Advisory</span>
                        <span class="drawer-hub-card-sub">Strategic partnership</span>
                    </a>
                    <a href="/TheHub/studio.html" class="drawer-hub-card">
                        <div class="drawer-hub-card-icon"><i class="fa-solid fa-palette"></i></div>
                        <span class="drawer-hub-card-title">The Studio</span>
                        <span class="drawer-hub-card-sub">Creative production</span>
                    </a>
                </div>
            </div>

            <!-- Global Nav Section -->
            <div class="drawer-global-section">
                <div class="drawer-section-label">Ecosystem</div>
                <a href="/index.html" class="drawer-global-link">
                    <div class="drawer-global-icon"><i class="fa-solid fa-layer-group"></i></div>
                    <span class="drawer-global-text">Portfolio</span>
                </a>
                <a href="/insights/" class="drawer-global-link">
                    <div class="drawer-global-icon"><i class="fa-solid fa-lightbulb"></i></div>
                    <span class="drawer-global-text">Insights</span>
                </a>
                <a href="/TheHub/" class="drawer-global-link active">
                    <div class="drawer-global-icon"><i class="fa-solid fa-diagram-project"></i></div>
                    <span class="drawer-global-text">The Hub</span>
                </a>
                <a href="https://portal.luis-gilberto.com" class="drawer-global-link">
                    <div class="drawer-global-icon"><i class="fa-solid fa-arrow-up-right-from-square"></i></div>
                    <span class="drawer-global-text">The Portal</span>
                </a>
            </div>
        </div>

        <div class="drawer-footer">
            <div class="drawer-footer-status">
                <span class="drawer-status-dot"></span>
                Accepting Projects
            </div>
            <button class="drawer-theme-toggle" id="drawerThemeToggle" aria-label="Toggle theme">
                <i class="fa-regular fa-moon"></i>
            </button>
        </div>
    </div>

    <!-- ===================== MAIN CONTENT ===================== -->
    <main>
        <!-- HERO -->
        <section class="hero-section container">
            <div class="hero-grid">
                <div class="hero-content">
                    <h1 class="hero-headline animate-in">STRATEGY,<br>OPERATIONALIZED.</h1>
                    <p class="hero-subhead animate-in delay-1">The Hub is the strategic system I use to create meaningful progress. It reveals <em>what's slowing you down</em>, clarifies what matters most, and builds a plan designed to deliver <em>real outcomes</em>.</p>
                    
                    <div class="cta-buttons-hero animate-in delay-2">
                        <a href="#how-it-works" class="btn-coral">See the Flow</a>
                        <a href="#pillars" class="btn-outline-teal">Explore Pillars</a>
                    </div>
                </div>
                <div class="hero-visual animate-in delay-3">
                    <img src="../assets/images/portal/StrategyIQ_Hero_3D.png" alt="StrategyIQ 3D Visualization">
                </div>
            </div>
        </section>

        <!-- PILLARS -->
        <section class="section container" id="pillars">
            <h2 class="section-title">The Architecture</h2>
            <p class="section-subtitle">Four arms. One integrated system.</p>
            
            <div class="architecture-grid">
                <a href="/TheHub/strategy-iq.html" class="arch-card">
                    <div class="icon-circle"><img src="../assets/images/icons/strategyiq-icon-3d.png" alt="StrategyIQ"></div>
                    <h3 class="arch-title">StrategyIQ™</h3>
                    <span class="arch-role">Strategic Intelligence</span>
                    <p class="arch-desc">The diagnostic engine. We benchmark your maturity and define the roadmap before building a single asset.</p>
                </a>
                <a href="/TheHub/advisory/" class="arch-card">
                    <div class="icon-circle"><img src="../assets/images/Luis_portrait_circle.png" alt="Luis Gilberto"></div>
                    <h3 class="arch-title">Advisory</h3>
                    <span class="arch-role" style="color:var(--coral);">Partnership</span>
                    <p class="arch-desc">Direct access to Luis Gilberto for leadership alignment, narrative architecture, and decision support.</p>
                </a>
                <a href="/TheHub/studio.html" class="arch-card">
                    <div class="icon-circle"><img src="../assets/images/Teal_LG-3D.png" alt="Studio"></div>
                    <h3 class="arch-title">The Studio</h3>
                    <span class="arch-role">Creative Production</span>
                    <p class="arch-desc">High-fidelity execution. From brand identity to full-stack web development and campaign assets.</p>
                </a>
                <a href="https://portal.luis-gilberto.com" class="arch-card">
                    <div class="icon-circle"><img src="../assets/images/portal/The_Portal_round.png" alt="The Portal"></div>
                    <h3 class="arch-title">The Portal</h3>
                    <span class="arch-role" style="color:var(--coral);">Command Center</span>
                    <p class="arch-desc">Your secure environment for project management, asset approvals, and timeline tracking.</p>
                </a>
            </div>
        </section>

        <!-- FLOW -->
        <section class="section-flow" id="how-it-works">
            <div class="container">
                <h2 class="section-title" style="margin-bottom: 0;">The Flow</h2>
                <div class="flow-container" id="flow-system">
                    <div class="flow-node" data-step="1">
                        <span class="flow-sub">Step 1</span>
                        <h3 class="flow-title">Diagnosis</h3>
                    </div>
                    <div class="flow-connector"><div class="signal-line"></div></div>
                    <div class="flow-node" data-step="2">
                        <span class="flow-sub">Step 2</span>
                        <h3 class="flow-title">Strategy</h3>
                    </div>
                    <div class="flow-connector"><div class="signal-line"></div></div>
                    <div class="flow-node" data-step="3">
                        <span class="flow-sub">Step 3</span>
                        <h3 class="flow-title">Execution</h3>
                    </div>
                    <div class="flow-connector"><div class="signal-line"></div></div>
                    <div class="flow-node" data-step="4">
                        <span class="flow-sub">Living In</span>
                        <h3 class="flow-title">The Portal</h3>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- ===================== FOOTER ===================== -->
    <footer class="site-footer">
        <div class="footer-container">
            <div class="footer-grid-layout">
                <div class="footer-brand-col">
                    <a href="/index.html" class="footer-logo-link">
                        <img src="/assets/images/Logomark_White_a.png" class="footer-logo-img" alt="Luis Gilberto">
                        <span class="footer-brand-name">Luis Gilberto</span>
                    </a>
                    <p class="footer-tagline">Making technology feel human through clarity, beautiful execution, and systems that scale.</p>
                    <div class="footer-social">
                        <a href="https://www.linkedin.com/in/luisgilberto00" target="_blank" class="social-icon" aria-label="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
                        <a href="/cdn-cgi/l/email-protection#a9c1ccc5c5c6e9c5dcc0da84cec0c5cbccdbddc687cac6c4" class="social-icon" aria-label="Email"><i class="far fa-envelope"></i></a>
                    </div>
                </div>
                <nav class="footer-nav-col">
                    <h3 class="footer-nav-heading">Ecosystem</h3>
                    <ul class="footer-nav-list">
                        <li><a href="/index.html" class="footer-nav-link">Portfolio</a></li>
                        <li><a href="/insights/index.html" class="footer-nav-link">Insights</a></li>
                        <li><a href="/TheHub/index.html" class="footer-nav-link">The Hub</a></li>
                        <li><a href="https://portal.luis-gilberto.com" target="_blank" class="footer-nav-link">The Portal</a></li>
                    </ul>
                </nav>
                <nav class="footer-nav-col">
                    <h3 class="footer-nav-heading">Connect</h3>
                    <ul class="footer-nav-list">
                        <li><a href="/contact.html" class="footer-nav-link">Contact</a></li>
                        <li><a href="/about.html" class="footer-nav-link">About Me</a></li>
                        <li><a href="https://www.linkedin.com/in/luisgilberto00" target="_blank" class="footer-nav-link">LinkedIn</a></li>
                    </ul>
                </nav>
                <div class="footer-status-col">
                    <h3 class="footer-nav-heading">Status</h3>
                    <div class="footer-status-card">
                        <div class="status-indicator">
                            <span class="status-dot"></span>
                            <span class="status-text">Accepting Projects</span>
                        </div>
                        <p class="status-description">Currently available for strategic consulting and creative direction.</p>
                        <a href="/contact.html" class="status-cta">Start a Conversation <i class="fas fa-arrow-right" style="font-size:0.7rem;"></i></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="footer-bottom-bar">
            <div class="footer-bottom-container">
                <p class="footer-copyright">&copy; <span id="currentYear"></span> Luis Gilberto. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <!-- ===================== SCRIPTS ===================== -->
    <script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>
        (function() {
            const html = document.documentElement;

            // ---- Theme Management ----
            const savedTheme = localStorage.getItem('theme') || 'dark';
            html.setAttribute('data-theme', savedTheme);
            updateThemeIcons(savedTheme);

            function toggleTheme() {
                const current = html.getAttribute('data-theme');
                const next = current === 'dark' ? 'light' : 'dark';
                html.setAttribute('data-theme', next);
                localStorage.setItem('theme', next);
                updateThemeIcons(next);
            }

            function updateThemeIcons(theme) {
                document.querySelectorAll('#themeToggle i, #drawerThemeToggle i').forEach(icon => {
                    icon.className = theme === 'dark' ? 'fa-regular fa-moon' : 'fa-regular fa-sun';
                });
            }

            document.getElementById('themeToggle')?.addEventListener('click', toggleTheme);
            document.getElementById('drawerThemeToggle')?.addEventListener('click', toggleTheme);

            // ---- Mobile Drawer ----
            const mobileToggle = document.getElementById('mobileMenuToggle');
            const mobileDrawer = document.getElementById('mobileDrawer');
            const drawerOverlay = document.getElementById('drawerOverlay');
            const drawerClose = document.getElementById('drawerClose');

            function openDrawer() {
                mobileDrawer.classList.add('active');
                drawerOverlay.classList.add('active');
                document.body.style.overflow = 'hidden';
                mobileToggle.querySelector('i').className = 'fa-solid fa-xmark';
            }

            function closeDrawer() {
                mobileDrawer.classList.remove('active');
                drawerOverlay.classList.remove('active');
                document.body.style.overflow = '';
                mobileToggle.querySelector('i').className = 'fa-solid fa-bars';
            }

            mobileToggle?.addEventListener('click', () => {
                mobileDrawer.classList.contains('active') ? closeDrawer() : openDrawer();
            });
            drawerClose?.addEventListener('click', closeDrawer);
            drawerOverlay?.addEventListener('click', closeDrawer);
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && mobileDrawer.classList.contains('active')) closeDrawer();
            });

            // ---- Custom Cursor (Desktop only) ----
            const cursor = document.getElementById('cursor-lens');
            if (cursor && window.innerWidth > 1024) {
                document.body.style.cursor = 'none';
                let mx = 0, my = 0, cx = 0, cy = 0;
                document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
                function anim() { 
                    cx += (mx - cx) * 0.15; cy += (my - cy) * 0.15; 
                    cursor.style.left = cx + 'px'; cursor.style.top = cy + 'px'; 
                    requestAnimationFrame(anim); 
                }
                anim();
                document.querySelectorAll('a, button, .arch-card').forEach(el => {
                    el.addEventListener('mouseenter', () => document.body.classList.add('cursor-hover'));
                    el.addEventListener('mouseleave', () => document.body.classList.remove('cursor-hover'));
                });
            }

            // ---- Flow Animation Observer ----
            const obs = new IntersectionObserver(entries => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const nodes = entry.target.querySelectorAll('.flow-node');
                        const conns = entry.target.querySelectorAll('.flow-connector');
                        nodes.forEach((n, i) => setTimeout(() => n.classList.add('active'), i * 600));
                        conns.forEach((c, i) => setTimeout(() => c.classList.add('active'), (i * 600) + 300));
                    }
                });
            }, { threshold: 0.2 });
            const flow = document.getElementById('flow-system');
            if (flow) obs.observe(flow);

            // ---- Footer Year ----
            const yearEl = d