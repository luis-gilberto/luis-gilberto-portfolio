<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Start the Intake — The Hub</title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Big+Shoulders+Display:wght@700;800;900&family=Cormorant+Garamond:ital,wght@0,400;0,500;1,400;1,500&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --coral:        #F96F6E;
      --teal:         #4BADA8;
      --obsidian:     #0E0C0A;
      --obsidian-2:   #141210;
      --obsidian-3:   #1A1816;
      --cloud:        #F5F0EB;
      --text-primary: #F0EBE5;
      --text-muted:   rgba(240,235,229,0.55);
      --border:       rgba(240,235,229,0.08);
      --border-coral: rgba(249,111,110,0.35);
      --font-display: 'Big Shoulders Display', sans-serif;
      --font-serif:   'Cormorant Garamond', Georgia, serif;
      --font-ui:      'Inter', sans-serif;
    }

    html[data-theme="light"] {
      --obsidian:     #F5F0EB;
      --obsidian-2:   #EDE8E2;
      --obsidian-3:   #E8E2DC;
      --text-primary: #1A1714;
      --text-muted:   rgba(26,23,20,0.55);
      --border:       rgba(26,23,20,0.10);
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }

    body {
      background: var(--obsidian);
      color: var(--text-primary);
      font-family: var(--font-ui);
      -webkit-font-smoothing: antialiased;
      min-height: 100vh;
    }

    #site-nav { position: relative; z-index: 100; }

    .intake-page {
      max-width: 680px;
      margin: 0 auto;
      padding: 80px 40px 140px;
    }

    .intake-breadcrumb {
      display: flex;
      align-items: center;
      gap: 8px;
      font-family: var(--font-ui);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 56px;
    }

    .intake-breadcrumb a {
      color: var(--text-muted);
      text-decoration: none;
      transition: color 0.2s;
    }

    .intake-breadcrumb a:hover { color: var(--coral); }
    .intake-breadcrumb-sep { opacity: 0.4; }

    .intake-time-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-family: var(--font-ui);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--teal);
      background: rgba(75,173,168,0.08);
      border: 1px solid rgba(75,173,168,0.20);
      border-radius: 100px;
      padding: 5px 12px;
      margin-bottom: 32px;
    }

    .intake-time-dot {
      width: 5px;
      height: 5px;
      border-radius: 50%;
      background: var(--teal);
      animation: pulse-dot 2s ease-in-out infinite;
    }

    @keyframes pulse-dot {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.3; }
    }

    .intake-eyebrow {
      font-family: var(--font-ui);
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--teal);
      margin-bottom: 20px;
    }

    .intake-headline {
      font-family: var(--font-serif);
      font-size: clamp(36px, 5vw, 52px);
      font-weight: 400;
      line-height: 1.1;
      color: var(--text-primary);
      margin-bottom: 20px;
    }

    .intake-subhead {
      font-family: var(--font-ui);
      font-size: 15px;
      font-weight: 300;
      line-height: 1.75;
      color: var(--text-muted);
      margin-bottom: 56px;
      max-width: 520px;
    }

    .intake-rule {
      border: none;
      height: 1px;
      background: var(--border);
      margin-bottom: 48px;
    }

    .intake-section-label {
      font-family: var(--font-ui);
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--coral);
      margin-bottom: 16px;
    }

    .intake-why {
      font-family: var(--font-ui);
      font-size: 15px;
      font-weight: 300;
      line-height: 1.8;
      color: var(--text-muted);
      margin-bottom: 48px;
    }

    .intake-why strong {
      color: var(--text-primary);
      font-weight: 500;
    }

    .intake-covers {
      margin-bottom: 48px;
    }

    .intake-covers-list {
      list-style: none;
      padding: 0;
      border-top: 1px solid var(--border);
    }

    .intake-covers-list li {
      display: flex;
      align-items: baseline;
      gap: 14px;
      padding: 14px 0;
      border-bottom: 1px solid var(--border);
      font-family: var(--font-ui);
      font-size: 14px;
      font-weight: 300;
      line-height: 1.6;
      color: var(--text-muted);
    }

    .intake-covers-list li::before {
      content: '';
      width: 5px;
      height: 5px;
      border-radius: 50%;
      background: var(--teal);
      flex-shrink: 0;
      margin-top: 7px;
    }

    .intake-reassurance {
      background: var(--obsidian-3);
      border: 1px solid var(--border);
      border-left: 3px solid var(--teal);
      border-radius: 0 8px 8px 0;
      padding: 20px 24px;
      margin-bottom: 56px;
      font-family: var(--font-ui);
      font-size: 14px;
      font-weight: 300;
      line-height: 1.75;
      color: var(--text-muted);
    }

    .intake-reassurance strong {
      color: var(--text-primary);
      font-weight: 500;
    }

    .intake-cta-wrap {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }

    .intake-btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      font-family: var(--font-ui);
      font-size: 13px;
      font-weight: 600;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: var(--obsidian);
      background: var(--coral);
      text-decoration: none;
      padding: 15px 28px;
      border-radius: 100px;
      transition: opacity 0.2s, gap 0.2s;
    }

    .intake-btn-primary:hover { opacity: 0.88; gap: 14px; }

    .intake-btn-back {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: var(--font-ui);
      font-size: 12px;
      font-weight: 400;
      color: var(--text-muted);
      text-decoration: none;
      letter-spacing: 0.02em;
      transition: color 0.2s;
    }

    .intake-btn-back:hover { color: var(--text-primary); }

    @media (max-width: 640px) {
      .intake-page { padding: 60px 24px 100px; }
      .intake-headline { font-size: 36px; }
      .intake-btn-primary { width: 100%; justify-content: center; }
    }
  </style>
</head>
<body>

  <div id="site-nav" data-base="../../" data-active="hub"></div>

  <main class="intake-page">

    <nav class="intake-breadcrumb" aria-label="Breadcrumb">
      <a href="/TheHub/">The Hub</a>
      <span class="intake-breadcrumb-sep">/</span>
      <span>Intake</span>
    </nav>

    <div class="intake-time-badge">
      <span class="intake-time-dot"></span>
      10 to 15 minutes
    </div>

    <p class="intake-eyebrow">Step 1 of the engagement</p>

    <h1 class="intake-headline">Before strategy begins,<br>we get context right.</h1>

    <p class="intake-subhead">The intake takes about 10 to 15 minutes. It gives StrategyIQ the context needed to prepare your diagnostic before the strategy session — so the first conversation is already informed.</p>

    <hr class="intake-rule">

    <div class="intake-why">
      <p class="intake-section-label">Why this exists</p>
      <p><strong>Most strategy conversations start cold. Yours won't.</strong> The intake captures your organization context, the problem you're solving, and the constraints you're working within.</p>
    </div>

    <div class="intake-covers">
      <p class="intake-section-label">What this covers</p>
      <ul class="intake-covers-list">
        <li>Your organization and current stage</li>
        <li>The problem or opportunity you're trying to solve</li>
        <li>Where things are getting stuck</li>
        <li>Timeline, urgency, and priorities</li>
        <li>What success looks like for you</li>
      </ul>
    </div>

    <div class="intake-reassurance">
      <strong>There are no right answers.</strong> This isn't a test — it's context. The more honest you are, the more useful the diagnostic becomes.
    </div>

    <div class="intake-cta-wrap">
      <a href="https://app.fillout.com/t/xwludkptld" class="intake-btn-primary" target="_blank" rel="noopener">
        Open Intake
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M3 8H13M13 8L9 4M13 8L9 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
      <a href="/TheHub/" class="intake-btn-back">
        <svg width="14" height="14" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M13 8H3M3 8L7 4M3 8L7 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        Back to The Hub
      </a>
    </div>

  </main>

  <script src="/assets/js/nav-component.js"></script>

</body>
</html>