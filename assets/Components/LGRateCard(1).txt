Got it. Here’s a **dark-first, glass** version—consumer-first + SMB/startup fluent, your coral accents, and **no Genspark badge scripts**. Drop this in as-is.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Engagement Toolkit | Luis Gilberto</title>

  <!-- Tailwind (layout utilities) -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">

  <!-- Brand Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=General+Sans:wght@400;500;600;700;800&family=Recoleta:wght@600;700&family=Editorial+New:opsz@6..72&display=swap" rel="stylesheet"/>

  <style>
    /* ====== Luis Gilberto — Dark-First Glass Theme ====== */
    :root{
      --coral:#F96F6E;
      --bg:#0E0F12;                /* page background */
      --bg-2:#121318;              /* subtle contrast */
      --card:#15171D;              /* card surface */
      --line:rgba(255,255,255,.10);
      --ink:#F2F3F5;               /* primary text */
      --ink-2:#C9CBD1;             /* secondary text */
      --shadow:0 8px 24px rgba(0,0,0,.45);
      --shadow-xl:0 40px 80px rgba(0,0,0,.55);

      --font-ui:'General Sans',system-ui,-apple-system,Segoe UI,Inter,Arial,sans-serif;
      --font-display:'Recoleta','Editorial New',Georgia,serif;
      --font-editorial:'Editorial New','Recoleta',Georgia,serif;
    }

    *{ box-sizing:border-box; }
    body{
      font-family:var(--font-ui);
      background:var(--bg);
      color:var(--ink);
      line-height:1.6;
      overflow-x:hidden;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    h1,h2,h3{ color:rgba(255,255,255,.96); }
    p, li, small{ color:var(--ink-2); }

    .font-display{ font-family:var(--font-display); letter-spacing:-.01em; }
    .font-editorial{ font-family:var(--font-editorial); }

    .glass{
      background:linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
      border:1px solid var(--line);
      backdrop-filter: blur(10px);
    }
    .bg-coral{ background:var(--coral); }
    .rule-coral{ height:2px; width:96px; background:var(--coral); border-radius:2px; }

    .hover-lift{ transition: transform .25s ease, box-shadow .25s ease; }
    .hover-lift:hover{ transform: translateY(-4px); box-shadow: var(--shadow-xl); }

    /* Tags */
    .skill-tag{
      background:rgba(255,255,255,.06);
      border:1px solid rgba(255,255,255,.12);
      color:var(--ink);
      padding:.5rem 1rem;
      border-radius:999px;
      font-size:.875rem;
      font-weight:600;
      display:inline-block;
      margin:.25rem;
    }

    /* Cards */
    .stats-card{
      position:relative; border-radius:1rem; padding:1.5rem;
      background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.03));
      border:1px solid var(--line); overflow:hidden;
    }
    .stats-card::after{
      content:''; position:absolute; left:0; right:0; bottom:0; height:3px; background:var(--coral);
    }
    .testimonial-card{
      position:relative; border-radius:1rem; padding:1.5rem;
      background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.03));
      border:1px solid var(--line);
    }

    /* Packages */
    .package-row{
      transition:all .3s ease;
      border-left:4px solid transparent;
      background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.03));
      border:1px solid var(--line);
      border-radius:1rem;
    }
    .package-row:hover{
      border-left-color:var(--coral);
      background:linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.04));
      transform: translateX(4px);
    }

    /* Animations */
    .animate-fade-in{ animation:fadeIn .6s ease-out; }
    .animate-slide-up{ animation:slideUp .8s ease-out; }
    .animate-pulse-subtle{ animation:pulseSubtle 2.2s infinite; }
    @keyframes fadeIn{ from{opacity:0;} to{opacity:1;} }
    @keyframes slideUp{ from{opacity:0; transform:translateY(26px);} to{opacity:1; transform:translateY(0);} }
    @keyframes pulseSubtle{ 0%,100%{opacity:1;} 50%{opacity:.88;} }

    /* Config panel (glass) */
    .config-panel{
      background:linear-gradient(135deg, rgba(21,23,29,.9), rgba(21,23,29,.7));
      border:1px solid var(--line);
      backdrop-filter: blur(14px);
      color:var(--ink-2);
    }
    .config-panel strong{ color:var(--ink); }

    /* Main card */
    .main-card{ box-shadow: var(--shadow-xl); }

    /* Print */
    @media print{
      .no-print{ display:none !important; }
      body{ background:#fff; color:#000; }
      .glass, .stats-card, .testimonial-card, .package-row{ background:#fff !important; border:1px solid #ddd !important; }
      .rule-coral{ background:#000 !important; }
      a{ color:#000 !important; text-decoration:underline; }
    }
  </style>
</head>

<body class="min-h-screen">
  <!-- Subtle background pattern -->
  <div class="fixed inset-0 pointer-events-none" style="opacity:.05; background-image: radial-gradient(circle at 1px 1px, #fff 1px, transparent 0); background-size: 18px 18px;"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 py-8">
    <!-- Top Bar -->
    <div class="flex items-center justify-between mb-8 animate-fade-in">
      <div class="flex items-center gap-4">
        <div class="bg-coral text-white px-4 py-2 rounded-full text-sm font-medium flex items-center gap-2">
          <i class="fas fa-lock"></i>
          Private • By Invitation
        </div>
      </div>
      <div class="flex items-center gap-4 no-print">
        <!-- Swap to your preferred lockup/icon -->
        <img src="https://luis-gilberto.com/wp-content/uploads/2025/05/Preferred-version.png" alt="Luis Gilberto" class="h-8 opacity-95" />
        <button class="glass text-white px-4 py-2 rounded-lg hover-lift transition-colors flex items-center gap-2" id="copyLink">
          <i class="fas fa-link opacity-90"></i><span>Copy Share Link</span>
        </button>
        <button class="bg-coral text-white px-4 py-2 rounded-lg hover:opacity-90 transition-opacity flex items-center gap-2" onclick="window.print()">
          <i class="fas fa-download"></i><span>Download PDF</span>
        </button>
      </div>
    </div>

    <!-- Config Panel -->
    <div class="config-panel rounded-2xl p-6 mb-8 animate-slide-up no-print" id="configBar">
      <div class="flex flex-wrap items-center justify-between gap-6">
        <div class="flex flex-wrap items-center gap-6">
          <div>
            <strong class="mb-2 block">Show sections:</strong>
            <div class="flex flex-wrap gap-4">
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox" data-toggle="sec-intro" checked class="rounded border-gray-600 focus:ring-indigo-500">
                <span class="text-sm">Intro</span>
              </label>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox" data-toggle="sec-offer" checked class="rounded border-gray-600 focus:ring-indigo-500">
                <span class="text-sm">What You Get</span>
              </label>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox" data-toggle="sec-trust" checked class="rounded border-gray-600 focus:ring-indigo-500">
                <span class="text-sm">Consumer, SMB & Startup Teams</span>
              </label>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox" data-toggle="sec-models" checked class="rounded border-gray-600 focus:ring-indigo-500">
                <span class="text-sm">Ways to Work Together</span>
              </label>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox" data-toggle="sec-footer" checked class="rounded border-gray-600 focus:ring-indigo-500">
                <span class="text-sm">Footer</span>
              </label>
            </div>
          </div>
        </div>
        <div>
          <strong class="mb-2 block">Packages:</strong>
          <div class="flex flex-wrap gap-4">
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="checkbox" data-package="quick" checked class="rounded border-gray-600 focus:ring-indigo-500">
              <span class="text-sm">Quick-Start</span>
            </label>
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="checkbox" data-package="fractional" checked class="rounded border-gray-600 focus:ring-indigo-500">
              <span class="text-sm">Fractional</span>
            </label>
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="checkbox" data-package="launch" checked class="rounded border-gray-600 focus:ring-indigo-500">
              <span class="text-sm">Launch</span>
            </label>
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="checkbox" data-package="ally" checked class="rounded border-gray-600 focus:ring-indigo-500">
              <span class="text-sm">Founder's Ally</span>
            </label>
          </div>
        </div>
      </div>
      <div class="mt-4 text-sm flex items-center gap-2" style="color:var(--ink-2)">
        <i class="fas fa-lightbulb" style="color:#FFD766"></i>
        Tip: use the “Copy Share Link” button to generate a URL with your current selections.
      </div>
    </div>

    <!-- Main Card -->
    <section class="glass rounded-3xl overflow-hidden animate-slide-up main-card">
      <div class="grid lg:grid-cols-3 gap-0">
        <!-- Left Column - 2/3 -->
        <div class="lg:col-span-2 p-8 lg:p-12">
          <!-- Intro -->
          <div id="sec-intro" class="mb-12">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-12 h-12 bg-coral rounded-full flex items-center justify-center animate-pulse-subtle">
                <i class="fas fa-user-tie text-white text-lg"></i>
              </div>
              <div class="text-sm font-semibold uppercase tracking-wider" style="color:var(--ink-2)">Luis Gilberto</div>
            </div>

            <h1 class="font-display text-5xl lg:text-6xl font-bold mb-4 leading-tight">
              Integrated Marketing Partner
            </h1>
            <p class="text-xs tracking-widest uppercase mb-6" style="letter-spacing:.18em; color:var(--ink-2)">
              Consumer campaigns • SMB & startup GTM
            </p>
            <div class="rule-coral mb-8"></div>

            <div class="flex flex-wrap items-center gap-4 mb-8">
              <div class="skill-tag">Strategy</div>
              <div class="skill-tag">Storytelling</div>
              <div class="skill-tag">Campaigns</div>
              <div class="skill-tag">SMB & Startups</div>
            </div>

            <p class="text-xl leading-relaxed mb-4">
              My home turf is <strong style="color:var(--coral)">consumer</strong>: 15+ years shaping campaigns across Edge, Microsoft 365, Surface, Teams, and Copilot—work built to capture attention and land a feeling.
            </p>
            <p class="leading-relaxed">
              I also led <strong style="color:var(--coral)">Office SMB</strong> integrated marketing. Today I translate big-brand rigor for startups, founders, and small teams—clear positioning, testable narratives, and creative that ships.
            </p>
          </div>

          <!-- What You Get -->
          <div id="sec-offer" class="mb-12">
            <h2 class="font-display text-3xl font-semibold mb-8 flex items-center gap-3">
              <div class="w-8 h-8 rounded-lg glass flex items-center justify-center">
                <i class="fas fa-rocket text-sm"></i>
              </div>
              What You Get
            </h2>

            <div class="grid md:grid-cols-2 gap-6">
              <div class="hover-lift glass p-6 rounded-2xl border border-[rgba(255,255,255,.10)]">
                <div class="flex items-center gap-3 mb-3">
                  <i class="fas fa-compass text-sm"></i>
                  <h3 class="font-semibold">Go-to-Market Strategy</h3>
                </div>
                <p>Market mapping → positioning → testable hypotheses. Decide what to say, to whom, and where it moves the needle first.</p>
              </div>

              <div class="hover-lift glass p-6 rounded-2xl border border-[rgba(255,255,255,.10)]">
                <div class="flex items-center gap-3 mb-3">
                  <i class="fas fa-bullhorn text-sm"></i>
                  <h3 class="font-semibold">Brand Positioning & Messaging</h3>
                </div>
                <p>Value prop, proof, and voice—so site, pitch, and socials finally sound like one company.</p>
              </div>

              <div class="hover-lift glass p-6 rounded-2xl border border-[rgba(255,255,255,.10)]">
                <div class="flex items-center gap-3 mb-3">
                  <i class="fas fa-chart-line text-sm"></i>
                  <h3 class="font-semibold">Campaign Planning & Execution</h3>
                </div>
                <p>From insight to launch: creative, media, content, and iteration—right-sized for the team you have.</p>
              </div>

              <div class="hover-lift glass p-6 rounded-2xl border border-[rgba(255,255,255,.10)]">
                <div class="flex items-center gap-3 mb-3">
                  <i class="fas fa-pen-fancy text-sm"></i>
                  <h3 class="font-semibold">Cross-Channel Narrative</h3>
                </div>
                <p>One story across product, web, email, and content—built to scale without bloating headcount.</p>
              </div>
            </div>
          </div>

          <!-- Consumer, SMB & Startup Teams -->
          <div id="sec-trust" class="mb-12">
            <h2 class="font-display text-3xl font-semibold mb-8 flex items-center gap-3">
              <div class="w-8 h-8 rounded-lg glass flex items-center justify-center">
                <i class="fas fa-users text-sm"></i>
              </div>
              Consumer, SMB & Startup Teams
            </h2>

            <div class="grid md:grid-cols-2 gap-8">
              <div class="stats-card hover-lift">
                <div class="relative z-10">
                  <h3 class="text-2xl font-bold mb-2">Consumer at Scale</h3>
                  <p style="color:var(--ink-2)" class="mb-4">Edge • Microsoft 365 • Surface • Teams • Copilot</p>
                  <div class="text-4xl font-bold">Attention → Resonance</div>
                  <div class="text-sm" style="color:var(--ink-2)">Stories that move people (and product)</div>
                </div>
              </div>

              <div class="testimonial-card hover-lift">
                <div class="relative z-10">
                  <p class="mb-4 italic" style="color:rgba(255,255,255,.88)">
                    “Luis made us sound like the company we’re becoming—sharp positioning, a crisp story, and a launch plan our small team could actually run.”
                  </p>
                  <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-coral rounded-full flex items-center justify-center">
                      <i class="fas fa-building text-white text-sm"></i>
                    </div>
                    <div>
                      <div class="font-semibold">Founder, Seed-Stage Startup</div>
                      <div class="text-sm" style="color:var(--ink-2)">Consumer tech • 8-person team</div>
                    </div>
                  </div>
                </div>
              </div>

              <div class="stats-card hover-lift md:col-span-2">
                <div class="relative z-10">
                  <h3 class="text-2xl font-bold mb-2">SMB & Startup GTM</h3>
                  <p style="color:var(--ink-2)" class="mb-4">Office SMB lead • test-and-learn roadmaps • lean creative systems</p>
                  <div class="text-4xl font-bold">0 → 1 → Scale</div>
                  <div class="text-sm" style="color:var(--ink-2)">Rigor, without the bloat</div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Right Column - 1/3 -->
        <div class="p-8 lg:p-12 border-l" id="sec-models" style="background:linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.01)); border-color:var(--line)">
          <h2 class="font-display text-3xl font-semibold mb-8 flex items-center gap-3">
            <div class="w-8 h-8 rounded-lg glass flex items-center justify-center">
              <i class="fas fa-handshake text-sm"></i>
            </div>
            Ways to Work Together
          </h2>

          <div class="space-y-4" id="packagesTable">
            <!-- Quick-Start -->
            <div class="package-row p-6 shadow-sm pkg" data-id="quick">
              <div class="flex items-start justify-between mb-3">
                <h3 class="font-semibold">Quick-Start Sprint</h3>
                <div class="font-bold text-lg" style="color:var(--coral)">$4k–$8k</div>
              </div>
              <p class="mb-4">Positioning audit • lightweight customer inputs • GTM roadmap you can run tomorrow</p>
              <div class="flex items-center gap-2 text-xs" style="color:var(--ink-2)">
                <i class="fas fa-clock"></i><span>2–4 weeks</span>
              </div>
            </div>

            <!-- Fractional -->
            <div class="package-row p-6 shadow-sm pkg" data-id="fractional">
              <div class="flex items-start justify-between mb-3">
                <h3 class="font-semibold">Fractional Leadership</h3>
                <div class="font-bold text-lg" style="color:var(--coral)">$3k–$6k/mo</div>
              </div>
              <p class="mb-4">Hands-on marketing leadership without full-time headcount</p>
              <div class="flex items-center gap-2 text-xs" style="color:var(--ink-2)">
                <i class="fas fa-calendar"></i><span>Ongoing partnership</span>
              </div>
            </div>

            <!-- Launch -->
            <div class="package-row p-6 shadow-sm pkg" data-id="launch">
              <div class="flex items-start justify-between mb-3">
                <h3 class="font-semibold">Launch Architect</h3>
                <div class="font-bold text-lg" style="color:var(--coral)">$10k–$20k</div>
              </div>
              <p class="mb-4">Messaging, creative, and channel plan from soft-signal tests to launch day</p>
              <div class="flex items-center gap-2 text-xs" style="color:var(--ink-2)">
                <i class="fas fa-rocket"></i><span>Full launch cycle</span>
              </div>
            </div>

            <!-- Ally -->
            <div class="package-row p-6 shadow-sm pkg" data-id="ally">
              <div class="flex items-start justify-between mb-3">
                <h3 class="font-semibold">Founder's Ally</h3>
                <div class="font-bold text-lg" style="color:var(--coral)">Custom</div>
              </div>
              <p class="mb-4">Long-term partnership (equity + cash hybrid)</p>
              <div class="flex items-center gap-2 text-xs" style="color:var(--ink-2)">
                <i class="fas fa-handshake"></i><span>Strategic partnership</span>
              </div>
            </div>
          </div>

          <div class="mt-8 p-4 glass rounded-xl border border-[rgba(255,255,255,.10)]">
            <div class="flex items-start gap-3">
              <i class="fas fa-info-circle mt-1"></i>
              <div>
                <p class="font-medium text-sm">Pricing Note</p>
                <p class="text-xs" style="color:var(--ink-2)">Final pricing depends on scope, timeline, and deliverables. Every engagement includes strategic guidance and clear checkpoints.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <div id="sec-footer" class="mt-12 pt-8 animate-fade-in" style="border-top:1px solid var(--line)">
      <div class="flex flex-col md:flex-row items-center justify-between gap-6">
        <div class="flex flex-col md:flex-row items-start md:items-center gap-3 md:gap-6" style="color:var(--ink-2)">
          <div class="flex items-center gap-2">
            <i class="fas fa-map-marker-alt" style="color:var(--coral)"></i>
            <span>Based in Seattle • Available globally</span>
          </div>
          <p class="text-sm">Building something ambitious with a small team? Let’s make it land.</p>
        </div>

        <div class="flex items-center gap-6">
          <a href="https://luis-gilberto.com" class="font-medium hover:opacity-90" style="color:var(--coral)">
            luis-gilberto.com
          </a>
          <div class="flex items-center gap-4">
            <a href="https://www.linkedin.com/in/luisgilberto00" class="w-10 h-10 glass rounded-full flex items-center justify-center hover-lift" aria-label="LinkedIn">
              <i class="fab fa-linkedin"></i>
            </a>
            <a href="mailto:hello@luis-gilberto.com" class="w-10 h-10 glass rounded-full flex items-center justify-center hover-lift" aria-label="Email">
              <i class="fas fa-envelope"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script>
    // Utility
    const qs = new URLSearchParams(location.search);
    const $ = (s, p=document) => p.querySelector(s);
    const $$ = (s, p=document) => Array.from(p.querySelectorAll(s));

    // Apply incoming query params
    (function initFromQuery(){
      const show = (qs.get('show')||'intro,offer,trust,models,footer')
        .split(',').map(s=>s.trim().toLowerCase());
      const pkgs = (qs.get('packages')||'quick,fractional,launch,ally')
        .split(',').map(s=>s.trim());

      const map = { intro:'sec-intro', offer:'sec-offer', trust:'sec-trust', models:'sec-models', footer:'sec-footer' };

      Object.entries(map).forEach(([k,id])=>{
        const el = document.getElementById(id);
        const on = show.includes(k);
        if (el) el.style.display = on ? '' : 'none';
        const chk = document.querySelector(`[data-toggle="${id}"]`);
        if (chk) chk.checked = on;
      });

      $$('.pkg').forEach(row=>{
        const id = row.dataset.id;
        const on = pkgs.includes(id);
        row.style.display = on ? 'block' : 'none';
        const chk = document.querySelector(`[data-package="${id}"]`);
        if (chk) chk.checked = on;
      });
    })();

    // Live toggles
    $$('#configBar [data-toggle]').forEach(chk=>{
      chk.addEventListener('change', e=>{
        const id = e.target.getAttribute('data-toggle');
        const el = document.getElementById(id);
        if (!el) return;
        el.style.display = e.target.checked ? '' : 'none';
      });
    });

    $$('#configBar [data-package]').forEach(chk=>{
      chk.addEventListener('change', e=>{
        const id = e.target.getAttribute('data-package');
        const row = document.querySelector(`.pkg[data-id="${id}"]`);
        if (row) row.style.display = e.target.checked ? 'block' : 'none';
      });
    });

    // Copy share link
    $('#copyLink')?.addEventListener('click', ()=>{
      const sections = [];
      const map = { 'sec-intro':'intro','sec-offer':'offer','sec-trust':'trust','sec-models':'models','sec-footer':'footer' };
      Object.entries(map).forEach(([id,key])=>{
        const el = document.getElementById(id);
        if (el && el.style.display !== 'none') sections.push(key);
      });
      const pkgs = $$('.pkg').filter(r=>r.style.display!=='none').map(r=>r.dataset.id);
      const url = new URL(location.href);
      url.searchParams.set('show', sections.join(','));
      url.searchParams.set('packages', pkgs.join(','));
      navigator.clipboard.writeText(url.toString()).then(()=>{
        const toast = document.createElement('div');
        toast.className = 'fixed top-4 right-4 bg-green-500 text-white px-6 py-3 rounded-lg shadow-lg z-50 flex items-center gap-2';
        toast.innerHTML = '<i class="fas fa-check"></i> Share link copied to clipboard!';
        document.body.appendChild(toast);
        setTimeout(() => toast.remove(), 3000);
      });
    });

    // Soft reveal on .hover-lift
    document.addEventListener('DOMContentLoaded', function() {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
          }
        });
      }, { threshold: 0.08 });

      $$('.hover-lift').forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(16px)';
        el.style.transition = 'all 0.6s ease';
        observer.observe(el);
      });
    });
  </script>
</body>
</html>
```


