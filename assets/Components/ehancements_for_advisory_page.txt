<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luis Gilberto - Advisory Services</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* ===================================
           SMOOTH SCROLL FOUNDATION
        =================================== */
        html {
            scroll-behavior: smooth;
            scroll-snap-type: y proximity;
            overflow-x: hidden;
        }

        /* Smooth scroll sections */
        section {
            scroll-snap-align: start;
            scroll-snap-stop: normal;
        }

        /* ===================================
           ENHANCED COLOR SYSTEM
        =================================== */
        :root {
            --lg-coral: #f96f6e;
            --lg-teal: #2ed3c6;
            --lg-bg: #0f0f0f;
            --lg-fg: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.7);
            --text-muted: rgba(255, 255, 255, 0.6);
            --glass-bg: rgba(0, 0, 0, 0.3);
            --glass-border: rgba(255, 255, 255, 0.2);
            --hover-bg: rgba(255, 255, 255, 0.12);
            
            /* Enhanced subtle backgrounds */
            --bg-gradient: radial-gradient(1200px 800px at 10% -10%, rgba(249,111,110,0.08), rgba(0,0,0,0) 60%),
                          radial-gradient(1000px 700px at 90% 10%, rgba(29,211,176,0.06), rgba(0,0,0,0) 60%),
                          linear-gradient(180deg, #0a0a0a 0%, #121212 50%, #0f0f0f 100%);
        }

        /* ===================================
           GLOBAL RESET & BASE
        =================================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            line-height: 1.65;
            color: var(--lg-fg);
            background: var(--bg-gradient);
            overflow-x: hidden;
            position: relative;
            min-height: 100vh;
        }

        /* ===================================
           REFINED TYPOGRAPHY SCALE
        =================================== */
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
            font-weight: 600;
            line-height: 1.15;
            letter-spacing: -0.02em;
        }

        /* Consistent type scale - only 3 sizes */
        h1 { font-size: clamp(3rem, 5.5vw, 4.5rem); }
        h2 { font-size: clamp(2rem, 3.5vw, 2.75rem); }
        h3 { font-size: clamp(1.25rem, 2vw, 1.5rem); }

        p {
            font-size: 1.125rem;
            line-height: 1.65;
            max-width: 65ch; /* Optimal reading length */
        }

        /* ===================================
           LAYOUT CONTAINER
        =================================== */
        .main-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            position: relative;
            z-index: 1;
        }

        /* ===================================
           HERO SECTION - ENHANCED
        =================================== */
        .hero {
            min-height: 100vh;
            padding: 6rem 0 8rem;
            display: flex;
            align-items: center;
            position: relative;
        }

        /* Subtle atmospheric effects */
        .hero::before {
            content: '';
            position: absolute;
            top: -80px;
            left: -100px;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(249, 111, 110, 0.06), transparent 70%);
            z-index: 0;
            pointer-events: none;
            animation: float 20s ease-in-out infinite;
        }

        .hero::after {
            content: '';
            position: absolute;
            bottom: -100px;
            right: -80px;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(46, 211, 198, 0.04), transparent 70%);
            z-index: 0;
            pointer-events: none;
            animation: float 25s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(30px, -30px); }
        }

        .hero-content {
            position: relative;
            text-align: left;
            max-width: 900px;
            margin: 0 auto;
        }

        /* Enhanced portrait with halo effect */
        .hero-photo {
            width: 140px;
            height: 140px;
            border-radius: 50%;
            object-fit: cover;
            float: left;
            margin: 0 2.5rem 1.5rem 0;
            shape-outside: circle(70px at 50% 50%);
            shape-margin: 2rem;
            position: relative;
            z-index: 2;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            
            /* Refined shadow with brand halo */
            box-shadow: 
                0 0 0 3px rgba(249, 111, 110, 0.15),
                0 0 30px rgba(249, 111, 110, 0.2),
                0 12px 40px rgba(0, 0, 0, 0.3);
        }

        .hero-photo:hover {
            transform: scale(1.05);
            box-shadow: 
                0 0 0 4px rgba(249, 111, 110, 0.25),
                0 0 40px rgba(249, 111, 110, 0.3),
                0 16px 50px rgba(0, 0, 0, 0.4);
        }

        .hero-brow {
            font-family: 'Inter', sans-serif;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.25em;
            color: var(--lg-coral);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .hero h1 {
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .highlight {
            color: var(--lg-coral);
            font-weight: 700;
        }

        .hero-subtitle {
            font-size: 1.25rem;
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            line-height: 1.5;
            font-weight: 400;
        }

        .hero-value-prop {
            font-size: 1.125rem;
            color: var(--text-muted);
            margin-bottom: 3rem;
            line-height: 1.65;
            clear: left;
        }

        .hero-buttons {
            display: flex;
            gap: 1.2rem;
            flex-wrap: wrap;
            clear: left;
        }

        .hero-cta {
            display: inline-flex;
            align-items: center;
            padding: 18px 36px;
            background: var(--lg-coral);
            color: var(--lg-fg);
            text-decoration: none;
            border-radius: 12px;
            font-weight: 700;
            font-size: 1.05rem;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border: 2px solid var(--lg-coral);
            position: relative;
            overflow: hidden;
        }

        .hero-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.6s;
        }

        .hero-cta:hover::before {
            left: 100%;
        }

        .hero-cta:hover {
            transform: translateY(-3px) scale(1.03);
            box-shadow: 
                0 12px 35px rgba(249, 111, 110, 0.4),
                0 0 25px rgba(249, 111, 110, 0.2);
        }

        .hero-cta-secondary {
            background: transparent;
            border: 2px solid var(--lg-coral);
            color: var(--lg-coral);
        }

        /* ===================================
           ELEGANT SECTION DIVIDERS
        =================================== */
        .section-divider {
            padding: 4rem 0;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0.6;
        }

        .divider-line {
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--glass-border), transparent);
        }

        .divider-content {
            padding: 0 2rem;
            font-family: 'Inter', sans-serif;
            font-size: 0.75rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 500;
        }

        /* ===================================
           SECTION SPACING & RHYTHM
        =================================== */
        .services, .investment, .microsoft-legacy {
            min-height: 100vh;
            padding: 8rem 0;
            position: relative;
        }

        /* Add breathing room between sections */
        .services { padding-top: 10rem; }
        .investment { 
            padding-top: 10rem;
            background: rgba(0, 0, 0, 0.15);
            backdrop-filter: blur(10px);
        }
        .microsoft-legacy { padding-top: 8rem; }

        /* ===================================
           SECTION HEADERS - UNIFIED STYLE
        =================================== */
        .section-header {
            text-align: center;
            margin-bottom: 4rem;
            padding: 0 2rem;
        }

        .section-header h2 {
            margin-bottom: 1rem;
            position: relative;
            display: inline-block;
        }

        /* Subtle accent underline */
        .section-header h2::after {
            content: '';
            position: absolute;
            bottom: -0.5rem;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--lg-coral), transparent);
        }

        .section-header p {
            font-size: 1.125rem;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 1.5rem auto 0;
        }

        /* ===================================
           SERVICES GRID
        =================================== */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2.5rem;
            margin-top: 4rem;
        }

        .service-group {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            backdrop-filter: blur(20px);
        }

        .service-group:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            border-color: rgba(255, 255, 255, 0.3);
        }

        .service-group-header {
            padding: 2.5rem;
            text-align: center;
        }

        .strategy-group .service-group-header {
            background: linear-gradient(135deg, var(--lg-coral), #ff8a80);
        }

        .creative-group .service-group-header {
            background: linear-gradient(135deg, var(--lg-teal), #4dd0e1);
        }

        .service-card {
            padding: 1.75rem;
            border-bottom: 1px solid var(--glass-border);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .service-card:hover {
            background: var(--hover-bg);
        }

        /* ===================================
           PRICING CARDS
        =================================== */
        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2.5rem;
            margin-top: 4rem;
        }

        .investment-card {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 2.5rem;
            text-align: center;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            backdrop-filter: blur(20px);
        }

        .investment-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            border-color: rgba(255, 255, 255, 0.3);
        }

        .investment-card.featured {
            border: 2px solid rgba(249, 111, 110, 0.3);
            background: linear-gradient(135deg, rgba(249, 111, 110, 0.08), rgba(249, 111, 110, 0.04));
        }

        .tier-badge {
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, var(--lg-coral), #e94e4d);
            color: white;
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 0.7rem;
            font-weight: 700;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        /* ===================================
           FINAL CTA SECTION
        =================================== */
        .final-cta {
            text-align: center;
            padding: 8rem 0;
            position: relative;
        }

        .final-cta h2 {
            margin-bottom: 1.5rem;
        }

        .final-cta p {
            color: var(--text-secondary);
            margin: 0 auto 3rem;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 20px 40px;
            background: var(--lg-coral);
            color: white;
            text-decoration: none;
            border-radius: 12px;
            font-weight: 700;
            font-size: 1.125rem;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 8px 30px rgba(249, 111, 110, 0.3);
        }

        .cta-button:hover {
            transform: translateY(-4px);
            box-shadow: 0 16px 50px rgba(249, 111, 110, 0.5);
        }

        /* ===================================
           FADE-IN ANIMATIONS
        =================================== */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* ===================================
           RESPONSIVE DESIGN
        =================================== */
        @media (max-width: 768px) {
            .hero {
                padding: 4rem 0;
            }

            .hero-photo {
                float: none;
                shape-outside: none;
                margin: 0 auto 2rem;
                display: block;
            }

            .hero h1, .hero-subtitle, .hero-value-prop {
                text-align: center;
                clear: both;
            }

            .hero-buttons {
                justify-content: center;
            }

            .services, .investment, .microsoft-legacy {
                padding: 5rem 0;
            }

            .services-grid, .pricing-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="main-content">
        <!-- HERO SECTION -->
        <section class="hero" id="hero">
            <div class="hero-content fade-in">
                <img src="assets/Luis_portrait_circle.png" alt="Luis Gilberto" class="hero-photo" />
                
                <div class="hero-brow">ADVISORY</div>
                <h1>
                    <span class="highlight">Clarity</span> You Need.<br>
                    <span class="highlight">Strategy</span> You Deserve.
                </h1>
                
                <p class="hero-subtitle">
                    Forget generic frameworks. I look at where you are and what you've got… then chart the strategy you actually deserve.
                </p>
                
                <p class="hero-value-prop">
                    Helping brands unlock growth through strategic insight and emotionally resonant storytelling.
                </p>
                
                <div class="hero-buttons">
                    <a href="#" class="hero-cta">Get Custom Quote</a>
                    <a href="#" class="hero-cta hero-cta-secondary">Start Strategy Discussion</a>
                </div>
            </div>
        </section>

        <!-- SECTION DIVIDER -->
        <div class="section-divider">
            <div class="divider-line"></div>
            <div class="divider-content">Strategic Services</div>
            <div class="divider-line"></div>
        </div>

        <!-- SERVICES SECTION -->
        <section class="services" id="services">
            <div class="section-header fade-in">
                <h2>Strategic Services</h2>
                <p>Comprehensive consulting services designed to accelerate your business growth through strategic thinking and creative execution.</p>
            </div>
            
            <div class="services-grid">
                <div class="service-group fade-in">
                    <div class="service-group-header">
                        <h3>Strategy</h3>
                        <p style="opacity: 0.9;">Foundation & Direction</p>
                    </div>
                    <!-- Service cards here -->
                </div>
                
                <div class="service-group fade-in">
                    <div class="service-group-header">
                        <h3>Creative</h3>
                        <p style="opacity: 0.9;">Expression & Execution</p>
                    </div>
                    <!-- Service cards here -->
                </div>
            </div>
        </section>

        <!-- SECTION DIVIDER -->
        <div class="section-divider">
            <div class="divider-line"></div>
            <div class="divider-content">Investment</div>
            <div class="divider-line"></div>
        </div>

        <!-- INVESTMENT SECTION -->
        <section class="investment" id="investment">
            <div class="section-header fade-in">
                <h2>Investment Levels</h2>
                <p>Flexible engagement models designed to match your project scope and budget requirements.</p>
            </div>
            
            <div class="pricing-grid">
                <div class="investment-card fade-in">
                    <h3>Quick-Start Sprint</h3>
                    <p style="color: var(--text-secondary); margin: 1rem 0;">2–4 weeks engagement</p>
                    <!-- Pricing details -->
                </div>
                
                <div class="investment-card featured fade-in" style="position: relative;">
                    <div class="tier-badge">MOST POPULAR</div>
                    <h3>Strategic Planning</h3>
                    <p style="color: var(--text-secondary); margin: 1rem 0;">6–10 weeks engagement</p>
                    <!-- Pricing details -->
                </div>
                
                <div class="investment-card fade-in">
                    <h3>Strategic Intelligence</h3>
                    <p style="color: var(--text-secondary); margin: 1rem 0;">4–8 weeks engagement</p>
                    <!-- Pricing details -->
                </div>
            </div>
        </section>

        <!-- SECTION DIVIDER -->
        <div class="section-divider">
            <div class="divider-line"></div>
            <div class="divider-content">Proven Impact</div>
            <div class="divider-line"></div>
        </div>

        <!-- MICROSOFT LEGACY SECTION -->
        <section class="microsoft-legacy" id="microsoft">
            <div class="section-header fade-in">
                <h2>15+ Years in Motion</h2>
                <p>From Windows to Copilot, shaping brands and products across Microsoft—now fueling clarity and growth for what's next.</p>
            </div>
        </section>

        <!-- FINAL CTA -->
        <section class="final-cta fade-in">
            <h2>Ready to Define Your Strategy?</h2>
            <p>Let's chart the path from where you are to where you deserve to be.</p>
            <a href="#" class="cta-button">
                Start Your Strategy
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                    <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </a>
        </section>
    </div>

    <script>
        // SMOOTH SCROLL ENHANCEMENT
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // FADE-IN ON SCROLL ANIMATION
        const observerOptions = {
            threshold: 0.15,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach((el) => {
            observer.observe(el);
        });

        // Initial fade-in for hero
        setTimeout(() => {
            document.querySelector('.hero .fade-in')?.classList.add('visible');
        }, 200);
    </script>
</body>
</html>