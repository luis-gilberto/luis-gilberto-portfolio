=================================================================
IMPLEMENTATION INSTRUCTIONS: INVESTMENT SECTION VISUAL FIXES
=================================================================

OVERVIEW:
Fix three visual issues in the Investment Levels section:
1. Price lock input is too large (should be more compact)
2. Checkmarks are overlapping text (positioning issue)
3. Featured card needs teal glassmorphism effect

=================================================================
FIX #1: COMPACT PRICE LOCK INPUT
=================================================================

LOCATION: Find the .price-lock styles (around line 1060-1100)

FIND:
.price-lock {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 12px;
    padding: 0.75rem 1rem;
    margin: 1rem auto 0;
    max-width: 720px;
}

REPLACE WITH:
.price-lock {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 12px;
    padding: 0.75rem 1rem;
    margin: 1rem auto 2rem;
    max-width: 580px;  /* ✅ CHANGED: Narrower container */
}

---

FIND:
.price-input {
    flex: 1 1 200px;
    min-width: 160px;
    width: 100%;
    padding: 0.5rem 0.65rem;
    height: 44px;
    font-size: 0.95rem;
    border-radius: 8px;
    border: 1px solid var(--glass-border);
    background: rgba(255,255,255,0.03);
    color: var(--ink);
    outline: none;
}

REPLACE WITH:
.price-input {
    flex: 0 1 180px;  /* ✅ CHANGED: Don't grow, max 180px */
    min-width: 140px;  /* ✅ CHANGED: Smaller minimum */
    padding: 0.5rem 0.75rem;
    height: 40px;  /* ✅ CHANGED: Slightly shorter */
    font-size: 0.9rem;  /* ✅ CHANGED: Smaller text */
    border-radius: 8px;
    border: 1px solid var(--glass-border);
    background: rgba(255,255,255,0.03);
    color: var(--ink);
    outline: none;
}

---

FIND:
.lock-label { font-weight: 600; color: var(--ink-muted); }

REPLACE WITH:
.lock-label { 
    font-weight: 600; 
    color: var(--ink-muted);
    font-size: 0.9rem;  /* ✅ ADDED: Smaller label */
}

---

FIND:
.price-status { min-width: 160px; font-size: 0.9rem; color: var(--ink-muted); }

REPLACE WITH:
.price-status { 
    flex: 0 1 140px;  /* ✅ CHANGED: Don't grow */
    font-size: 0.85rem;  /* ✅ CHANGED: Smaller text */
    color: var(--ink-muted); 
}

=================================================================
FIX #2: CHECKMARK POSITIONING
=================================================================

LOCATION: Find .tier-features styles (around line 950-980)

FIND:
.tier-features li {
    padding: 0.5rem 0;
    color: var(--ink-dim);
    position: relative;
    padding-left: 1.5rem;
}

.tier-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--teal);
    font-weight: bold;
}

REPLACE WITH:
.tier-features li {
    padding: 0.625rem 0;
    color: var(--ink-dim);
    position: relative;
    padding-left: 1.75rem;  /* ✅ CHANGED: More space */
    line-height: 1.5;  /* ✅ ADDED: Better line height */
}

.tier-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    top: 0.625rem;  /* ✅ ADDED: Vertical alignment */
    color: var(--teal);
    font-weight: bold;
    font-size: 0.9rem;  /* ✅ ADDED: Slightly smaller checkmark */
}

=================================================================
FIX #3: TEAL GLASSMORPHISM FOR FEATURED CARD
=================================================================

LOCATION: Find .tier-featured styles (around line 920-930)

FIND:
.tier-featured {
    border-color: var(--teal);
    background: rgba(46, 211, 198, 0.05);
}

REPLACE WITH:
.tier-featured {
    border: 2px solid rgba(46, 211, 198, 0.4);  /* ✅ CHANGED: Stronger border */
    background: linear-gradient(
        135deg,
        rgba(46, 211, 198, 0.08) 0%,
        rgba(46, 211, 198, 0.03) 100%
    );  /* ✅ CHANGED: Gradient glassmorphism */
    backdrop-filter: blur(16px);  /* ✅ ADDED: Stronger blur */
    box-shadow: 
        0 8px 32px rgba(46, 211, 198, 0.15),
        inset 0 1px 0 rgba(46, 211, 198, 0.2);  /* ✅ ADDED: Glow effect */
}

---

ALSO ADD THIS NEW STYLE (after .tier-featured):

.tier-featured::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 20px;
    padding: 2px;
    background: linear-gradient(
        135deg,
        rgba(46, 211, 198, 0.3) 0%,
        rgba(46, 211, 198, 0.1) 50%,
        rgba(46, 211, 198, 0.3) 100%
    );
    -webkit-mask: 
        linear-gradient(#fff 0 0) content-box, 
        linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    pointer-events: none;
    z-index: -1;
}

=================================================================
MOBILE ADJUSTMENTS
=================================================================

LOCATION: In the @media (max-width: 480px) block

ADD THIS:

@media (max-width: 480px) {
    .price-lock { 
        flex-direction: column; 
        align-items: stretch; 
        gap: 0.5rem; 
        padding: 1rem;  /* ✅ More padding on mobile */
    }
    
    .price-input { 
        flex: 1;
        min-width: 100%;
        height: 44px; 
        font-size: 1rem; 
    }
    
    .lock-label,
    .price-status {
        width: 100%;
        text-align: center;
    }
}

=================================================================
TESTING CHECKLIST:
=================================================================

[ ] Price lock section is compact (not overly wide)
[ ] Password input is ~180px wide on desktop
[ ] Checkmarks align properly with text (not overlapping)
[ ] Featured card has visible teal glassmorphism
[ ] Featured card has subtle border glow
[ ] Mobile: Price lock stacks vertically and looks clean
[ ] All text is readable against backgrounds

=================================================================
VISUAL RESULT:
=================================================================

✅ Compact, professional price lock (like shown in mockup)
✅ Checkmarks properly positioned to the left of text
✅ Featured card stands out with teal glass effect and glow
✅ Matches the design expectations from the original mockup

=================================================================
END OF IMPLEMENTATION INSTRUCTIONS
=================================================================