# Redirects
# /about.html /about 301  <-- REMOVED to prevent redirect loops
# /contact.html /contact 301 <-- REMOVED to prevent redirect loops

# Fallback for SPA (if needed, but this is a static site)
# /* /index.html 200
