TRAE AI IDE IMPLEMENTATION INSTRUCTIONS
=====================================

OBJECTIVE: Remove dark overlay rectangles from background sections and implement elegant compass watermark backgrounds matching the demo exactly.

COMPASS IMAGE ASSETS PROVIDED:
- hero-compass.png (Coral-teal compass for hero section)
- services-compass.png (Teal-coral compass for services section) 
- investment-compass.png (Compact alternating compass for investment section)

STEP 1: CLEAN EXISTING CONFLICTING CODE
======================================

REMOVE these existing CSS rules completely from the HTML file:

1. Remove ALL existing compass background CSS (lines approximately 1820-2080):
   - Remove: .hero::before { ... } (the existing SVG compass)
   - Remove: .hero::after { ... } (dark overlay)
   - Remove: .services::before { ... } (existing SVG compass)
   - Remove: .services::after { ... } (dark overlay)
   - Remove: .investment::before { ... } (existing SVG compass)
   - Remove: .investment::after { ... } (dark overlay)
   - Keep: .microsoft-legacy::before { ... } (DO NOT TOUCH - this one works correctly)

2. Remove dark background from investment section:
   - Find: .investment { ... background: rgba(0, 0, 0, 0.2); ... }
   - Change to: .investment { ... background: transparent; ... }

3. Remove all animation keyframes related to old compass:
   - Remove: @keyframes compassRotate { ... }
   - Remove: @keyframes compassGlow { ... }
   - Remove: @keyframes float-rotate { ... }
   - Remove: @keyframes float-rotate-reverse { ... }
   - Remove: @keyframes pulse-glow { ... }

STEP 2: IMPLEMENT NEW COMPASS BACKGROUNDS
========================================

ADD this exact CSS code to replace the removed sections:

```css
/* ===== NEW ELEGANT COMPASS BACKGROUNDS ===== */

/* Hero Section - Elegant Coral-Teal Compass */
.hero::before {
    content: '';
    position: absolute;
    top: 20%;
    right: -10%;
    width: 600px;
    height: 600px;
    background-image: url('assets/hero-compass.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.08;
    transform: rotate(15deg);
    pointer-events: none;
    z-index: 0;
    animation: gentle-rotate 120s linear infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.8) 40%,
        rgba(0,0,0,0.4) 70%,
        rgba(0,0,0,0.1) 85%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.8) 40%,
        rgba(0,0,0,0.4) 70%,
        rgba(0,0,0,0.1) 85%,
        transparent 100%
    );
}

/* Services Section - Elegant Teal-Coral Compass */
.services::before {
    content: '';
    position: absolute;
    top: 15%;
    left: -5%;
    width: 700px;
    height: 700px;
    background-image: url('assets/services-compass.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.06;
    transform: rotate(-20deg);
    pointer-events: none;
    z-index: 0;
    animation: gentle-rotate-reverse 100s linear infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.7) 35%,
        rgba(0,0,0,0.3) 65%,
        rgba(0,0,0,0.05) 85%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.7) 35%,
        rgba(0,0,0,0.3) 65%,
        rgba(0,0,0,0.05) 85%,
        transparent 100%
    );
}

/* Investment Section - Elegant Compact Compass */
.investment::before {
    content: '';
    position: absolute;
    bottom: 20%;
    right: 10%;
    width: 500px;
    height: 500px;
    background-image: url('assets/investment-compass.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.07;
    transform: rotate(30deg);
    pointer-events: none;
    z-index: 0;
    animation: gentle-float 80s ease-in-out infinite;
    mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.75) 30%,
        rgba(0,0,0,0.4) 60%,
        rgba(0,0,0,0.1) 80%,
        transparent 100%
    );
    -webkit-mask-image: radial-gradient(
        circle at center,
        rgba(0,0,0,1) 0%,
        rgba(0,0,0,0.75) 30%,
        rgba(0,0,0,0.4) 60%,
        rgba(0,0,0,0.1) 80%,
        transparent 100%
    );
}

/* New Gentle Animations */
@keyframes gentle-rotate {
    0% { transform: rotate(15deg); }
    100% { transform: rotate(375deg); }
}

@keyframes gentle-rotate-reverse {
    0% { transform: rotate(-20deg); }
    100% { transform: rotate(-380deg); }
}

@keyframes gentle-float {
    0%, 100% { 
        transform: rotate(30deg) translateY(0px);
        opacity: 0.07;
    }
    50% { 
        transform: rotate(32deg) translateY(-5px);
        opacity: 0.09;
    }
}

/* Mobile Responsive Adjustments */
@media (max-width: 768px) {
    .hero::before {
        width: 400px;
        height: 400px;
        right: -20%;
        opacity: 0.06;
    }

    .services::before {
        width: 500px;
        height: 500px;
        left: -15%;
        opacity: 0.05;
    }

    .investment::before {
        width: 350px;
        height: 350px;
        right: 5%;
        opacity: 0.06;
    }
}

@media (max-width: 480px) {
    .hero::before {
        width: 300px;
        height: 300px;
        opacity: 0.05;
    }

    .services::before {
        width: 350px;
        height: 350px;
        opacity: 0.04;
    }

    .investment::before {
        width: 250px;
        height: 250px;
        opacity: 0.05;
    }
}
```

STEP 3: VERIFY IMPLEMENTATION
============================

After implementation, verify these requirements are met:

1. NO dark rectangle overlays visible behind any section
2. Hero section shows subtle coral-teal compass in top-right area
3. Services section shows subtle teal-coral compass in top-left area  
4. Investment section shows subtle compact compass in bottom-right area
5. Legacy section compass remains unchanged (blue compass working correctly)
6. All compass backgrounds are very subtle (barely visible, elegant watermarks)
7. All content remains fully readable and properly positioned above backgrounds
8. Mobile responsive behavior maintained across all screen sizes

CRITICAL SUCCESS CRITERIA:
- Result must match the demo exactly
- NO dark overlays or rectangles blocking background patterns
- Compass backgrounds are elegant watermarks, not prominent design elements
- All existing functionality and layout preserved
- Legacy section compass untouched and working as before

ASSET REQUIREMENTS:
- Place compass image files in assets/ folder relative to HTML file
- hero-compass.png → Coral-teal compass for hero section
- services-compass.png → Teal-coral compass for services section  
- investment-compass.png → Compact alternating compass for investment section

TROUBLESHOOTING:
- If compass images don't appear: verify file paths and names match exactly
- If overlays still visible: ensure ALL ::after pseudo-elements are removed from hero, services, and investment sections
- If backgrounds too prominent: verify opacity values are exactly as specified (0.06-0.08)
- If mobile issues: ensure responsive media queries are included exactly as provided

FINAL VERIFICATION CHECKLIST:
□ Dark rectangles completely removed
□ Elegant compass watermarks visible but subtle
□ Hero compass: coral-teal, top-right, rotating slowly
□ Services compass: teal-coral, top-left, reverse rotation
□ Investment compass: compact design, bottom-right, gentle float
□ Legacy compass: unchanged and working
□ Mobile responsive across all screen sizes
□ All content readable and properly positioned